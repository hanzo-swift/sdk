//
// PromoteReq.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct PromoteReq: Sendable, Codable, ParameterConvertible, Hashable {

    /** App is the application's slug, from the path. */
    public var app: String?
    /** DeploymentID promotes that deployment's exact built image. One of this and Tag is required. */
    public var deploymentId: String?
    /** Project is the project the application lives under, from the path. */
    public var project: String?
    /** Tag promotes an image tag, resolved the same way a deploy resolves one. */
    public var tag: String?

    public init(app: String? = nil, deploymentId: String? = nil, project: String? = nil, tag: String? = nil) {
        self.app = app
        self.deploymentId = deploymentId
        self.project = project
        self.tag = tag
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case app
        case deploymentId
        case project
        case tag
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(app, forKey: .app)
        try container.encodeIfPresent(deploymentId, forKey: .deploymentId)
        try container.encodeIfPresent(project, forKey: .project)
        try container.encodeIfPresent(tag, forKey: .tag)
    }
}

