//
// OppReq.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct OppReq: Sendable, Codable, ParameterConvertible, Hashable {

    /** Amount is the deal value in minor units (cents) of Currency. */
    public var amount: Int?
    /** CloseDate is the expected close, as a unix second (0 = unset). */
    public var closeDate: Int?
    /** CompanyID links the deal to one of the org's companies. */
    public var companyId: String?
    /** Currency is the ISO code Amount is denominated in; empty defaults to USD. */
    public var currency: String?
    /** ID names the opportunity to update and comes from the path. A create ignores it: the server mints the id. */
    public var id: String?
    /** Name is the deal name. Required. */
    public var name: String?
    /** PointOfContact links the deal to one of the org's contacts. */
    public var pointOfContactId: String?
    /** Stage is the pipeline stage: NEW, SCREENING, MEETING, PROPOSAL or CUSTOMER (case-insensitive). Empty defaults to NEW. */
    public var stage: String?

    public init(amount: Int? = nil, closeDate: Int? = nil, companyId: String? = nil, currency: String? = nil, id: String? = nil, name: String? = nil, pointOfContactId: String? = nil, stage: String? = nil) {
        self.amount = amount
        self.closeDate = closeDate
        self.companyId = companyId
        self.currency = currency
        self.id = id
        self.name = name
        self.pointOfContactId = pointOfContactId
        self.stage = stage
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case amount
        case closeDate
        case companyId
        case currency
        case id
        case name
        case pointOfContactId
        case stage
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(amount, forKey: .amount)
        try container.encodeIfPresent(closeDate, forKey: .closeDate)
        try container.encodeIfPresent(companyId, forKey: .companyId)
        try container.encodeIfPresent(currency, forKey: .currency)
        try container.encodeIfPresent(id, forKey: .id)
        try container.encodeIfPresent(name, forKey: .name)
        try container.encodeIfPresent(pointOfContactId, forKey: .pointOfContactId)
        try container.encodeIfPresent(stage, forKey: .stage)
    }
}


@available(iOS 13, tvOS 13, watchOS 6, macOS 10.15, *)
extension OppReq: Identifiable {}
