//
// EnableResp.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct EnableResp: Sendable, Codable, ParameterConvertible, Hashable {

    /** AccountToken is the org's own tunnel-account credential. Treat it as a secret: it is what the CLI enables an environment with. */
    public var accountToken: String?
    /** Controller is the public controller endpoint the CLI enables against. */
    public var controller: String?
    /** Namespace is the public frontend a share is published into, when the deployment names one. */
    public var namespace: String?
    /** URLTemplate is the shape a share token expands to, so the CLI can print the resulting URL without asking again. */
    public var urlTemplate: String?

    public init(accountToken: String? = nil, controller: String? = nil, namespace: String? = nil, urlTemplate: String? = nil) {
        self.accountToken = accountToken
        self.controller = controller
        self.namespace = namespace
        self.urlTemplate = urlTemplate
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case accountToken
        case controller
        case namespace
        case urlTemplate
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(accountToken, forKey: .accountToken)
        try container.encodeIfPresent(controller, forKey: .controller)
        try container.encodeIfPresent(namespace, forKey: .namespace)
        try container.encodeIfPresent(urlTemplate, forKey: .urlTemplate)
    }
}

