//
// ProjectsCreate.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct ProjectsCreate: Sendable, Codable, ParameterConvertible, Hashable {

    /** Analytics is the opt-OUT for the wired-by-default analytics beacon: absent (nil) ⇒ ON (the default); explicit false ⇒ off. A pointer so \"unset\" is distinguishable from \"false\" — the only way to turn the default off. */
    public var analytics: Bool?
    public var description: String?
    public var framework: String?
    public var license: String?
    public var name: String?
    public var repo: ProjectsCreateRepo?
    public var slug: String?
    /** Upstream/License credit the third-party work this project was published from. Taken from any caller: disclaiming authorship can only cost the publisher credit, so it needs no gate (see Project.Upstream). */
    public var upstream: String?
    /** Visibility is \"public\" (the default when absent) or \"private\". Publishing publicly is ungated — that is the point of a community. Going PRIVATE is the paid feature, so an unfunded org asking for it is refused rather than silently downgraded (see resolve). */
    public var visibility: String?

    public init(analytics: Bool? = nil, description: String? = nil, framework: String? = nil, license: String? = nil, name: String? = nil, repo: ProjectsCreateRepo? = nil, slug: String? = nil, upstream: String? = nil, visibility: String? = nil) {
        self.analytics = analytics
        self.description = description
        self.framework = framework
        self.license = license
        self.name = name
        self.repo = repo
        self.slug = slug
        self.upstream = upstream
        self.visibility = visibility
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case analytics
        case description
        case framework
        case license
        case name
        case repo
        case slug
        case upstream
        case visibility
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(analytics, forKey: .analytics)
        try container.encodeIfPresent(description, forKey: .description)
        try container.encodeIfPresent(framework, forKey: .framework)
        try container.encodeIfPresent(license, forKey: .license)
        try container.encodeIfPresent(name, forKey: .name)
        try container.encodeIfPresent(repo, forKey: .repo)
        try container.encodeIfPresent(slug, forKey: .slug)
        try container.encodeIfPresent(upstream, forKey: .upstream)
        try container.encodeIfPresent(visibility, forKey: .visibility)
    }
}

