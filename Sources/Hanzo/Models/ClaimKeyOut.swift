//
// ClaimKeyOut.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct ClaimKeyOut: Sendable, Codable, ParameterConvertible, Hashable {

    /** ClaimKey is the capability itself. It is returned ONCE and never again — only its SHA-256 hash is stored — so a daemon that loses it mints a new one. */
    public var claimKey: String?
    /** TargetID is the machine the key authenticates. */
    public var targetId: String?

    public init(claimKey: String? = nil, targetId: String? = nil) {
        self.claimKey = claimKey
        self.targetId = targetId
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case claimKey
        case targetId
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(claimKey, forKey: .claimKey)
        try container.encodeIfPresent(targetId, forKey: .targetId)
    }
}

