//
// PricingRegionList.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct PricingRegionList: Sendable, Codable, ParameterConvertible, Hashable {

    /** Regions are the regions cloud instances can be placed in, each an opaque object exactly as the pricing source emits it — typically id, name and location. */
    public var regions: [[String: JSONValue]]?

    public init(regions: [[String: JSONValue]]? = nil) {
        self.regions = regions
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case regions
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(regions, forKey: .regions)
    }
}

