//
// ChatResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct ChatResponse: Sendable, Codable, ParameterConvertible, Hashable {

    /** Funnel is the org's trailing-window traffic → signups → orders. */
    public var funnel: Funnel?
    /** Reply is the coach's answer, grounded only in the quests and funnel below. When no AI plane is reachable it is the deterministic reply naming the top real quest — never silence, never invention. */
    public var reply: String?
    /** Suggestions are the current candidate quests, ranked best-first. */
    public var suggestions: [Suggestion]?

    public init(funnel: Funnel? = nil, reply: String? = nil, suggestions: [Suggestion]? = nil) {
        self.funnel = funnel
        self.reply = reply
        self.suggestions = suggestions
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case funnel
        case reply
        case suggestions
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(funnel, forKey: .funnel)
        try container.encodeIfPresent(reply, forKey: .reply)
        try container.encodeIfPresent(suggestions, forKey: .suggestions)
    }
}

