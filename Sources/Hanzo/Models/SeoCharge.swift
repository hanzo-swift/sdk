//
// SeoCharge.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct SeoCharge: Sendable, Codable, ParameterConvertible, Hashable {

    /** Op is the operation id — seoKeyword, seoRank — so a line here and a tool in a model's list are the same name. */
    public var op: String?
    /** Request is the flat charge for making the call, in USD, as an exact decimal string. */
    public var request: String?
    /** Result is the charge for each row returned, in USD, as an exact decimal string. Zero for the ops priced per request. */
    public var result: String?

    public init(op: String? = nil, request: String? = nil, result: String? = nil) {
        self.op = op
        self.request = request
        self.result = result
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case op
        case request
        case result
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(op, forKey: .op)
        try container.encodeIfPresent(request, forKey: .request)
        try container.encodeIfPresent(result, forKey: .result)
    }
}

