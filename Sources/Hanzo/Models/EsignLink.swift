//
// EsignLink.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct EsignLink: Sendable, Codable, ParameterConvertible, Hashable {

    /** Email is the address this link is meant for. */
    public var email: String?
    /** RecipientID is the recipient the link identifies. */
    public var recipientId: String?
    /** Role is their role — only a SIGNER or an APPROVER gets a link, because only they are asked to act. */
    public var role: String?
    /** SigningPath is the tail of the address to send them, relative to wherever the signing page is served. */
    public var signingPath: String?
    /** Token is the crypto-random signing capability. It is the entire credential, so treat it as a secret and give each one only to the recipient it names. */
    public var token: String?

    public init(email: String? = nil, recipientId: String? = nil, role: String? = nil, signingPath: String? = nil, token: String? = nil) {
        self.email = email
        self.recipientId = recipientId
        self.role = role
        self.signingPath = signingPath
        self.token = token
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case email
        case recipientId
        case role
        case signingPath
        case token
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(email, forKey: .email)
        try container.encodeIfPresent(recipientId, forKey: .recipientId)
        try container.encodeIfPresent(role, forKey: .role)
        try container.encodeIfPresent(signingPath, forKey: .signingPath)
        try container.encodeIfPresent(token, forKey: .token)
    }
}

