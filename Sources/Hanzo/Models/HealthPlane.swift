//
// HealthPlane.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct HealthPlane: Sendable, Codable, ParameterConvertible, Hashable {

    /** Bus is the address this process reaches the plane at. */
    public var bus: String?
    /** Ready reports whether an ingest would succeed right now. False is a 503. */
    public var ready: Bool?
    /** Reason is the plane's own failure text, present only when Ready is false. */
    public var reason: String?
    /** Stream is the JetStream stream every signal lands on. */
    public var stream: String?

    public init(bus: String? = nil, ready: Bool? = nil, reason: String? = nil, stream: String? = nil) {
        self.bus = bus
        self.ready = ready
        self.reason = reason
        self.stream = stream
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case bus
        case ready
        case reason
        case stream
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(bus, forKey: .bus)
        try container.encodeIfPresent(ready, forKey: .ready)
        try container.encodeIfPresent(reason, forKey: .reason)
        try container.encodeIfPresent(stream, forKey: .stream)
    }
}

