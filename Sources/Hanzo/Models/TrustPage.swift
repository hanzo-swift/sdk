//
// TrustPage.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct TrustPage: Sendable, Codable, ParameterConvertible, Hashable {

    /** Items is everything the centre publishes: what can be read now, and what exists and is released on request. */
    public var items: [TrustItem]?
    /** Name is the org's display name for its centre. */
    public var name: String?
    /** Nda is the text a party must accept to ask for the gated items, verbatim. Empty when the org asks for none. */
    public var nda: String?
    /** Slug is the centre's public address. */
    public var slug: String?

    public init(items: [TrustItem]? = nil, name: String? = nil, nda: String? = nil, slug: String? = nil) {
        self.items = items
        self.name = name
        self.nda = nda
        self.slug = slug
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case items
        case name
        case nda
        case slug
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(items, forKey: .items)
        try container.encodeIfPresent(name, forKey: .name)
        try container.encodeIfPresent(nda, forKey: .nda)
        try container.encodeIfPresent(slug, forKey: .slug)
    }
}

