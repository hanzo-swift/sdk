//
// ModelStream.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct ModelStream: Sendable, Codable, ParameterConvertible, Hashable {

    /** Config is the stream's configuration. */
    public var config: Config?
    /** Created is when the stream was created. */
    public var created: Date?
    /** Name is the stream name within the org. */
    public var name: String?
    /** State is the stream's current state. */
    public var state: State?

    public init(config: Config? = nil, created: Date? = nil, name: String? = nil, state: State? = nil) {
        self.config = config
        self.created = created
        self.name = name
        self.state = state
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case config
        case created
        case name
        case state
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(config, forKey: .config)
        try container.encodeIfPresent(created, forKey: .created)
        try container.encodeIfPresent(name, forKey: .name)
        try container.encodeIfPresent(state, forKey: .state)
    }
}

