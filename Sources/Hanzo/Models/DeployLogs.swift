//
// DeployLogs.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct DeployLogs: Sendable, Codable, ParameterConvertible, Hashable {

    /** DeploymentID is the deployment these logs belong to. */
    public var deploymentId: String?
    /** Logs is the recorded status timeline followed by the streamed pod output, newline-separated. */
    public var logs: String?
    /** Source says which pod the log body carries — `build`, `app`, or `none` when neither pod was reachable — so a console can label the pane honestly. */
    public var source: String?

    public init(deploymentId: String? = nil, logs: String? = nil, source: String? = nil) {
        self.deploymentId = deploymentId
        self.logs = logs
        self.source = source
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case deploymentId
        case logs
        case source
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(deploymentId, forKey: .deploymentId)
        try container.encodeIfPresent(logs, forKey: .logs)
        try container.encodeIfPresent(source, forKey: .source)
    }
}

