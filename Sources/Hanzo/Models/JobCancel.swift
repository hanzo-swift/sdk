//
// JobCancel.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct JobCancel: Sendable, Codable, ParameterConvertible, Hashable {

    /** ID is the job (activity) id, from the URL path. */
    public var id: String?
    /** Reason is recorded on the cancellation; empty records \"canceled from console\". */
    public var reason: String?
    /** Run is the run id; empty defaults to the job id, which is what the dispatcher sets (runId == activityId == prompt_id), so the common case sends no body. */
    public var run: String?

    public init(id: String? = nil, reason: String? = nil, run: String? = nil) {
        self.id = id
        self.reason = reason
        self.run = run
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case id
        case reason
        case run
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(id, forKey: .id)
        try container.encodeIfPresent(reason, forKey: .reason)
        try container.encodeIfPresent(run, forKey: .run)
    }
}


@available(iOS 13, tvOS 13, watchOS 6, macOS 10.15, *)
extension JobCancel: Identifiable {}
