//
// MarketCatalog.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct MarketCatalog: Sendable, Codable, ParameterConvertible, Hashable {

    /** Items is every capability the caller can see in their own (org, project), each carrying any public listing's shop metadata and whether it is installed. */
    public var items: [MarketItem]?

    public init(items: [MarketItem]? = nil) {
        self.items = items
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case items
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(items, forKey: .items)
    }
}

