//
// ProjectsDeploySiteRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Body for POST /v1/sites/deploy — deploy a raw file manifest. */
public struct ProjectsDeploySiteRequest: Codable, JSONEncodable, Hashable {

    public static let slugRule = StringRule(minLength: nil, maxLength: nil, pattern: "/^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$/")
    /** Site files (required, non-empty); index.html must be present at the root. */
    public var files: [ProjectsSiteFile]
    /** Target project slug; derived from name (or minted \"site-<token>\") when omitted. */
    public var slug: String?
    /** Display name; defaults to \"Site\". */
    public var name: String?

    public init(files: [ProjectsSiteFile], slug: String? = nil, name: String? = nil) {
        self.files = files
        self.slug = slug
        self.name = name
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case files
        case slug
        case name
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(files, forKey: .files)
        try container.encodeIfPresent(slug, forKey: .slug)
        try container.encodeIfPresent(name, forKey: .name)
    }
}

