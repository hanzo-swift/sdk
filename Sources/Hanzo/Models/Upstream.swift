//
// Upstream.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct Upstream: Sendable, Codable, ParameterConvertible, Hashable {

    /** Backends are the upstream servers to balance across: 1..32 of them. */
    public var backends: [Backend]?
    /** ID identifies the pool within the org: [A-Za-z0-9-_.], at most 128 chars. A create that omits it gets a generated one. Routes reference it by this id. */
    public var id: String?
    /** PassHostHeader forwards the client's original Host header upstream instead of rewriting it to the backend's. */
    public var passHostHeader: Bool?

    public init(backends: [Backend]? = nil, id: String? = nil, passHostHeader: Bool? = nil) {
        self.backends = backends
        self.id = id
        self.passHostHeader = passHostHeader
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case backends
        case id
        case passHostHeader
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(backends, forKey: .backends)
        try container.encodeIfPresent(id, forKey: .id)
        try container.encodeIfPresent(passHostHeader, forKey: .passHostHeader)
    }
}


@available(iOS 13, tvOS 13, watchOS 6, macOS 10.15, *)
extension Upstream: Identifiable {}
