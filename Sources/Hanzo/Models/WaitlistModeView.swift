//
// WaitlistModeView.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct WaitlistModeView: Sendable, Codable, ParameterConvertible, Hashable {

    /** Host is the queried host, normalized (lowercased, port stripped). */
    public var host: String?
    /** Known is false when no registered service claims this host, or when the registry is unavailable — the guard then lets the request through, which is why the two cases answer alike. */
    public var known: Bool?
    /** Service is the registered service that governs this host, empty when none does. */
    public var service: String?
    /** WaitlistMode is true when the service is GATED to approved users, false when it is open. Always false for an ungoverned host. */
    public var waitlistMode: Bool?

    public init(host: String? = nil, known: Bool? = nil, service: String? = nil, waitlistMode: Bool? = nil) {
        self.host = host
        self.known = known
        self.service = service
        self.waitlistMode = waitlistMode
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case host
        case known
        case service
        case waitlistMode
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(host, forKey: .host)
        try container.encodeIfPresent(known, forKey: .known)
        try container.encodeIfPresent(service, forKey: .service)
        try container.encodeIfPresent(waitlistMode, forKey: .waitlistMode)
    }
}

