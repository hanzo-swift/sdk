//
// RunView.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct RunView: Sendable, Codable, ParameterConvertible, Hashable {

    /** ID is the application id the run created or converged. */
    public var id: String?
    /** Name is the run's name, as stored. */
    public var name: String?
    /** Shape is the compute size label the request asked for, or \"auto\". */
    public var shape: String?
    /** Status is the application's state — `deploying` on a fresh accept. */
    public var status: String?
    /** URL is the run's live HTTPS address. */
    public var url: String?

    public init(id: String? = nil, name: String? = nil, shape: String? = nil, status: String? = nil, url: String? = nil) {
        self.id = id
        self.name = name
        self.shape = shape
        self.status = status
        self.url = url
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case id
        case name
        case shape
        case status
        case url
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(id, forKey: .id)
        try container.encodeIfPresent(name, forKey: .name)
        try container.encodeIfPresent(shape, forKey: .shape)
        try container.encodeIfPresent(status, forKey: .status)
        try container.encodeIfPresent(url, forKey: .url)
    }
}


@available(iOS 13, tvOS 13, watchOS 6, macOS 10.15, *)
extension RunView: Identifiable {}
