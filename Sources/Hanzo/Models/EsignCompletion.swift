//
// EsignCompletion.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct EsignCompletion: Sendable, Codable, ParameterConvertible, Hashable {

    /** DocumentStatus is COMPLETED when this was the last signature and the document sealed here, PENDING while others have still to sign. */
    public var documentStatus: String?
    /** RecipientID is the recipient who finished. */
    public var recipientId: String?
    /** Sealed is whether the document sealed on this call — the field values rendered onto the PDF and a real x509 PKCS#7 signature applied. */
    public var sealed: Bool?

    public init(documentStatus: String? = nil, recipientId: String? = nil, sealed: Bool? = nil) {
        self.documentStatus = documentStatus
        self.recipientId = recipientId
        self.sealed = sealed
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case documentStatus
        case recipientId
        case sealed
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(documentStatus, forKey: .documentStatus)
        try container.encodeIfPresent(recipientId, forKey: .recipientId)
        try container.encodeIfPresent(sealed, forKey: .sealed)
    }
}

