//
// O11yO11yDashboardPostable.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct O11yO11yDashboardPostable: Sendable, Codable, ParameterConvertible, Hashable {

    /** GenerateName derives a fresh unique name from spec.display.name instead of taking Name. */
    public var generateName: Bool?
    /** Image is an optional cover image reference. */
    public var image: String?
    /** Name is the dashboard's unique internal name (a DNS-1123 label). Omit it with generateName to derive one from the display name. */
    public var name: String?
    /** SchemaVersion is the dashboard schema version; must be the current v6. */
    public var schemaVersion: String?
    public var spec: JSONValue?
    /** Tags are the dashboard's tags; at most ten, and none may use a reserved DSL key. */
    public var tags: [O11yO11yDashboardPostableTag]?

    public init(generateName: Bool? = nil, image: String? = nil, name: String? = nil, schemaVersion: String? = nil, spec: JSONValue? = nil, tags: [O11yO11yDashboardPostableTag]? = nil) {
        self.generateName = generateName
        self.image = image
        self.name = name
        self.schemaVersion = schemaVersion
        self.spec = spec
        self.tags = tags
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case generateName
        case image
        case name
        case schemaVersion
        case spec
        case tags
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(generateName, forKey: .generateName)
        try container.encodeIfPresent(image, forKey: .image)
        try container.encodeIfPresent(name, forKey: .name)
        try container.encodeIfPresent(schemaVersion, forKey: .schemaVersion)
        try container.encodeIfPresent(spec, forKey: .spec)
        try container.encodeIfPresent(tags, forKey: .tags)
    }
}

