//
// EndpointList.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct EndpointList: Sendable, Codable, ParameterConvertible, Hashable {

    /** Data is the org's endpoints, newest first, each with its signing secret REDACTED — the secret leaves the server only on create and on rotate. */
    public var data: [Endpoint]?

    public init(data: [Endpoint]? = nil) {
        self.data = data
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case data
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(data, forKey: .data)
    }
}

