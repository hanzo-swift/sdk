//
// AccountsUsage.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct AccountsUsage: Sendable, Codable, ParameterConvertible, Hashable {

    /** Accounts is one row per linked account the gateway actually routed through. */
    public var accounts: [RoutedUsage]?
    /** Scope is always \"user\": the caller's own linked accounts. */
    public var scope: String?
    /** Source is always \"routed\": the gateway's own routed ledger. */
    public var source: String?
    /** Total is the honest sum across the rows. */
    public var total: AccountsTotal?

    public init(accounts: [RoutedUsage]? = nil, scope: String? = nil, source: String? = nil, total: AccountsTotal? = nil) {
        self.accounts = accounts
        self.scope = scope
        self.source = source
        self.total = total
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case accounts
        case scope
        case source
        case total
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(accounts, forKey: .accounts)
        try container.encodeIfPresent(scope, forKey: .scope)
        try container.encodeIfPresent(source, forKey: .source)
        try container.encodeIfPresent(total, forKey: .total)
    }
}

