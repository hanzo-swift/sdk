//
// RunnerBuildResp.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct RunnerBuildResp: Sendable, Codable, ParameterConvertible, Hashable {

    /** BuildJobID is the queued build's id, and what its progress is read by. */
    public var buildJobId: String?
    /** Image is the ref the image lane will push. */
    public var image: String?
    /** Index is the binaries.json URL the artifact lane will publish. */
    public var index: String?
    /** RunnerPool is the runner class the build was placed on. */
    public var runnerPool: String?
    /** Status is `queued` — the build was accepted and has not finished. */
    public var status: String?
    /** Target is the multi-stage build target, echoed back. */
    public var target: String?

    public init(buildJobId: String? = nil, image: String? = nil, index: String? = nil, runnerPool: String? = nil, status: String? = nil, target: String? = nil) {
        self.buildJobId = buildJobId
        self.image = image
        self.index = index
        self.runnerPool = runnerPool
        self.status = status
        self.target = target
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case buildJobId
        case image
        case index
        case runnerPool
        case status
        case target
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(buildJobId, forKey: .buildJobId)
        try container.encodeIfPresent(image, forKey: .image)
        try container.encodeIfPresent(index, forKey: .index)
        try container.encodeIfPresent(runnerPool, forKey: .runnerPool)
        try container.encodeIfPresent(status, forKey: .status)
        try container.encodeIfPresent(target, forKey: .target)
    }
}

