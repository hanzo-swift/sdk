//
// EsignLinks.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct EsignLinks: Sendable, Codable, ParameterConvertible, Hashable {

    /** ID is the document that went out. */
    public var id: String?
    /** Recipients is one link per signing recipient. Nothing is emailed by this call; delivering the links is the caller's. */
    public var recipients: [EsignLink]?
    /** Status is PENDING — the state a sent document is in until every signer has finished. */
    public var status: String?

    public init(id: String? = nil, recipients: [EsignLink]? = nil, status: String? = nil) {
        self.id = id
        self.recipients = recipients
        self.status = status
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case id
        case recipients
        case status
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(id, forKey: .id)
        try container.encodeIfPresent(recipients, forKey: .recipients)
        try container.encodeIfPresent(status, forKey: .status)
    }
}


@available(iOS 13, tvOS 13, watchOS 6, macOS 10.15, *)
extension EsignLinks: Identifiable {}
