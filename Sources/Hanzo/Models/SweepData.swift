//
// SweepData.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct SweepData: Sendable, Codable, ParameterConvertible, Hashable {

    /** AccruedCents is the amount moved into the reserve fund. */
    public var accruedCents: Int?
    /** Created is false when this period had already been swept — the accrual is idempotent. */
    public var created: Bool?
    /** Period is the period actually accrued. */
    public var period: String?
    /** ReserveCents is the fund balance after the accrual. */
    public var reserveCents: Int?
    /** RevenueCents is the revenue the share was computed from. */
    public var revenueCents: Int?

    public init(accruedCents: Int? = nil, created: Bool? = nil, period: String? = nil, reserveCents: Int? = nil, revenueCents: Int? = nil) {
        self.accruedCents = accruedCents
        self.created = created
        self.period = period
        self.reserveCents = reserveCents
        self.revenueCents = revenueCents
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case accruedCents
        case created
        case period
        case reserveCents
        case revenueCents
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(accruedCents, forKey: .accruedCents)
        try container.encodeIfPresent(created, forKey: .created)
        try container.encodeIfPresent(period, forKey: .period)
        try container.encodeIfPresent(reserveCents, forKey: .reserveCents)
        try container.encodeIfPresent(revenueCents, forKey: .revenueCents)
    }
}

