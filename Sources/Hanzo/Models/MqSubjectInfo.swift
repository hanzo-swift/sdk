//
// MqSubjectInfo.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct MqSubjectInfo: Codable, JSONEncodable, Hashable {

    /** Subject name. */
    public var subject: String?
    /** Number of active subscribers. */
    public var subscribers: Int?
    /** Current message rate (messages per second). */
    public var messagesPerSec: Double?
    /** Current data rate (bytes per second). */
    public var bytesPerSec: Int64?

    public init(subject: String? = nil, subscribers: Int? = nil, messagesPerSec: Double? = nil, bytesPerSec: Int64? = nil) {
        self.subject = subject
        self.subscribers = subscribers
        self.messagesPerSec = messagesPerSec
        self.bytesPerSec = bytesPerSec
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case subject
        case subscribers
        case messagesPerSec = "messages_per_sec"
        case bytesPerSec = "bytes_per_sec"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(subject, forKey: .subject)
        try container.encodeIfPresent(subscribers, forKey: .subscribers)
        try container.encodeIfPresent(messagesPerSec, forKey: .messagesPerSec)
        try container.encodeIfPresent(bytesPerSec, forKey: .bytesPerSec)
    }
}

