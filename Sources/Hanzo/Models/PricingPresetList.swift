//
// PricingPresetList.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct PricingPresetList: Sendable, Codable, ParameterConvertible, Hashable {

    /** Presets are the named compute sizes, each an opaque object exactly as the pricing source emits it — typically id, name, provider slug, vCPU, memory, disk and price. */
    public var presets: [[String: JSONValue]]?

    public init(presets: [[String: JSONValue]]? = nil) {
        self.presets = presets
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case presets
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(presets, forKey: .presets)
    }
}

