//
// ProviderBreakdown.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct ProviderBreakdown: Sendable, Codable, ParameterConvertible, Hashable {

    /** Available is false when the warehouse could not be read, which means \"no answer\" and NOT \"no usage\" — Items is then empty for a reason. */
    public var available: Bool?
    /** Items is one row per provider, most tokens first. */
    public var items: [ProviderRow]?
    /** Source names the warehouse table the rows came from. */
    public var source: String?

    public init(available: Bool? = nil, items: [ProviderRow]? = nil, source: String? = nil) {
        self.available = available
        self.items = items
        self.source = source
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case available
        case items
        case source
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(available, forKey: .available)
        try container.encodeIfPresent(items, forKey: .items)
        try container.encodeIfPresent(source, forKey: .source)
    }
}

