//
// SignReply.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct SignReply: Sendable, Codable, ParameterConvertible, Hashable {

    /** Document is the document, now out for signature. Its rendered body is not repeated here. */
    public var document: DocumentSummary?
    /** EsignRef is the provider's own reference for the request — what a webhook or a status poll quotes. */
    public var esignRef: String?
    /** Provider names the e-signature provider that took the request. \"manual\" means no provider is wired on this deployment and the org fulfils it out of band. */
    public var provider: String?

    public init(document: DocumentSummary? = nil, esignRef: String? = nil, provider: String? = nil) {
        self.document = document
        self.esignRef = esignRef
        self.provider = provider
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case document
        case esignRef
        case provider
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(document, forKey: .document)
        try container.encodeIfPresent(esignRef, forKey: .esignRef)
        try container.encodeIfPresent(provider, forKey: .provider)
    }
}

