//
// Totals.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct Totals: Sendable, Codable, ParameterConvertible, Hashable {

    /** AccruedCents is lifetime commission accrued summed over those rows, in cents. */
    public var accruedCents: Int?
    /** Applied is how many of those rows are still awaiting approval — no code, no accrual yet. */
    public var applied: Int?
    /** Approved is how many are approved: the only rows whose code resolves for attribution and whose balance can still grow. */
    public var approved: Int?
    /** PaidCents is lifetime commission already paid out summed over those rows, in cents. */
    public var paidCents: Int?
    /** PendingCents is accrued minus paid summed over those rows, in cents — the outstanding liability across the page. */
    public var pendingCents: Int?
    /** Suspended is how many were suspended. What they already accrued stays accrued and stays payable. */
    public var suspended: Int?
    /** Total is how many affiliate rows this page covered, at every status. It is the page, not the table: a limit that truncates truncates this too. */
    public var total: Int?

    public init(accruedCents: Int? = nil, applied: Int? = nil, approved: Int? = nil, paidCents: Int? = nil, pendingCents: Int? = nil, suspended: Int? = nil, total: Int? = nil) {
        self.accruedCents = accruedCents
        self.applied = applied
        self.approved = approved
        self.paidCents = paidCents
        self.pendingCents = pendingCents
        self.suspended = suspended
        self.total = total
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case accruedCents
        case applied
        case approved
        case paidCents
        case pendingCents
        case suspended
        case total
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(accruedCents, forKey: .accruedCents)
        try container.encodeIfPresent(applied, forKey: .applied)
        try container.encodeIfPresent(approved, forKey: .approved)
        try container.encodeIfPresent(paidCents, forKey: .paidCents)
        try container.encodeIfPresent(pendingCents, forKey: .pendingCents)
        try container.encodeIfPresent(suspended, forKey: .suspended)
        try container.encodeIfPresent(total, forKey: .total)
    }
}

