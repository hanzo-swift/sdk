//
// O11yO11yReductionRulePreviewIn.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct O11yO11yReductionRulePreviewIn: Sendable, Codable, ParameterConvertible, Hashable {

    /** Labels are the label names the rule would match. Required, at least one. */
    public var labels: [String]
    /** LookbackMs is how far back to sample when estimating. */
    public var lookbackMs: Int?
    /** MatchType is drop or keep. Required. */
    public var matchType: String
    /** MetricName is the metric the rule would govern. Required. */
    public var metricName: String

    public init(labels: [String], lookbackMs: Int? = nil, matchType: String, metricName: String) {
        self.labels = labels
        self.lookbackMs = lookbackMs
        self.matchType = matchType
        self.metricName = metricName
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case labels
        case lookbackMs
        case matchType
        case metricName
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(labels, forKey: .labels)
        try container.encodeIfPresent(lookbackMs, forKey: .lookbackMs)
        try container.encode(matchType, forKey: .matchType)
        try container.encode(metricName, forKey: .metricName)
    }
}

