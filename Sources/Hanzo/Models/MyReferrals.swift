//
// MyReferrals.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct MyReferrals: Sendable, Codable, ParameterConvertible, Hashable {

    /** Code is the org's STABLE referral code — a deterministic function of the org id, so it never changes and never has to be stored to be reproduced. */
    public var code: String?
    /** Counts tallies this org's referrals by status. */
    public var counts: StatusCounts?
    /** Link is the shareable signup link carrying the code, on the brand's own host. */
    public var link: String?
    /** Referrals is one row per org that signed up with this code. */
    public var referrals: [MyReferralView]?

    public init(code: String? = nil, counts: StatusCounts? = nil, link: String? = nil, referrals: [MyReferralView]? = nil) {
        self.code = code
        self.counts = counts
        self.link = link
        self.referrals = referrals
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case code
        case counts
        case link
        case referrals
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(code, forKey: .code)
        try container.encodeIfPresent(counts, forKey: .counts)
        try container.encodeIfPresent(link, forKey: .link)
        try container.encodeIfPresent(referrals, forKey: .referrals)
    }
}

