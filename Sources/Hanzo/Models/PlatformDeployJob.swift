//
// PlatformDeployJob.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public enum PlatformDeployJob: Codable, JSONEncodable, Hashable {
    case typePlatformDeployJobApplication(PlatformDeployJobApplication)
    case typePlatformDeployJobApplicationPreview(PlatformDeployJobApplicationPreview)
    case typePlatformDeployJobCompose(PlatformDeployJobCompose)

    public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .typePlatformDeployJobApplication(let value):
            try container.encode(value)
        case .typePlatformDeployJobApplicationPreview(let value):
            try container.encode(value)
        case .typePlatformDeployJobCompose(let value):
            try container.encode(value)
        }
    }

    private enum DiscriminatorCodingKey: String, CodingKey {
        case applicationType = "applicationType"
    }

    public init(from decoder: Decoder) throws {
        let keyedContainer = try decoder.container(keyedBy: DiscriminatorCodingKey.self)
        let discriminatorValue = try keyedContainer.decode(String.self, forKey: .applicationType)

        switch discriminatorValue {
        case "application":
            self = .typePlatformDeployJobApplication(try PlatformDeployJobApplication(from: decoder))
        case "application-preview":
            self = .typePlatformDeployJobApplicationPreview(try PlatformDeployJobApplicationPreview(from: decoder))
        case "compose":
            self = .typePlatformDeployJobCompose(try PlatformDeployJobCompose(from: decoder))
        default:
            throw DecodingError.dataCorrupted(
                DecodingError.Context(
                    codingPath: decoder.codingPath,
                    debugDescription: "Unknown discriminator value '\(discriminatorValue)' for PlatformDeployJob"
                )
            )
        }
    }
}

