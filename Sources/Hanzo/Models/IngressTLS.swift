//
// IngressTLS.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct IngressTLS: Sendable, Codable, ParameterConvertible, Hashable {

    /** ACMEDirectory is the ACME endpoint in use: the staging URL, or \"letsencrypt-production\". */
    public var acmeDirectory: String?
    /** ACMEEmail is the account email the PROCESS was started with (CLOUD_INGRESS_ACME_EMAIL), not the stored config's. */
    public var acmeEmail: String?
    /** Config is the caller org's stored ACME intent. */
    public var config: TLSConfig?
    /** EdgeEnabled is true when the edge listeners are actually bound. */
    public var edgeEnabled: Bool?
    /** ManagedHosts is every host the ACME HostPolicy will issue a certificate for — the union across ALL orgs of TLS-marked routes and configured extraHosts, because one process holds one certificate cache. */
    public var managedHosts: [String]?
    /** Note states which fields hot-apply and which need an edge restart. */
    public var note: String?
    /** Role is \"edge\" when this instance binds the listeners, else \"app\". */
    public var role: String?

    public init(acmeDirectory: String? = nil, acmeEmail: String? = nil, config: TLSConfig? = nil, edgeEnabled: Bool? = nil, managedHosts: [String]? = nil, note: String? = nil, role: String? = nil) {
        self.acmeDirectory = acmeDirectory
        self.acmeEmail = acmeEmail
        self.config = config
        self.edgeEnabled = edgeEnabled
        self.managedHosts = managedHosts
        self.note = note
        self.role = role
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case acmeDirectory
        case acmeEmail
        case config
        case edgeEnabled
        case managedHosts
        case note
        case role
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(acmeDirectory, forKey: .acmeDirectory)
        try container.encodeIfPresent(acmeEmail, forKey: .acmeEmail)
        try container.encodeIfPresent(config, forKey: .config)
        try container.encodeIfPresent(edgeEnabled, forKey: .edgeEnabled)
        try container.encodeIfPresent(managedHosts, forKey: .managedHosts)
        try container.encodeIfPresent(note, forKey: .note)
        try container.encodeIfPresent(role, forKey: .role)
    }
}

