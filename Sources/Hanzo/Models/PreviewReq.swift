//
// PreviewReq.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct PreviewReq: Sendable, Codable, ParameterConvertible, Hashable {

    /** App is the parent application's slug, from the path. */
    public var app: String?
    /** Branch is the branch to preview; defaults to the parent app's branch. */
    public var branch: String?
    /** Image is the already-built image ref to deploy. Required — a preview never builds. */
    public var image: String?
    /** Project is the project the parent application lives under, from the path. */
    public var project: String?

    public init(app: String? = nil, branch: String? = nil, image: String? = nil, project: String? = nil) {
        self.app = app
        self.branch = branch
        self.image = image
        self.project = project
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case app
        case branch
        case image
        case project
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(app, forKey: .app)
        try container.encodeIfPresent(branch, forKey: .branch)
        try container.encodeIfPresent(image, forKey: .image)
        try container.encodeIfPresent(project, forKey: .project)
    }
}

