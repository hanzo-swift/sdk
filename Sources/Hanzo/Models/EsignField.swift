//
// EsignField.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct EsignField: Sendable, Codable, ParameterConvertible, Hashable {

    /** CustomText is the value a non-signature field was filled with, empty until it is. A signature's value is not here: it is stored separately and rendered onto the page at sealing. */
    public var customText: String?
    public var fieldMeta: JSONValue?
    /** Height is the field's height, -1 when the renderer is to choose one. */
    public var height: Double?
    /** ID is the field id. */
    public var id: String?
    /** Inserted is whether this field has been filled in. */
    public var inserted: Bool?
    /** Page is the 1-based page the field sits on. */
    public var page: Double?
    /** PositionX is the field's horizontal position on that page. */
    public var positionX: Double?
    /** PositionY is the field's vertical position on that page. */
    public var positionY: Double?
    /** RecipientID is who must fill this field. It is absent on a signer's own view of a document, where every field returned is already theirs. */
    public var recipientId: String?
    /** Type is what the field collects — SIGNATURE, DATE, NAME, EMAIL, TEXT and the rest. */
    public var type: String?
    /** Width is the field's width, -1 when the renderer is to choose one. */
    public var width: Double?

    public init(customText: String? = nil, fieldMeta: JSONValue? = nil, height: Double? = nil, id: String? = nil, inserted: Bool? = nil, page: Double? = nil, positionX: Double? = nil, positionY: Double? = nil, recipientId: String? = nil, type: String? = nil, width: Double? = nil) {
        self.customText = customText
        self.fieldMeta = fieldMeta
        self.height = height
        self.id = id
        self.inserted = inserted
        self.page = page
        self.positionX = positionX
        self.positionY = positionY
        self.recipientId = recipientId
        self.type = type
        self.width = width
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case customText
        case fieldMeta
        case height
        case id
        case inserted
        case page
        case positionX
        case positionY
        case recipientId
        case type
        case width
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(customText, forKey: .customText)
        try container.encodeIfPresent(fieldMeta, forKey: .fieldMeta)
        try container.encodeIfPresent(height, forKey: .height)
        try container.encodeIfPresent(id, forKey: .id)
        try container.encodeIfPresent(inserted, forKey: .inserted)
        try container.encodeIfPresent(page, forKey: .page)
        try container.encodeIfPresent(positionX, forKey: .positionX)
        try container.encodeIfPresent(positionY, forKey: .positionY)
        try container.encodeIfPresent(recipientId, forKey: .recipientId)
        try container.encodeIfPresent(type, forKey: .type)
        try container.encodeIfPresent(width, forKey: .width)
    }
}


@available(iOS 13, tvOS 13, watchOS 6, macOS 10.15, *)
extension EsignField: Identifiable {}
