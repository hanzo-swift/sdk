//
// ProjectsForkProjectRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct ProjectsForkProjectRequest: Codable, JSONEncodable, Hashable {

    /** Template slug to fork (required). */
    public var slug: String
    /** Target project name; defaults to the template title. */
    public var name: String?
    /** Overrides the derived project slug; defaults to the template slug. */
    public var target: String?

    public init(slug: String, name: String? = nil, target: String? = nil) {
        self.slug = slug
        self.name = name
        self.target = target
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case slug
        case name
        case target
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(slug, forKey: .slug)
        try container.encodeIfPresent(name, forKey: .name)
        try container.encodeIfPresent(target, forKey: .target)
    }
}

