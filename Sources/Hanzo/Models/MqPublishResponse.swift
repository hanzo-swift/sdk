//
// MqPublishResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Response after publishing a message. */
public struct MqPublishResponse: Codable, JSONEncodable, Hashable {

    /** Stream name if the subject is bound to a JetStream stream. Absent for core NATS subjects.  */
    public var stream: String?
    /** Stream sequence number. Present only for JetStream-bound subjects.  */
    public var sequence: Int?
    /** True if the message was a duplicate (dedup window). */
    public var duplicate: Bool?

    public init(stream: String? = nil, sequence: Int? = nil, duplicate: Bool? = nil) {
        self.stream = stream
        self.sequence = sequence
        self.duplicate = duplicate
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case stream
        case sequence
        case duplicate
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(stream, forKey: .stream)
        try container.encodeIfPresent(sequence, forKey: .sequence)
        try container.encodeIfPresent(duplicate, forKey: .duplicate)
    }
}

