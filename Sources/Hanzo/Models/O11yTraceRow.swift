//
// O11yTraceRow.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct O11yTraceRow: Sendable, Codable, ParameterConvertible, Hashable {

    /** DurationMs is End minus Start in milliseconds: the trace's wall clock, not the sum of its spans, which double-counts everything concurrent. */
    public var durationMs: Double?
    /** End is the latest span end, RFC3339 with nanoseconds, in UTC. */
    public var end: String?
    /** NumSpans is how many spans the trace carries. */
    public var numSpans: Int?
    /** Start is the earliest span start, RFC3339 with nanoseconds, in UTC. */
    public var start: String?
    /** TraceID is the trace's id — the {traceId} of the detail read. */
    public var traceId: String?

    public init(durationMs: Double? = nil, end: String? = nil, numSpans: Int? = nil, start: String? = nil, traceId: String? = nil) {
        self.durationMs = durationMs
        self.end = end
        self.numSpans = numSpans
        self.start = start
        self.traceId = traceId
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case durationMs
        case end
        case numSpans
        case start
        case traceId
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(durationMs, forKey: .durationMs)
        try container.encodeIfPresent(end, forKey: .end)
        try container.encodeIfPresent(numSpans, forKey: .numSpans)
        try container.encodeIfPresent(start, forKey: .start)
        try container.encodeIfPresent(traceId, forKey: .traceId)
    }
}

