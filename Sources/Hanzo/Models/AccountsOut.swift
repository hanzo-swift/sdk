//
// AccountsOut.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct AccountsOut: Sendable, Codable, ParameterConvertible, Hashable {

    /** Accounts are the ledger accounts in scope with their balances. */
    public var accounts: [AccountView]?
    /** Scope is the scope actually served: \"org\" or \"house\". */
    public var scope: String?
    /** Tenant is the org whose accounts these are (empty for the house scope's own rows). */
    public var tenant: String?

    public init(accounts: [AccountView]? = nil, scope: String? = nil, tenant: String? = nil) {
        self.accounts = accounts
        self.scope = scope
        self.tenant = tenant
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case accounts
        case scope
        case tenant
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(accounts, forKey: .accounts)
        try container.encodeIfPresent(scope, forKey: .scope)
        try container.encodeIfPresent(tenant, forKey: .tenant)
    }
}

