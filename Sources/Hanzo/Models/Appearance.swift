//
// Appearance.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct Appearance: Sendable, Codable, ParameterConvertible, Hashable {

    /** Accent is the one hue — a CSS colour token (a hex, or a bounded functional colour like rgb()/oklch()). Anything else is dropped rather than stored. */
    public var accent: String?
    /** Density is the spacing step: \"compact\", \"default\" or \"comfortable\". */
    public var density: String?
    /** Type is the text-size multiplier, clamped to the ramp window [0.85, 1.4]. Absent (0) leaves the published default. */
    public var type: Double?

    public init(accent: String? = nil, density: String? = nil, type: Double? = nil) {
        self.accent = accent
        self.density = density
        self.type = type
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case accent
        case density
        case type
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(accent, forKey: .accent)
        try container.encodeIfPresent(density, forKey: .density)
        try container.encodeIfPresent(type, forKey: .type)
    }
}

