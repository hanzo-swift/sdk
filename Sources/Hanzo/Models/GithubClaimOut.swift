//
// GithubClaimOut.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct GithubClaimOut: Sendable, Codable, ParameterConvertible, Hashable {

    /** Already were bound before the call and are unchanged by it. */
    public var already: [String]?
    /** Claimed are the accounts this call bound. Never null; [] when none. */
    public var claimed: [String]?

    public init(already: [String]? = nil, claimed: [String]? = nil) {
        self.already = already
        self.claimed = claimed
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case already
        case claimed
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(already, forKey: .already)
        try container.encodeIfPresent(claimed, forKey: .claimed)
    }
}

