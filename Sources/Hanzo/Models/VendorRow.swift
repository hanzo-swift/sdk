//
// VendorRow.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct VendorRow: Sendable, Codable, ParameterConvertible, Hashable {

    /** Aliases are the other spellings a receipt may print the vendor under; a scan matching any of them resolves to this vendor. */
    public var aliases: [String]?
    /** Canonical is the vendor's one true name, and the key an upsert writes by. */
    public var canonical: String?
    /** DefaultCategory is the COA expense account new bills from this vendor book to. An upsert normalizes a slug (\"software\") to its account number. */
    public var defaultCategory: String?

    public init(aliases: [String]? = nil, canonical: String? = nil, defaultCategory: String? = nil) {
        self.aliases = aliases
        self.canonical = canonical
        self.defaultCategory = defaultCategory
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case aliases
        case canonical
        case defaultCategory
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(aliases, forKey: .aliases)
        try container.encodeIfPresent(canonical, forKey: .canonical)
        try container.encodeIfPresent(defaultCategory, forKey: .defaultCategory)
    }
}

