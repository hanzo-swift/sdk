//
// ProvisioningCreateResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Returned on 201. &#x60;password&#x60; and &#x60;username&#x60; are present only for secretful kinds (sql, kv, and the dedicated datastore/docdb admin credential). The password is returned ONCE and is never retrievable again.  */
public struct ProvisioningCreateResponse: Codable, JSONEncodable, Hashable {

    public enum Kind: String, Codable, CaseIterable {
        case sql = "sql"
        case vector = "vector"
        case datastore = "datastore"
        case kv = "kv"
        case search = "search"
        case s3 = "s3"
        case docdb = "docdb"
    }
    /** Server-generated resource id. */
    public var id: String
    public var kind: Kind
    public var name: String
    /** Resource status (e.g. \"ready\", or \"provisioning\" for a dedicated instance still coming up). */
    public var status: String
    /** Customer-facing host (public gateway host, or dedicated instance service). */
    public var host: String
    public var port: Int
    /** Present only for secretful kinds. */
    public var username: String?
    public var database: String
    /** Public, routable DSN (internal admin host is remapped out). */
    public var connectionString: String
    /** Present only for secretful kinds; returned ONCE. */
    public var password: String?

    public init(id: String, kind: Kind, name: String, status: String, host: String, port: Int, username: String? = nil, database: String, connectionString: String, password: String? = nil) {
        self.id = id
        self.kind = kind
        self.name = name
        self.status = status
        self.host = host
        self.port = port
        self.username = username
        self.database = database
        self.connectionString = connectionString
        self.password = password
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case id
        case kind
        case name
        case status
        case host
        case port
        case username
        case database
        case connectionString
        case password
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(id, forKey: .id)
        try container.encode(kind, forKey: .kind)
        try container.encode(name, forKey: .name)
        try container.encode(status, forKey: .status)
        try container.encode(host, forKey: .host)
        try container.encode(port, forKey: .port)
        try container.encodeIfPresent(username, forKey: .username)
        try container.encode(database, forKey: .database)
        try container.encode(connectionString, forKey: .connectionString)
        try container.encodeIfPresent(password, forKey: .password)
    }
}


@available(iOS 13, tvOS 13, watchOS 6, macOS 10.15, *)
extension ProvisioningCreateResponse: Identifiable {}
