//
// Overview.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct Overview: Sendable, Codable, ParameterConvertible, Hashable {

    /** Commerce is the orders/revenue lens over product events. */
    public var commerce: CommerceOverview?
    /** End is the window's exclusive upper bound, RFC3339 UTC. */
    public var end: String?
    /** Interval is the bucket width the window implies: hour or day. */
    public var interval: String?
    /** LLM is the LLM usage lens — real per-org data. */
    public var llm: LLMOverview?
    /** Range is the window that was actually applied: 24h, 7d, 30d or custom. */
    public var range: String?
    /** Scope names the tenant these numbers belong to. */
    public var scope: Scope?
    /** Start is the window's inclusive lower bound, RFC3339 UTC. */
    public var start: String?
    /** Web is the web-traffic lens over product events. */
    public var web: WebOverview?

    public init(commerce: CommerceOverview? = nil, end: String? = nil, interval: String? = nil, llm: LLMOverview? = nil, range: String? = nil, scope: Scope? = nil, start: String? = nil, web: WebOverview? = nil) {
        self.commerce = commerce
        self.end = end
        self.interval = interval
        self.llm = llm
        self.range = range
        self.scope = scope
        self.start = start
        self.web = web
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case commerce
        case end
        case interval
        case llm
        case range
        case scope
        case start
        case web
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(commerce, forKey: .commerce)
        try container.encodeIfPresent(end, forKey: .end)
        try container.encodeIfPresent(interval, forKey: .interval)
        try container.encodeIfPresent(llm, forKey: .llm)
        try container.encodeIfPresent(range, forKey: .range)
        try container.encodeIfPresent(scope, forKey: .scope)
        try container.encodeIfPresent(start, forKey: .start)
        try container.encodeIfPresent(web, forKey: .web)
    }
}

