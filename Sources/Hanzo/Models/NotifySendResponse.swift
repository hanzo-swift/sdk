//
// NotifySendResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Canonical send result (&#x60;notify/pkg/types.SendResponse&#x60;). In the sync fold &#x60;task_id&#x60; is always empty and &#x60;status&#x60; is the terminal result.  */
public struct NotifySendResponse: Codable, JSONEncodable, Hashable {

    public enum Status: String, Codable, CaseIterable {
        case queued = "queued"
        case sending = "sending"
        case sent = "sent"
        case delivered = "delivered"
        case failed = "failed"
    }
    /** Generated 16-byte hex message id (opaque handle). */
    public var messageId: String
    /** hanzoai/tasks workflow id in async mode; always empty in the sync fold.  */
    public var taskId: String?
    /** Terminal delivery status. In the sync fold this is `sent` on success or `failed` on terminal failure. (The type also defines `queued`, `sending`, `delivered` for the async plane, which is not folded.)  */
    public var status: Status
    /** Set on terminal failure (sync mode only). */
    public var error: String?

    public init(messageId: String, taskId: String? = nil, status: Status, error: String? = nil) {
        self.messageId = messageId
        self.taskId = taskId
        self.status = status
        self.error = error
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case messageId = "message_id"
        case taskId = "task_id"
        case status
        case error
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(messageId, forKey: .messageId)
        try container.encodeIfPresent(taskId, forKey: .taskId)
        try container.encode(status, forKey: .status)
        try container.encodeIfPresent(error, forKey: .error)
    }
}

