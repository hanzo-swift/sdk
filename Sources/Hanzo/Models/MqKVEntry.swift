//
// MqKVEntry.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** A key-value entry with revision metadata. */
public struct MqKVEntry: Codable, JSONEncodable, Hashable {

    public enum Operation: String, Codable, CaseIterable {
        case put = "put"
        case delete = "delete"
        case purge = "purge"
    }
    /** Key name. */
    public var key: String?
    /** Value (base64-encoded for binary data). */
    public var value: String?
    /** Revision number. */
    public var revision: Int?
    /** Timestamp of this revision. */
    public var created: Date?
    /** Operation that produced this revision. `delete` is a tombstone. `purge` removes all prior revisions.  */
    public var operation: Operation?
    /** Bucket name. */
    public var bucket: String?
    /** Number of revisions since this entry (for watch operations).  */
    public var delta: Int?

    public init(key: String? = nil, value: String? = nil, revision: Int? = nil, created: Date? = nil, operation: Operation? = nil, bucket: String? = nil, delta: Int? = nil) {
        self.key = key
        self.value = value
        self.revision = revision
        self.created = created
        self.operation = operation
        self.bucket = bucket
        self.delta = delta
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case key
        case value
        case revision
        case created
        case operation
        case bucket
        case delta
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(key, forKey: .key)
        try container.encodeIfPresent(value, forKey: .value)
        try container.encodeIfPresent(revision, forKey: .revision)
        try container.encodeIfPresent(created, forKey: .created)
        try container.encodeIfPresent(operation, forKey: .operation)
        try container.encodeIfPresent(bucket, forKey: .bucket)
        try container.encodeIfPresent(delta, forKey: .delta)
    }
}

