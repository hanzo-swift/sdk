//
// WorkerList.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct WorkerList: Sendable, Codable, ParameterConvertible, Hashable {

    /** Workers is one row per connected BYO machine, each carrying the host's own report (GPUs, driver versions, capabilities) rather than a normalized view. */
    public var workers: [ByoWorker]?

    public init(workers: [ByoWorker]? = nil) {
        self.workers = workers
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case workers
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(workers, forKey: .workers)
    }
}

