//
// DataroomLinkCreate.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct DataroomLinkCreate: Sendable, Codable, ParameterConvertible, Hashable {

    public var allowDownload: JSONValue?
    /** AllowList narrows which addresses pass the email gate. Optional; an entry may be a full address (\"ada@example.com\"), an \"@domain.com\" suffix, or a bare \"domain.com\". An omitted or EMPTY list admits everyone, so a link with no list enforces the email gate alone. */
    public var allowList: [JSONValue]?
    public var dataroomId: JSONValue?
    /** DenyList rejects addresses, in the same three forms as the allow list. Optional. It is checked BEFORE the allow list, so deny always wins. */
    public var denyList: [JSONValue]?
    public var documentId: JSONValue?
    public var emailProtected: JSONValue?
    public var expiresAt: JSONValue?
    public var name: JSONValue?
    public var password: JSONValue?

    public init(allowDownload: JSONValue? = nil, allowList: [JSONValue]? = nil, dataroomId: JSONValue? = nil, denyList: [JSONValue]? = nil, documentId: JSONValue? = nil, emailProtected: JSONValue? = nil, expiresAt: JSONValue? = nil, name: JSONValue? = nil, password: JSONValue? = nil) {
        self.allowDownload = allowDownload
        self.allowList = allowList
        self.dataroomId = dataroomId
        self.denyList = denyList
        self.documentId = documentId
        self.emailProtected = emailProtected
        self.expiresAt = expiresAt
        self.name = name
        self.password = password
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case allowDownload
        case allowList
        case dataroomId
        case denyList
        case documentId
        case emailProtected
        case expiresAt
        case name
        case password
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(allowDownload, forKey: .allowDownload)
        try container.encodeIfPresent(allowList, forKey: .allowList)
        try container.encodeIfPresent(dataroomId, forKey: .dataroomId)
        try container.encodeIfPresent(denyList, forKey: .denyList)
        try container.encodeIfPresent(documentId, forKey: .documentId)
        try container.encodeIfPresent(emailProtected, forKey: .emailProtected)
        try container.encodeIfPresent(expiresAt, forKey: .expiresAt)
        try container.encodeIfPresent(name, forKey: .name)
        try container.encodeIfPresent(password, forKey: .password)
    }
}

