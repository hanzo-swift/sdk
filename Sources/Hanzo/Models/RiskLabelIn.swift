//
// RiskLabelIn.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct RiskLabelIn: Sendable, Codable, ParameterConvertible, Hashable {

    /** Labels is the batch. Each member is judged on its own: one refusal does not discard the rest, because a webhook redelivering five disputes must not lose four of them to one malformed fifth. */
    public var labels: [RiskLabelFact]?

    public init(labels: [RiskLabelFact]? = nil) {
        self.labels = labels
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case labels
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(labels, forKey: .labels)
    }
}

