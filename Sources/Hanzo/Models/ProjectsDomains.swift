//
// ProjectsDomains.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct ProjectsDomains: Sendable, Codable, ParameterConvertible, Hashable {

    /** Claims is one row per host — live, or pending with the DNS records it still owes. */
    public var claims: [ProjectsDomain]?
    /** Domains are the hostnames that are VERIFIED and routing right now. */
    public var domains: [String]?
    /** Org and Slug identify the site the panel belongs to. */
    public var org: String?
    public var slug: String?

    public init(claims: [ProjectsDomain]? = nil, domains: [String]? = nil, org: String? = nil, slug: String? = nil) {
        self.claims = claims
        self.domains = domains
        self.org = org
        self.slug = slug
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case claims
        case domains
        case org
        case slug
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(claims, forKey: .claims)
        try container.encodeIfPresent(domains, forKey: .domains)
        try container.encodeIfPresent(org, forKey: .org)
        try container.encodeIfPresent(slug, forKey: .slug)
    }
}

