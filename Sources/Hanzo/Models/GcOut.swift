//
// GcOut.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct GcOut: Sendable, Codable, ParameterConvertible, Hashable {

    /** Maintained is always true; the call fails rather than reporting false. */
    public var maintained: Bool?
    /** Repo is the repo that was repacked. */
    public var repo: String?
    /** SizeBytes is the size measured AFTER the repack — usually smaller, since repacking drops the packs it supersedes. */
    public var sizeBytes: Int?

    public init(maintained: Bool? = nil, repo: String? = nil, sizeBytes: Int? = nil) {
        self.maintained = maintained
        self.repo = repo
        self.sizeBytes = sizeBytes
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case maintained
        case repo
        case sizeBytes
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(maintained, forKey: .maintained)
        try container.encodeIfPresent(repo, forKey: .repo)
        try container.encodeIfPresent(sizeBytes, forKey: .sizeBytes)
    }
}

