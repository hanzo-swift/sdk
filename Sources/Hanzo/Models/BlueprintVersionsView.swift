//
// BlueprintVersionsView.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct BlueprintVersionsView: Sendable, Codable, ParameterConvertible, Hashable {

    /** Brand is the blueprint key the history belongs to — this deployment's brand, or \"\" (the base blueprint) when the brand has no row of its own. */
    public var brand: String?
    /** Versions are the stored versions, newest first: metadata only, never the documents. */
    public var versions: [VersionMeta]?

    public init(brand: String? = nil, versions: [VersionMeta]? = nil) {
        self.brand = brand
        self.versions = versions
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case brand
        case versions
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(brand, forKey: .brand)
        try container.encodeIfPresent(versions, forKey: .versions)
    }
}

