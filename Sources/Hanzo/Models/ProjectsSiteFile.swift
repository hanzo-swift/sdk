//
// ProjectsSiteFile.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** One file in a site manifest — a relative path and its full contents. */
public struct ProjectsSiteFile: Codable, JSONEncodable, Hashable {

    /** Relative path within the site (no leading \"/\", no \"..\"); index.html must be present at the root. */
    public var path: String
    /** Full file contents. HTML files get a mobile viewport tag injected when absent. */
    public var content: String

    public init(path: String, content: String) {
        self.path = path
        self.content = content
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case path
        case content
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(path, forKey: .path)
        try container.encode(content, forKey: .content)
    }
}

