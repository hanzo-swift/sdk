//
// PluginPluginInfo.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Introspection view of one mounted plugin. Derived from the manifest &#x60;Plugin&#x60; entry; only the three public identity fields are surfaced.  */
public struct PluginPluginInfo: Codable, JSONEncodable, Hashable {

    public enum Kind: String, Codable, CaseIterable {
        case wasm = "wasm"
        case proxy = "proxy"
    }
    /** Plugin name from the manifest entry. */
    public var name: String?
    /** Plugin kind selecting how it was built and mounted. */
    public var kind: Kind?
    /** Mount point at which the plugin's routes are served. */
    public var _prefix: String?

    public init(name: String? = nil, kind: Kind? = nil, _prefix: String? = nil) {
        self.name = name
        self.kind = kind
        self._prefix = _prefix
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case name
        case kind
        case _prefix = "prefix"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(name, forKey: .name)
        try container.encodeIfPresent(kind, forKey: .kind)
        try container.encodeIfPresent(_prefix, forKey: ._prefix)
    }
}

