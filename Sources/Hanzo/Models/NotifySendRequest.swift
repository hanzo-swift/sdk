//
// NotifySendRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Notification send request (&#x60;notify/pkg/types.SendRequest&#x60;). Delivery content is either a raw &#x60;subject&#x60;/&#x60;body&#x60;, or a template resolved from &#x60;template_id&#x60;/&#x60;event&#x60; rendered against &#x60;template_vars&#x60;.  */
public struct NotifySendRequest: Codable, JSONEncodable, Hashable {

    /** Destination address(es). Phone number for SMS, email for email. Multiple recipients fan out into one provider call each and produce one response entry per recipient.  */
    public var to: [String]
    public var channel: NotifyChannel?
    /** Pins a specific provider service name (e.g. `twilio`, `plivo`, `twilio_email`, `mail`). When omitted the fold picks the org's default for the channel from configured KMS credentials.  */
    public var provider: String?
    /** Raw subject; used verbatim when `body` is set (no-template path). */
    public var subject: String?
    /** Raw message body. When non-empty it wins verbatim and no template is rendered.  */
    public var body: String?
    /** Built-in template identifier. When `body` is empty, the template is resolved from `template_id` or, failing that, `event`.  */
    public var templateId: String?
    /** Free-form variables the template is rendered against (e.g. `otp`, `app`, `recipient`). A missing `app` defaults to `Hanzo`.  */
    public var templateVars: [String: AnyCodable]?
    /** Event-catalog identifier. Also used as a fallback template id when `template_id` is unset (the IAM OTP path sends `event: iam.otp_sent` with no `template_id`).  */
    public var event: String?
    /** Per-tenant deduplication key. Accepted on the wire; the sync fold does not persist a Message row.  */
    public var idempotencyKey: String?
    /** RFC3339 schedule time for async dispatch. Async is not available in the fold, so a set value has no effect on the sync path.  */
    public var sendAt: String?
    /** Free-form per-provider knob bag. */
    public var options: [String: AnyCodable]?

    public init(to: [String], channel: NotifyChannel? = nil, provider: String? = nil, subject: String? = nil, body: String? = nil, templateId: String? = nil, templateVars: [String: AnyCodable]? = nil, event: String? = nil, idempotencyKey: String? = nil, sendAt: String? = nil, options: [String: AnyCodable]? = nil) {
        self.to = to
        self.channel = channel
        self.provider = provider
        self.subject = subject
        self.body = body
        self.templateId = templateId
        self.templateVars = templateVars
        self.event = event
        self.idempotencyKey = idempotencyKey
        self.sendAt = sendAt
        self.options = options
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case to
        case channel
        case provider
        case subject
        case body
        case templateId = "template_id"
        case templateVars = "template_vars"
        case event
        case idempotencyKey = "idempotency_key"
        case sendAt = "send_at"
        case options
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(to, forKey: .to)
        try container.encodeIfPresent(channel, forKey: .channel)
        try container.encodeIfPresent(provider, forKey: .provider)
        try container.encodeIfPresent(subject, forKey: .subject)
        try container.encodeIfPresent(body, forKey: .body)
        try container.encodeIfPresent(templateId, forKey: .templateId)
        try container.encodeIfPresent(templateVars, forKey: .templateVars)
        try container.encodeIfPresent(event, forKey: .event)
        try container.encodeIfPresent(idempotencyKey, forKey: .idempotencyKey)
        try container.encodeIfPresent(sendAt, forKey: .sendAt)
        try container.encodeIfPresent(options, forKey: .options)
    }
}

