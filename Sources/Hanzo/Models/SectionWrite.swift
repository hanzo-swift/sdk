//
// SectionWrite.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct SectionWrite: Sendable, Codable, ParameterConvertible, Hashable {

    public var data: JSONValue?
    /** ID is the record's id. Omit it on a create and one is minted; the single-valued sections (profile, risk) hold one record whatever is named. */
    public var id: String?
    /** Kind is the section being written. The URL is the authority. */
    public var kind: String?
    /** Ord orders this record within its section, ascending, ties broken by id. It is the organization's own ordering — the page renders in it. */
    public var ord: Int?

    public init(data: JSONValue? = nil, id: String? = nil, kind: String? = nil, ord: Int? = nil) {
        self.data = data
        self.id = id
        self.kind = kind
        self.ord = ord
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case data
        case id
        case kind
        case ord
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(data, forKey: .data)
        try container.encodeIfPresent(id, forKey: .id)
        try container.encodeIfPresent(kind, forKey: .kind)
        try container.encodeIfPresent(ord, forKey: .ord)
    }
}


@available(iOS 13, tvOS 13, watchOS 6, macOS 10.15, *)
extension SectionWrite: Identifiable {}
