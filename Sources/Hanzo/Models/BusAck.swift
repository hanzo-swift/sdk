//
// BusAck.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct BusAck: Sendable, Codable, ParameterConvertible, Hashable {

    /** Duplicate is true when JetStream deduplicated the message by its Nats-Msg-Id instead of storing it again. */
    public var duplicate: Bool?
    /** OK is true when the bus accepted the message. */
    public var ok: Bool?
    /** Seq is the message's sequence in that stream. */
    public var seq: Int?
    /** Stream is the stream that stored the message — absent when no stream captures the subject and the message went out core (fire-and-forget). */
    public var stream: String?

    public init(duplicate: Bool? = nil, ok: Bool? = nil, seq: Int? = nil, stream: String? = nil) {
        self.duplicate = duplicate
        self.ok = ok
        self.seq = seq
        self.stream = stream
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case duplicate
        case ok
        case seq
        case stream
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(duplicate, forKey: .duplicate)
        try container.encodeIfPresent(ok, forKey: .ok)
        try container.encodeIfPresent(seq, forKey: .seq)
        try container.encodeIfPresent(stream, forKey: .stream)
    }
}

