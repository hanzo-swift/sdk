//
// PubsubStreamConfig.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct PubsubStreamConfig: Codable, JSONEncodable, Hashable {

    public enum Storage: String, Codable, CaseIterable {
        case file = "file"
        case memory = "memory"
    }
    public enum Replicas: Int, Codable, CaseIterable {
        case _1 = 1
        case _3 = 3
        case _5 = 5
    }
    public enum Retention: String, Codable, CaseIterable {
        case limits = "limits"
        case interest = "interest"
        case workqueue = "workqueue"
    }
    public enum Discard: String, Codable, CaseIterable {
        case old = "old"
        case new = "new"
    }
    /** Unique stream name */
    public var name: String
    /** Subjects captured by this stream */
    public var subjects: [String]
    /** Storage backend */
    public var storage: Storage? = .file
    /** Replication factor */
    public var replicas: Replicas? = ._1
    public var retention: Retention? = .limits
    /** Maximum messages to retain */
    public var maxMsgs: Int? = -1
    /** Maximum bytes to retain */
    public var maxBytes: Int? = -1
    /** Maximum age in nanoseconds (0 = unlimited) */
    public var maxAge: Int? = 0
    /** Discard policy when limits are reached */
    public var discard: Discard? = .old

    public init(name: String, subjects: [String], storage: Storage? = .file, replicas: Replicas? = ._1, retention: Retention? = .limits, maxMsgs: Int? = -1, maxBytes: Int? = -1, maxAge: Int? = 0, discard: Discard? = .old) {
        self.name = name
        self.subjects = subjects
        self.storage = storage
        self.replicas = replicas
        self.retention = retention
        self.maxMsgs = maxMsgs
        self.maxBytes = maxBytes
        self.maxAge = maxAge
        self.discard = discard
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case name
        case subjects
        case storage
        case replicas
        case retention
        case maxMsgs = "max_msgs"
        case maxBytes = "max_bytes"
        case maxAge = "max_age"
        case discard
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(name, forKey: .name)
        try container.encode(subjects, forKey: .subjects)
        try container.encodeIfPresent(storage, forKey: .storage)
        try container.encodeIfPresent(replicas, forKey: .replicas)
        try container.encodeIfPresent(retention, forKey: .retention)
        try container.encodeIfPresent(maxMsgs, forKey: .maxMsgs)
        try container.encodeIfPresent(maxBytes, forKey: .maxBytes)
        try container.encodeIfPresent(maxAge, forKey: .maxAge)
        try container.encodeIfPresent(discard, forKey: .discard)
    }
}

