//
// RiskPublishOut.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct RiskPublishOut: Sendable, Codable, ParameterConvertible, Hashable {

    /** Minted is false when your model was ALREADY published under this name and nothing was written. Publication is idempotent on the value itself, which is what a content address is for — publishing at every boundary costs nothing rather than being the cheapest way to fill a disk. */
    public var minted: Bool?
    /** Tenant is whose history it entered. */
    public var tenant: String?
    /** Value is the published value: its name and what it is, never its masses. */
    public var value: RiskModelValue?

    public init(minted: Bool? = nil, tenant: String? = nil, value: RiskModelValue? = nil) {
        self.minted = minted
        self.tenant = tenant
        self.value = value
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case minted
        case tenant
        case value
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(minted, forKey: .minted)
        try container.encodeIfPresent(tenant, forKey: .tenant)
        try container.encodeIfPresent(value, forKey: .value)
    }
}

