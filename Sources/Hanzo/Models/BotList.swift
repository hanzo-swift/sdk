//
// BotList.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct BotList: Sendable, Codable, ParameterConvertible, Hashable {

    /** Bots is one row per kind=bot machine, each joined with its agent binding when it has one. */
    public var bots: [BotView]?

    public init(bots: [BotView]? = nil) {
        self.bots = bots
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case bots
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(bots, forKey: .bots)
    }
}

