//
// MeetHealth.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct MeetHealth: Sendable, Codable, ParameterConvertible, Hashable {

    /** Ready reports whether this deployment can mint join tokens. False is the 503. */
    public var ready: Bool?
    /** Service names the subsystem answering — always \"meet\". */
    public var service: String?
    /** Status is \"ok\" when tokens can be minted and \"degraded\" when they cannot. */
    public var status: String?

    public init(ready: Bool? = nil, service: String? = nil, status: String? = nil) {
        self.ready = ready
        self.service = service
        self.status = status
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case ready
        case service
        case status
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(ready, forKey: .ready)
        try container.encodeIfPresent(service, forKey: .service)
        try container.encodeIfPresent(status, forKey: .status)
    }
}

