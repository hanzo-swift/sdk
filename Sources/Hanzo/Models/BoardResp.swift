//
// BoardResp.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct BoardResp: Sendable, Codable, ParameterConvertible, Hashable {

    /** Account is the account the series narrows to, when one was named. */
    public var account: String?
    /** Available reports whether the warehouse answered; false is an honest \"we have no data\", NOT zero usage. */
    public var available: Bool?
    /** Current is the live state of each lane — the dash headline. */
    public var current: [ReadingView]?
    /** From and To are the resolved [from, to) window, RFC 3339 UTC. */
    public var from: String?
    /** Provider is the provider whose meter answered. */
    public var provider: String?
    /** Range is the resolved period label. */
    public var range: String?
    /** Scope is always \"user\": the caller's own linked accounts. */
    public var scope: String?
    /** Source is always \"account\": the provider's own meter, not a Hanzo charge. */
    public var source: String?
    public var to: String?
    /** Windows is every window instance in range, newest first. */
    public var windows: [ReadingView]?

    public init(account: String? = nil, available: Bool? = nil, current: [ReadingView]? = nil, from: String? = nil, provider: String? = nil, range: String? = nil, scope: String? = nil, source: String? = nil, to: String? = nil, windows: [ReadingView]? = nil) {
        self.account = account
        self.available = available
        self.current = current
        self.from = from
        self.provider = provider
        self.range = range
        self.scope = scope
        self.source = source
        self.to = to
        self.windows = windows
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case account
        case available
        case current
        case from
        case provider
        case range
        case scope
        case source
        case to
        case windows
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(account, forKey: .account)
        try container.encodeIfPresent(available, forKey: .available)
        try container.encodeIfPresent(current, forKey: .current)
        try container.encodeIfPresent(from, forKey: .from)
        try container.encodeIfPresent(provider, forKey: .provider)
        try container.encodeIfPresent(range, forKey: .range)
        try container.encodeIfPresent(scope, forKey: .scope)
        try container.encodeIfPresent(source, forKey: .source)
        try container.encodeIfPresent(to, forKey: .to)
        try container.encodeIfPresent(windows, forKey: .windows)
    }
}

