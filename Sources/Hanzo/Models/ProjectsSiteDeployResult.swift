//
// ProjectsSiteDeployResult.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Published-site result returned by the /v1/sites deploy entrypoints. */
public struct ProjectsSiteDeployResult: Codable, JSONEncodable, Hashable {

    /** Canonical live URL, https://<slug>.<apex>. */
    public var url: String
    public var slug: String
    public var name: String
    /** The recorded deployment id (dep_...). */
    public var deploymentId: String
    /** Deployed file paths, sorted. */
    public var files: [String]
    /** Deployment status (live on success). */
    public var status: String

    public init(url: String, slug: String, name: String, deploymentId: String, files: [String], status: String) {
        self.url = url
        self.slug = slug
        self.name = name
        self.deploymentId = deploymentId
        self.files = files
        self.status = status
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case url
        case slug
        case name
        case deploymentId
        case files
        case status
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(url, forKey: .url)
        try container.encode(slug, forKey: .slug)
        try container.encode(name, forKey: .name)
        try container.encode(deploymentId, forKey: .deploymentId)
        try container.encode(files, forKey: .files)
        try container.encode(status, forKey: .status)
    }
}

