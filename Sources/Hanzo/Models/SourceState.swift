//
// SourceState.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct SourceState: Sendable, Codable, ParameterConvertible, Hashable {

    /** Available reports whether this ledger answered; false is honest \"unavailable\", never a zero that would read as no usage. */
    public var available: Bool?
    /** Note says in prose what the ledger's numbers mean. */
    public var note: String?
    /** Scope is whose usage the ledger measures: user or org. */
    public var scope: String?
    /** Source is the table of record behind the ledger. */
    public var source: String?

    public init(available: Bool? = nil, note: String? = nil, scope: String? = nil, source: String? = nil) {
        self.available = available
        self.note = note
        self.scope = scope
        self.source = source
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case available
        case note
        case scope
        case source
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(available, forKey: .available)
        try container.encodeIfPresent(note, forKey: .note)
        try container.encodeIfPresent(scope, forKey: .scope)
        try container.encodeIfPresent(source, forKey: .source)
    }
}

