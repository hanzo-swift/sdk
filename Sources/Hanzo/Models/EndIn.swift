//
// EndIn.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct EndIn: Sendable, Codable, ParameterConvertible, Hashable {

    /** ID is the sandbox whose lease ends, from an earlier lease. */
    public var id: String?
    /** Purge deletes the project's DISK as well. It is opt-in because the disk holds the only copy of the checkout: ending a lease is cheap and reversible, deleting someone's uncommitted work is neither. */
    public var purge: Bool?

    public init(id: String? = nil, purge: Bool? = nil) {
        self.id = id
        self.purge = purge
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case id
        case purge
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(id, forKey: .id)
        try container.encodeIfPresent(purge, forKey: .purge)
    }
}


@available(iOS 13, tvOS 13, watchOS 6, macOS 10.15, *)
extension EndIn: Identifiable {}
