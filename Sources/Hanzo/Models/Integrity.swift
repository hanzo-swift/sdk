//
// Integrity.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct Integrity: Sendable, Codable, ParameterConvertible, Hashable {

    /** BrokenAt is the seq of the FIRST record that failed verification, or -1 when OK. Reason describes the break (recomputed-hash mismatch, prev-hash discontinuity, or a seq gap). */
    public var brokenAt: Int?
    /** Count is the number of records walked. */
    public var count: Int?
    /** HeadHash is the hash of the last record (or the genesis anchor for an empty chain). Pin this externally over time to detect tail-truncation. */
    public var headHash: String?
    /** OK is true iff every record's stored hash equals the recomputed hash AND the chain links are continuous (each PrevHash == the prior record's Hash, seqs gapless from 0). */
    public var ok: Bool?
    public var reason: String?

    public init(brokenAt: Int? = nil, count: Int? = nil, headHash: String? = nil, ok: Bool? = nil, reason: String? = nil) {
        self.brokenAt = brokenAt
        self.count = count
        self.headHash = headHash
        self.ok = ok
        self.reason = reason
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case brokenAt
        case count
        case headHash
        case ok
        case reason
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(brokenAt, forKey: .brokenAt)
        try container.encodeIfPresent(count, forKey: .count)
        try container.encodeIfPresent(headHash, forKey: .headHash)
        try container.encodeIfPresent(ok, forKey: .ok)
        try container.encodeIfPresent(reason, forKey: .reason)
    }
}

