//
// GuardSanitizeRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct GuardSanitizeRequest: Codable, JSONEncodable, Hashable {

    public enum Direction: String, Codable, CaseIterable {
        case input = "input"
        case output = "output"
    }
    /** Text to sanitize */
    public var text: String
    /** Sanitization direction (input runs all 5 stages, output runs PII + content filter) */
    public var direction: Direction? = .input
    /** User ID for rate limiting and audit */
    public var userId: String?
    /** Session ID for audit correlation */
    public var sessionId: String?
    public var config: GuardSanitizeConfig?

    public init(text: String, direction: Direction? = .input, userId: String? = nil, sessionId: String? = nil, config: GuardSanitizeConfig? = nil) {
        self.text = text
        self.direction = direction
        self.userId = userId
        self.sessionId = sessionId
        self.config = config
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case text
        case direction
        case userId = "user_id"
        case sessionId = "session_id"
        case config
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(text, forKey: .text)
        try container.encodeIfPresent(direction, forKey: .direction)
        try container.encodeIfPresent(userId, forKey: .userId)
        try container.encodeIfPresent(sessionId, forKey: .sessionId)
        try container.encodeIfPresent(config, forKey: .config)
    }
}

