//
// EsignDocuments.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct EsignDocuments: Sendable, Codable, ParameterConvertible, Hashable {

    /** Documents is the caller org's documents, newest first, capped at 200. There is no paging, so read it as the recent window rather than a complete export. */
    public var documents: [EsignDocument]?

    public init(documents: [EsignDocument]? = nil) {
        self.documents = documents
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case documents
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(documents, forKey: .documents)
    }
}

