//
// ScaleIn.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct ScaleIn: Sendable, Codable, ParameterConvertible, Hashable {

    /** Count is the node count to set. */
    public var count: Int?
    /** ID is the DOKS cluster id, from the path. */
    public var id: String?
    /** Pool is the node pool, from the path. Its DO id or its name — both are unique within a cluster, and an operator reads the name off the board. */
    public var pool: String?

    public init(count: Int? = nil, id: String? = nil, pool: String? = nil) {
        self.count = count
        self.id = id
        self.pool = pool
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case count
        case id
        case pool
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(count, forKey: .count)
        try container.encodeIfPresent(id, forKey: .id)
        try container.encodeIfPresent(pool, forKey: .pool)
    }
}


@available(iOS 13, tvOS 13, watchOS 6, macOS 10.15, *)
extension ScaleIn: Identifiable {}
