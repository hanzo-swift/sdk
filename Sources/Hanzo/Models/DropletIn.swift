//
// DropletIn.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct DropletIn: Sendable, Codable, ParameterConvertible, Hashable {

    /** Disk requests a PERMANENT resize that grows the disk. DO can never resize such a droplet down again, so it defaults false — a CPU/RAM-only change, reversible. */
    public var disk: Bool?
    /** ID is the DO droplet id, from the path. Numeric. */
    public var id: String?
    /** Size is the target DigitalOcean size slug on resize, e.g. \"s-4vcpu-8gb\". */
    public var size: String?

    public init(disk: Bool? = nil, id: String? = nil, size: String? = nil) {
        self.disk = disk
        self.id = id
        self.size = size
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case disk
        case id
        case size
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(disk, forKey: .disk)
        try container.encodeIfPresent(id, forKey: .id)
        try container.encodeIfPresent(size, forKey: .size)
    }
}


@available(iOS 13, tvOS 13, watchOS 6, macOS 10.15, *)
extension DropletIn: Identifiable {}
