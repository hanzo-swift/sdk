//
// ReconcileSource.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct ReconcileSource: Sendable, Codable, ParameterConvertible, Hashable {

    /** Path is the directory WITHIN the repository that is rendered — everything outside it is not this plane's desired state and is never applied. */
    public var path: String?
    /** Ref is the branch or tag the revision was resolved from. */
    public var ref: String?
    /** Repo is the clone URL of the repository holding the desired state. */
    public var repo: String?

    public init(path: String? = nil, ref: String? = nil, repo: String? = nil) {
        self.path = path
        self.ref = ref
        self.repo = repo
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case path
        case ref
        case repo
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(path, forKey: .path)
        try container.encodeIfPresent(ref, forKey: .ref)
        try container.encodeIfPresent(repo, forKey: .repo)
    }
}

