//
// KmsSdkEnvelope.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Canonical signed wrapper for every /v1/sdk operation. The signature is ML-DSA-65 over SHAKE256(\&quot;lux-svc-envelope-v1\&quot; || FullDigest || canonical(v,id,ts,nonce,op,req)). One source of truth in luxfi/kms/pkg/envelope.  */
public struct KmsSdkEnvelope: Codable, JSONEncodable, Hashable {

    /** Envelope version. Only 1 is accepted. */
    public var v: Int
    public var id: KmsSdkEnvelopeIdentity
    /** Unix seconds at signing. Rejected outside ±5m of server time. */
    public var ts: Int64
    /** Caller-fresh anti-replay nonce (typically 16 random bytes, base64). Reuse within the window is rejected. */
    public var nonce: String
    /** Signed operation. 0x0040 get, 0x0041 put (also rotate), 0x0042 list, 0x0043 delete, 0x0050 sign, 0x0051 verify.  */
    public var op: Int
    /** Inner request JSON. Shape by op — secrets:{path,name,env[,value]}; sign:{validator_id,key_type,message}; verify adds {signature}.  */
    public var req: AnyCodable
    /** ML-DSA-65 signature (base64) over the canonical digest. */
    public var sig: Data

    public init(v: Int, id: KmsSdkEnvelopeIdentity, ts: Int64, nonce: String, op: Int, req: AnyCodable, sig: Data) {
        self.v = v
        self.id = id
        self.ts = ts
        self.nonce = nonce
        self.op = op
        self.req = req
        self.sig = sig
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case v
        case id
        case ts
        case nonce
        case op
        case req
        case sig
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(v, forKey: .v)
        try container.encode(id, forKey: .id)
        try container.encode(ts, forKey: .ts)
        try container.encode(nonce, forKey: .nonce)
        try container.encode(op, forKey: .op)
        try container.encode(req, forKey: .req)
        try container.encode(sig, forKey: .sig)
    }
}

