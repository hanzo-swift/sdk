//
// MqConsumerConfig.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** JetStream consumer configuration. */
public struct MqConsumerConfig: Codable, JSONEncodable, Hashable {

    public enum AckPolicy: String, Codable, CaseIterable {
        case _none = "none"
        case all = "all"
        case explicit = "explicit"
    }
    public enum DeliverPolicy: String, Codable, CaseIterable {
        case all = "all"
        case last = "last"
        case new = "new"
        case byStartSequence = "by_start_sequence"
        case byStartTime = "by_start_time"
        case lastPerSubject = "last_per_subject"
    }
    public enum ReplayPolicy: String, Codable, CaseIterable {
        case instant = "instant"
        case original = "original"
    }
    public static let durableNameRule = StringRule(minLength: nil, maxLength: 256, pattern: "/^[a-zA-Z0-9_-]+$/")
    /** Durable consumer name. If set, the consumer survives client disconnections.  */
    public var durableName: String?
    /** Subject filter. Only messages matching this subject are delivered. Supports wildcards.  */
    public var filterSubject: String?
    /** Acknowledgment policy. `explicit` requires per-message ack. `all` acks all messages up to the acked sequence. `none` disables acks.  */
    public var ackPolicy: AckPolicy? = .explicit
    /** Where to start delivery. `all` delivers from the beginning. `last` delivers the last message. `new` delivers only new messages. `by_start_sequence` and `by_start_time` start from a specific point.  */
    public var deliverPolicy: DeliverPolicy? = .all
    /** Starting sequence number (used with deliver_policy `by_start_sequence`).  */
    public var optStartSeq: Int?
    /** Starting timestamp (used with deliver_policy `by_start_time`).  */
    public var optStartTime: Date?
    /** Maximum delivery attempts before the message is dropped or sent to a dead letter subject (-1 for unlimited).  */
    public var maxDeliver: Int? = -1
    /** Time to wait for acknowledgment before redelivery (e.g., \"30s\", \"5m\"). Defaults to \"30s\".  */
    public var ackWait: String? = "30s"
    /** Replay policy for historical messages. `instant` delivers as fast as possible. `original` preserves original timing gaps.  */
    public var replayPolicy: ReplayPolicy? = .instant
    /** Maximum number of unacknowledged messages before delivery pauses.  */
    public var maxAckPending: Int? = 1000
    /** Optional human-readable description. */
    public var description: String?

    public init(durableName: String? = nil, filterSubject: String? = nil, ackPolicy: AckPolicy? = .explicit, deliverPolicy: DeliverPolicy? = .all, optStartSeq: Int? = nil, optStartTime: Date? = nil, maxDeliver: Int? = -1, ackWait: String? = "30s", replayPolicy: ReplayPolicy? = .instant, maxAckPending: Int? = 1000, description: String? = nil) {
        self.durableName = durableName
        self.filterSubject = filterSubject
        self.ackPolicy = ackPolicy
        self.deliverPolicy = deliverPolicy
        self.optStartSeq = optStartSeq
        self.optStartTime = optStartTime
        self.maxDeliver = maxDeliver
        self.ackWait = ackWait
        self.replayPolicy = replayPolicy
        self.maxAckPending = maxAckPending
        self.description = description
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case durableName = "durable_name"
        case filterSubject = "filter_subject"
        case ackPolicy = "ack_policy"
        case deliverPolicy = "deliver_policy"
        case optStartSeq = "opt_start_seq"
        case optStartTime = "opt_start_time"
        case maxDeliver = "max_deliver"
        case ackWait = "ack_wait"
        case replayPolicy = "replay_policy"
        case maxAckPending = "max_ack_pending"
        case description
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(durableName, forKey: .durableName)
        try container.encodeIfPresent(filterSubject, forKey: .filterSubject)
        try container.encodeIfPresent(ackPolicy, forKey: .ackPolicy)
        try container.encodeIfPresent(deliverPolicy, forKey: .deliverPolicy)
        try container.encodeIfPresent(optStartSeq, forKey: .optStartSeq)
        try container.encodeIfPresent(optStartTime, forKey: .optStartTime)
        try container.encodeIfPresent(maxDeliver, forKey: .maxDeliver)
        try container.encodeIfPresent(ackWait, forKey: .ackWait)
        try container.encodeIfPresent(replayPolicy, forKey: .replayPolicy)
        try container.encodeIfPresent(maxAckPending, forKey: .maxAckPending)
        try container.encodeIfPresent(description, forKey: .description)
    }
}

