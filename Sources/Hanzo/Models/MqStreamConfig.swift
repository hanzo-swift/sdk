//
// MqStreamConfig.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** JetStream stream configuration. */
public struct MqStreamConfig: Codable, JSONEncodable, Hashable {

    public enum Retention: String, Codable, CaseIterable {
        case limits = "limits"
        case interest = "interest"
        case workqueue = "workqueue"
    }
    public enum Storage: String, Codable, CaseIterable {
        case file = "file"
        case memory = "memory"
    }
    public enum Discard: String, Codable, CaseIterable {
        case old = "old"
        case new = "new"
    }
    public static let nameRule = StringRule(minLength: nil, maxLength: 256, pattern: "/^[a-zA-Z0-9_-]+$/")
    public static let numReplicasRule = NumericRule<Int>(minimum: 1, exclusiveMinimum: false, maximum: 5, exclusiveMaximum: false, multipleOf: nil)
    /** Stream name (alphanumeric, hyphens, underscores). */
    public var name: String
    /** Subjects bound to this stream. Supports wildcards.  */
    public var subjects: [String]
    /** Message retention policy. `limits` keeps messages until limits are hit. `interest` keeps messages while consumers exist. `workqueue` deletes messages after acknowledgment.  */
    public var retention: Retention? = .limits
    /** Maximum number of messages (-1 for unlimited). */
    public var maxMsgs: Int64? = -1
    /** Maximum total bytes (-1 for unlimited). */
    public var maxBytes: Int64? = -1
    /** Maximum message age (e.g., \"24h\", \"7d\", \"0\" for unlimited).  */
    public var maxAge: String? = "0"
    /** Maximum single message size in bytes (-1 for default). */
    public var maxMsgSize: Int? = -1
    /** Storage backend for stream data. */
    public var storage: Storage? = .file
    /** Number of replicas in the cluster. */
    public var numReplicas: Int? = 1
    /** Discard policy when limits are reached. `old` discards the oldest messages. `new` rejects new messages.  */
    public var discard: Discard? = .old
    /** Window for message deduplication based on Nats-Msg-Id header (e.g., \"2m\"). Defaults to \"2m\".  */
    public var duplicateWindow: String? = "2m"
    /** Optional human-readable description. */
    public var description: String?

    public init(name: String, subjects: [String], retention: Retention? = .limits, maxMsgs: Int64? = -1, maxBytes: Int64? = -1, maxAge: String? = "0", maxMsgSize: Int? = -1, storage: Storage? = .file, numReplicas: Int? = 1, discard: Discard? = .old, duplicateWindow: String? = "2m", description: String? = nil) {
        self.name = name
        self.subjects = subjects
        self.retention = retention
        self.maxMsgs = maxMsgs
        self.maxBytes = maxBytes
        self.maxAge = maxAge
        self.maxMsgSize = maxMsgSize
        self.storage = storage
        self.numReplicas = numReplicas
        self.discard = discard
        self.duplicateWindow = duplicateWindow
        self.description = description
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case name
        case subjects
        case retention
        case maxMsgs = "max_msgs"
        case maxBytes = "max_bytes"
        case maxAge = "max_age"
        case maxMsgSize = "max_msg_size"
        case storage
        case numReplicas = "num_replicas"
        case discard
        case duplicateWindow = "duplicate_window"
        case description
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(name, forKey: .name)
        try container.encode(subjects, forKey: .subjects)
        try container.encodeIfPresent(retention, forKey: .retention)
        try container.encodeIfPresent(maxMsgs, forKey: .maxMsgs)
        try container.encodeIfPresent(maxBytes, forKey: .maxBytes)
        try container.encodeIfPresent(maxAge, forKey: .maxAge)
        try container.encodeIfPresent(maxMsgSize, forKey: .maxMsgSize)
        try container.encodeIfPresent(storage, forKey: .storage)
        try container.encodeIfPresent(numReplicas, forKey: .numReplicas)
        try container.encodeIfPresent(discard, forKey: .discard)
        try container.encodeIfPresent(duplicateWindow, forKey: .duplicateWindow)
        try container.encodeIfPresent(description, forKey: .description)
    }
}

