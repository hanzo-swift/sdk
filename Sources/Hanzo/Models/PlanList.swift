//
// PlanList.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct PlanList: Sendable, Codable, ParameterConvertible, Hashable {

    /** Plans are the plans in this section, each an opaque object exactly as the @hanzo/plans catalog emits it — typically id, name, description, priceMonthly, category, a feature list, a limits block and a price_ref. */
    public var plans: [JSONValue]?

    public init(plans: [JSONValue]? = nil) {
        self.plans = plans
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case plans
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(plans, forKey: .plans)
    }
}

