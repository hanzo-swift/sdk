//
// AccountFoldView.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct AccountFoldView: Sendable, Codable, ParameterConvertible, Hashable {

    /** Account is the account as it is now recorded. */
    public var account: CloudAccountView?
    /** Clusters is one entry per cluster discovered in the account. It is empty when discovery itself failed, which leaves the previously folded set untouched rather than mass-detaching it. */
    public var clusters: [ClusterResult]?

    public init(account: CloudAccountView? = nil, clusters: [ClusterResult]? = nil) {
        self.account = account
        self.clusters = clusters
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case account
        case clusters
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(account, forKey: .account)
        try container.encodeIfPresent(clusters, forKey: .clusters)
    }
}

