//
// SeedRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct SeedRequest: Sendable, Codable, ParameterConvertible, Hashable {

    /** AmountCents is the capital to inject, in minor units. Must be > 0. */
    public var amountCents: Int?
    /** Memo is the operator's note on the entry. Empty takes \"reserve capital injection\". */
    public var memo: String?
    /** Ref is an idempotency key. Without one each seed is a distinct injection. */
    public var ref: String?

    public init(amountCents: Int? = nil, memo: String? = nil, ref: String? = nil) {
        self.amountCents = amountCents
        self.memo = memo
        self.ref = ref
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case amountCents
        case memo
        case ref
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(amountCents, forKey: .amountCents)
        try container.encodeIfPresent(memo, forKey: .memo)
        try container.encodeIfPresent(ref, forKey: .ref)
    }
}

