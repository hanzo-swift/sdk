//
// O11yLogsResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct O11yLogsResponse: Codable, JSONEncodable, Hashable {

    public enum View: String, Codable, CaseIterable {
        case infra = "infra"
        case request = "request"
    }
    public var product: String?
    /** infra for a validated SuperAdmin, request for every other org. */
    public var view: View?
    public var lines: [O11yLogLine]?
    /** Max nanosecond cursor. Pass back as sinceNs for the next tail poll. */
    public var nextCursor: Int64?

    public init(product: String? = nil, view: View? = nil, lines: [O11yLogLine]? = nil, nextCursor: Int64? = nil) {
        self.product = product
        self.view = view
        self.lines = lines
        self.nextCursor = nextCursor
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case product
        case view
        case lines
        case nextCursor
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(product, forKey: .product)
        try container.encodeIfPresent(view, forKey: .view)
        try container.encodeIfPresent(lines, forKey: .lines)
        try container.encodeIfPresent(nextCursor, forKey: .nextCursor)
    }
}

