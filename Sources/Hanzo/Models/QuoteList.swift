//
// QuoteList.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct QuoteList: Sendable, Codable, ParameterConvertible, Hashable {

    /** Results is one quote per name, priced RETAIL — this deployment's markup is already applied and the wholesale cost is never on the wire. */
    public var results: [Offer]?

    public init(results: [Offer]? = nil) {
        self.results = results
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case results
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(results, forKey: .results)
    }
}

