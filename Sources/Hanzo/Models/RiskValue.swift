//
// RiskValue.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct RiskValue: Sendable, Codable, ParameterConvertible, Hashable {

    /** Baseline is what Observed was measured against: this organisation's own history for this subject. */
    public var baseline: Double?
    /** Blind marks a coordinate that could not be computed and took its neutral value. A model silently reading neutral for a dimension it never has data for is indistinguishable from one reading a genuine absence of risk. */
    public var blind: Bool?
    /** Feature is the dimension. */
    public var feature: String?
    /** Observed is the raw number X was computed from, quoted so the coordinate reads back as a sentence rather than a bare ratio. */
    public var observed: Double?
    /** Unit is how to read Observed. */
    public var unit: String?
    /** X is the coordinate in the model space, always dimensionless. */
    public var x: Double?

    public init(baseline: Double? = nil, blind: Bool? = nil, feature: String? = nil, observed: Double? = nil, unit: String? = nil, x: Double? = nil) {
        self.baseline = baseline
        self.blind = blind
        self.feature = feature
        self.observed = observed
        self.unit = unit
        self.x = x
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case baseline
        case blind
        case feature
        case observed
        case unit
        case x
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(baseline, forKey: .baseline)
        try container.encodeIfPresent(blind, forKey: .blind)
        try container.encodeIfPresent(feature, forKey: .feature)
        try container.encodeIfPresent(observed, forKey: .observed)
        try container.encodeIfPresent(unit, forKey: .unit)
        try container.encodeIfPresent(x, forKey: .x)
    }
}

