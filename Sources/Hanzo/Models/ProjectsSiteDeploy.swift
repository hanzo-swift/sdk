//
// ProjectsSiteDeploy.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct ProjectsSiteDeploy: Sendable, Codable, ParameterConvertible, Hashable {

    /** DeploymentID is the deployment this publish recorded, for the history. */
    public var deploymentId: String?
    /** Files are the site-relative paths that were uploaded, sorted. */
    public var files: [String]?
    /** Name is the project's display name. */
    public var name: String?
    /** Slug is the project the site was published into, created on the fly when the slug was free. */
    public var slug: String?
    /** Status is the deployment status, \"live\" on success. */
    public var status: String?
    /** URL is the canonical live URL, https://<slug>.<apex> — empty when the subdomain belongs to another tenant and this site has none. */
    public var url: String?

    public init(deploymentId: String? = nil, files: [String]? = nil, name: String? = nil, slug: String? = nil, status: String? = nil, url: String? = nil) {
        self.deploymentId = deploymentId
        self.files = files
        self.name = name
        self.slug = slug
        self.status = status
        self.url = url
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case deploymentId
        case files
        case name
        case slug
        case status
        case url
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(deploymentId, forKey: .deploymentId)
        try container.encodeIfPresent(files, forKey: .files)
        try container.encodeIfPresent(name, forKey: .name)
        try container.encodeIfPresent(slug, forKey: .slug)
        try container.encodeIfPresent(status, forKey: .status)
        try container.encodeIfPresent(url, forKey: .url)
    }
}

