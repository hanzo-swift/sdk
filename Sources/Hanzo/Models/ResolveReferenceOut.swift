//
// ResolveReferenceOut.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct ResolveReferenceOut: Sendable, Codable, ParameterConvertible, Hashable {

    /** Answers is one entry per (set, key) consulted. */
    public var answers: [ReferenceAnswer]?
    /** Consulted names the version of every set that took part, so a decision can record precisely what it leaned on. Record this with the decision: it is what makes the decision reproducible a year later. */
    public var consulted: [ReferenceVersion]?
    /** Refused names the consulted sets that could not answer at all. A key that missed in one of these is UNKNOWN, not clean. */
    public var refused: [String]?
    /** Stale names the consulted sets past their freshness bound. Staleness is itself a risk signal — a decision taken against a three-week-old list is a weaker decision, and this is how it knows. */
    public var stale: [String]?

    public init(answers: [ReferenceAnswer]? = nil, consulted: [ReferenceVersion]? = nil, refused: [String]? = nil, stale: [String]? = nil) {
        self.answers = answers
        self.consulted = consulted
        self.refused = refused
        self.stale = stale
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case answers
        case consulted
        case refused
        case stale
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(answers, forKey: .answers)
        try container.encodeIfPresent(consulted, forKey: .consulted)
        try container.encodeIfPresent(refused, forKey: .refused)
        try container.encodeIfPresent(stale, forKey: .stale)
    }
}

