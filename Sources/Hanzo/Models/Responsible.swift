//
// Responsible.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct Responsible: Sendable, Codable, ParameterConvertible, Hashable {

    /** Country is where they reside, ISO 3166-1 alpha-2. */
    public var country: String?
    /** Email reaches them for signature. */
    public var email: String?
    /** Name is their full legal name as the IRS will hold it. */
    public var name: String?
    /** USTaxID reports that they hold an SSN or ITIN. It is a BOOLEAN on purpose: the number itself is never needed here and a field that could hold it is a field that will eventually be logged. */
    public var usTaxId: Bool?

    public init(country: String? = nil, email: String? = nil, name: String? = nil, usTaxId: Bool? = nil) {
        self.country = country
        self.email = email
        self.name = name
        self.usTaxId = usTaxId
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case country
        case email
        case name
        case usTaxId
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(country, forKey: .country)
        try container.encodeIfPresent(email, forKey: .email)
        try container.encodeIfPresent(name, forKey: .name)
        try container.encodeIfPresent(usTaxId, forKey: .usTaxId)
    }
}

