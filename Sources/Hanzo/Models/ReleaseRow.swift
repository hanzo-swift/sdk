//
// ReleaseRow.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct ReleaseRow: Sendable, Codable, ParameterConvertible, Hashable {

    /** Environment is the deploy target the application names. */
    public var environment: String?
    /** ID is the deployment's id — a release IS a deployment that reached the cluster. */
    public var id: String?
    /** Name is the application the release belongs to. */
    public var name: String?
    /** ReleasedAt is when the deployment last changed, RFC3339 UTC. */
    public var releasedAt: String?
    /** Status is deploying or live — the two states that mean released. */
    public var status: String?
    /** Version is the released image tag, or v<n> when the image carries none. */
    public var version: String?

    public init(environment: String? = nil, id: String? = nil, name: String? = nil, releasedAt: String? = nil, status: String? = nil, version: String? = nil) {
        self.environment = environment
        self.id = id
        self.name = name
        self.releasedAt = releasedAt
        self.status = status
        self.version = version
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case environment
        case id
        case name
        case releasedAt
        case status
        case version
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(environment, forKey: .environment)
        try container.encodeIfPresent(id, forKey: .id)
        try container.encodeIfPresent(name, forKey: .name)
        try container.encodeIfPresent(releasedAt, forKey: .releasedAt)
        try container.encodeIfPresent(status, forKey: .status)
        try container.encodeIfPresent(version, forKey: .version)
    }
}


@available(iOS 13, tvOS 13, watchOS 6, macOS 10.15, *)
extension ReleaseRow: Identifiable {}
