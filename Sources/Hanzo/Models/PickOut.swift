//
// PickOut.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct PickOut: Sendable, Codable, ParameterConvertible, Hashable {

    /** Consumers is the page, ordered by name. */
    public var consumers: [Consumer]?
    /** Total is the stream's consumer count before paging. */
    public var total: Int64?

    public init(consumers: [Consumer]? = nil, total: Int64? = nil) {
        self.consumers = consumers
        self.total = total
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case consumers
        case total
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(consumers, forKey: .consumers)
        try container.encodeIfPresent(total, forKey: .total)
    }
}

