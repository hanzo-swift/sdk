//
// EsignCompleteIn.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct EsignCompleteIn: Sendable, Codable, ParameterConvertible, Hashable {

    /** Signed, when present, overrides what the provider reports — the manual path for a provider whose webhook is not wired. Omit it to take the provider's answer. */
    public var signed: Bool?

    public init(signed: Bool? = nil) {
        self.signed = signed
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case signed
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(signed, forKey: .signed)
    }
}

