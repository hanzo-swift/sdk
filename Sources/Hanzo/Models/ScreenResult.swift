//
// ScreenResult.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct ScreenResult: Sendable, Codable, ParameterConvertible, Hashable {

    /** DraftReply is a suggested email reply for staff to edit and send. */
    public var draftReply: String?
    /** Error says why a failed screen failed — no AI gateway configured, a gateway error, or a reply that carried no parseable JSON. Absent on success. */
    public var error: String?
    /** Model is the LLM the screen ran on. */
    public var model: String?
    /** Score is the model's 0..100 fit score, clamped to that range. */
    public var score: Int?
    /** ScreenedAt is the unix second the screen finished (0 while pending). */
    public var screenedAt: Int?
    /** Status is the screen's state: pending | done | failed. */
    public var status: String?
    /** SuggestedCredits is the recommended credit grant in USD, snapped to the nearest allowed rung: 0 | 5000 | 25000 | 50000 | 150000. */
    public var suggestedCredits: Int?
    /** Summary is the model's short assessment of the application. */
    public var summary: String?
    /** Tier1Backed is the model's read on tier-1 backing, normalized to \"yes\", \"no\" or \"unclear\" (anything it cannot resolve reads \"unclear\"). */
    public var tier1Backed: String?

    public init(draftReply: String? = nil, error: String? = nil, model: String? = nil, score: Int? = nil, screenedAt: Int? = nil, status: String? = nil, suggestedCredits: Int? = nil, summary: String? = nil, tier1Backed: String? = nil) {
        self.draftReply = draftReply
        self.error = error
        self.model = model
        self.score = score
        self.screenedAt = screenedAt
        self.status = status
        self.suggestedCredits = suggestedCredits
        self.summary = summary
        self.tier1Backed = tier1Backed
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case draftReply
        case error
        case model
        case score
        case screenedAt
        case status
        case suggestedCredits
        case summary
        case tier1Backed
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(draftReply, forKey: .draftReply)
        try container.encodeIfPresent(error, forKey: .error)
        try container.encodeIfPresent(model, forKey: .model)
        try container.encodeIfPresent(score, forKey: .score)
        try container.encodeIfPresent(screenedAt, forKey: .screenedAt)
        try container.encodeIfPresent(status, forKey: .status)
        try container.encodeIfPresent(suggestedCredits, forKey: .suggestedCredits)
        try container.encodeIfPresent(summary, forKey: .summary)
        try container.encodeIfPresent(tier1Backed, forKey: .tier1Backed)
    }
}

