//
// WebResult.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct WebResult: Sendable, Codable, ParameterConvertible, Hashable {

    /** Content is the ENGINE's snippet — the few lines shown under the title, not the page's text. Read the page itself with POST /v1/crawl. */
    public var content: String?
    /** Engine names the backend that found this hit, so one engine's view of a query can be told from another's. */
    public var engine: String?
    /** Title is the page's title. */
    public var title: String?
    /** URL is the page's address, as the engine reported it. */
    public var url: String?

    public init(content: String? = nil, engine: String? = nil, title: String? = nil, url: String? = nil) {
        self.content = content
        self.engine = engine
        self.title = title
        self.url = url
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case content
        case engine
        case title
        case url
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(content, forKey: .content)
        try container.encodeIfPresent(engine, forKey: .engine)
        try container.encodeIfPresent(title, forKey: .title)
        try container.encodeIfPresent(url, forKey: .url)
    }
}

