//
// ProductVectorStats.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Aggregate vector statistics across all collections. */
public struct ProductVectorStats: Codable, JSONEncodable, Hashable {

    /** Total number of collections. */
    public var totalCollections: Int64
    /** Total vectors across all collections. */
    public var totalVectors: Int64
    /** Total storage bytes across all collections. */
    public var totalStorageBytes: Int64

    public init(totalCollections: Int64, totalVectors: Int64, totalStorageBytes: Int64) {
        self.totalCollections = totalCollections
        self.totalVectors = totalVectors
        self.totalStorageBytes = totalStorageBytes
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case totalCollections
        case totalVectors
        case totalStorageBytes
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(totalCollections, forKey: .totalCollections)
        try container.encode(totalVectors, forKey: .totalVectors)
        try container.encode(totalStorageBytes, forKey: .totalStorageBytes)
    }
}

