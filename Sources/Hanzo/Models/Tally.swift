//
// Tally.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct Tally: Sendable, Codable, ParameterConvertible, Hashable {

    /** AccruedLifetimeCents is all commission ever accrued, summed across every affiliate, in cents. It only grows; a payout does not reduce it. */
    public var accruedLifetimeCents: Int?
    /** Affiliates is how many affiliate rows the board read, at every status. The read is bounded at 1000 rows, so a larger fleet reports the bound. */
    public var affiliates: Int?
    /** Approved is how many of those rows are approved — the only ones whose code resolves for attribution and whose balance can grow. */
    public var approved: Int?
    /** PaidLifetimeCents is all commission ever paid out, in cents: credits grants plus record-only cash disbursements. */
    public var paidLifetimeCents: Int?
    /** PendingLiabilityCents is accrued minus paid across every affiliate, in cents. Read it as money OWED and not yet disbursed — a liability, not spend. */
    public var pendingLiabilityCents: Int?

    public init(accruedLifetimeCents: Int? = nil, affiliates: Int? = nil, approved: Int? = nil, paidLifetimeCents: Int? = nil, pendingLiabilityCents: Int? = nil) {
        self.accruedLifetimeCents = accruedLifetimeCents
        self.affiliates = affiliates
        self.approved = approved
        self.paidLifetimeCents = paidLifetimeCents
        self.pendingLiabilityCents = pendingLiabilityCents
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case accruedLifetimeCents
        case affiliates
        case approved
        case paidLifetimeCents
        case pendingLiabilityCents
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(accruedLifetimeCents, forKey: .accruedLifetimeCents)
        try container.encodeIfPresent(affiliates, forKey: .affiliates)
        try container.encodeIfPresent(approved, forKey: .approved)
        try container.encodeIfPresent(paidLifetimeCents, forKey: .paidLifetimeCents)
        try container.encodeIfPresent(pendingLiabilityCents, forKey: .pendingLiabilityCents)
    }
}

