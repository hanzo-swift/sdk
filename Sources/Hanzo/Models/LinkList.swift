//
// LinkList.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct LinkList: Sendable, Codable, ParameterConvertible, Hashable {

    /** Devices is the same rows folded per machine — the cross-machine \"AI Providers / Accounts\" view. */
    public var devices: [DeviceView]?
    /** Links is every link the caller registered, newest first. Revoked links are INCLUDED rather than dropped, because a logged-out account keeps its usage history and audit trail. */
    public var links: [LinkView]?

    public init(devices: [DeviceView]? = nil, links: [LinkView]? = nil) {
        self.devices = devices
        self.links = links
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case devices
        case links
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(devices, forKey: .devices)
        try container.encodeIfPresent(links, forKey: .links)
    }
}

