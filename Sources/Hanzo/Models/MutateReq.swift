//
// MutateReq.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct MutateReq: Sendable, Codable, ParameterConvertible, Hashable {

    /** Add is the product ids to turn ON. Each must already be an ACTIVE entitlement of the org's plan, unless the caller is a platform super admin. */
    public var add: [String]?
    /** Remove is the product ids to turn OFF. Disabling is never gated. */
    public var remove: [String]?

    public init(add: [String]? = nil, remove: [String]? = nil) {
        self.add = add
        self.remove = remove
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case add
        case remove
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(add, forKey: .add)
        try container.encodeIfPresent(remove, forKey: .remove)
    }
}

