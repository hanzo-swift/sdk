//
// ReferralBoard.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct ReferralBoard: Sendable, Codable, ParameterConvertible, Hashable {

    /** AccrualByLevel splits the lifetime accrual across the three upline levels — how much of the liability comes from direct referrals versus the chain above. */
    public var accrualByLevel: LevelSplit?
    /** Conversion is the funnel: referred orgs against those that actually earned. */
    public var conversion: Funnel?
    /** Summary is the fleet tally — population by status, and lifetime accrued, paid and still-owed commission. */
    public var summary: Tally?
    /** TopReferrers is the 25 affiliates with the most lifetime accrued commission, descending, orgs named. */
    public var topReferrers: [ReferrerRow]?

    public init(accrualByLevel: LevelSplit? = nil, conversion: Funnel? = nil, summary: Tally? = nil, topReferrers: [ReferrerRow]? = nil) {
        self.accrualByLevel = accrualByLevel
        self.conversion = conversion
        self.summary = summary
        self.topReferrers = topReferrers
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case accrualByLevel
        case conversion
        case summary
        case topReferrers
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(accrualByLevel, forKey: .accrualByLevel)
        try container.encodeIfPresent(conversion, forKey: .conversion)
        try container.encodeIfPresent(summary, forKey: .summary)
        try container.encodeIfPresent(topReferrers, forKey: .topReferrers)
    }
}

