//
// TrustDesk.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct TrustDesk: Sendable, Codable, ParameterConvertible, Hashable {

    /** Grants is every grant that has been made, newest first. */
    public var grants: [TrustGrantView]?
    /** Items is everything the org holds, both tiers, retired included. */
    public var items: [TrustItemView]?
    /** Name is the centre's display name. */
    public var name: String?
    /** Nda is the text a party must accept before asking. */
    public var nda: String?
    /** Published is whether the centre answers at its public address. */
    public var published: Bool?
    /** Requests is every ask, newest first, open ones included. */
    public var requests: [TrustAskView]?
    /** Slug is the public address, empty until the centre is published. */
    public var slug: String?

    public init(grants: [TrustGrantView]? = nil, items: [TrustItemView]? = nil, name: String? = nil, nda: String? = nil, published: Bool? = nil, requests: [TrustAskView]? = nil, slug: String? = nil) {
        self.grants = grants
        self.items = items
        self.name = name
        self.nda = nda
        self.published = published
        self.requests = requests
        self.slug = slug
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case grants
        case items
        case name
        case nda
        case published
        case requests
        case slug
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(grants, forKey: .grants)
        try container.encodeIfPresent(items, forKey: .items)
        try container.encodeIfPresent(name, forKey: .name)
        try container.encodeIfPresent(nda, forKey: .nda)
        try container.encodeIfPresent(published, forKey: .published)
        try container.encodeIfPresent(requests, forKey: .requests)
        try container.encodeIfPresent(slug, forKey: .slug)
    }
}

