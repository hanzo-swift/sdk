//
// Readiness.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct Readiness: Sendable, Codable, ParameterConvertible, Hashable {

    /** CRD is whether the operator App CRD was found, and is absent when no cluster client resolved and the question could not be asked. */
    public var crd: Bool?
    /** Error is the real reason the plane is degraded; absent when it is not. */
    public var error: String?
    /** K8s is whether a cluster client resolved at all. False means no kubeconfig. */
    public var k8s: Bool?
    /** Service is always \"platform\" — which control plane answered. */
    public var service: String?
    /** Status is \"ok\" when this plane can deploy, \"degraded\" when it cannot. */
    public var status: String?

    public init(crd: Bool? = nil, error: String? = nil, k8s: Bool? = nil, service: String? = nil, status: String? = nil) {
        self.crd = crd
        self.error = error
        self.k8s = k8s
        self.service = service
        self.status = status
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case crd
        case error
        case k8s
        case service
        case status
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(crd, forKey: .crd)
        try container.encodeIfPresent(error, forKey: .error)
        try container.encodeIfPresent(k8s, forKey: .k8s)
        try container.encodeIfPresent(service, forKey: .service)
        try container.encodeIfPresent(status, forKey: .status)
    }
}

