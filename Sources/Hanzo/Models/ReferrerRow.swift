//
// ReferrerRow.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct ReferrerRow: Sendable, Codable, ParameterConvertible, Hashable {

    /** AccruedCents is lifetime commission accrued, in cents. The board is sorted by this, descending. */
    public var accruedCents: Int?
    /** Code is that affiliate's minted referral code; empty if it is not approved. */
    public var code: String?
    /** Org is the partner's own org slug. Named only here, on the SuperAdmin board — the partner-facing leaderboard shows an opt-in handle and never an org. */
    public var org: String?
    /** PendingCents is accrued minus paid, in cents — what is still owed to this affiliate. Never negative. */
    public var pendingCents: Int?
    /** ReferredCount is how many orgs this affiliate is the DIRECT referrer of — its level-1 downline, not the whole three-level chain. */
    public var referredCount: Int?
    /** Status is \"applied\", \"approved\" or \"suspended\". */
    public var status: String?

    public init(accruedCents: Int? = nil, code: String? = nil, org: String? = nil, pendingCents: Int? = nil, referredCount: Int? = nil, status: String? = nil) {
        self.accruedCents = accruedCents
        self.code = code
        self.org = org
        self.pendingCents = pendingCents
        self.referredCount = referredCount
        self.status = status
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case accruedCents
        case code
        case org
        case pendingCents
        case referredCount
        case status
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(accruedCents, forKey: .accruedCents)
        try container.encodeIfPresent(code, forKey: .code)
        try container.encodeIfPresent(org, forKey: .org)
        try container.encodeIfPresent(pendingCents, forKey: .pendingCents)
        try container.encodeIfPresent(referredCount, forKey: .referredCount)
        try container.encodeIfPresent(status, forKey: .status)
    }
}

