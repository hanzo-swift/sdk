//
// IngestOut.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct IngestOut: Sendable, Codable, ParameterConvertible, Hashable {

    /** AttemptsIngested is how many attempt versions this call appended. */
    public var attemptsIngested: Int?
    /** AttemptsRetained is the full versioned attempt history the store now holds. */
    public var attemptsRetained: Int?
    /** CanonicalAttempts is the deduped attempt count the store now holds. */
    public var canonicalAttempts: Int?
    /** CanonicalExperiments is the deduped experiment count the store now holds. */
    public var canonicalExperiments: Int?
    /** ExperimentsIngested is how many experiment versions this call appended. */
    public var experimentsIngested: Int?
    /** ExperimentsRetained is the full versioned experiment history the store now holds. */
    public var experimentsRetained: Int?
    /** Project is the project the batch was filed under — the SERVER's value, never the body's. */
    public var project: String?
    /** RolledUp is false when the OLAP roll-up was skipped; the SQLite write still stands. */
    public var rolledUp: Bool?

    public init(attemptsIngested: Int? = nil, attemptsRetained: Int? = nil, canonicalAttempts: Int? = nil, canonicalExperiments: Int? = nil, experimentsIngested: Int? = nil, experimentsRetained: Int? = nil, project: String? = nil, rolledUp: Bool? = nil) {
        self.attemptsIngested = attemptsIngested
        self.attemptsRetained = attemptsRetained
        self.canonicalAttempts = canonicalAttempts
        self.canonicalExperiments = canonicalExperiments
        self.experimentsIngested = experimentsIngested
        self.experimentsRetained = experimentsRetained
        self.project = project
        self.rolledUp = rolledUp
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case attemptsIngested = "attempts_ingested"
        case attemptsRetained = "attempts_retained"
        case canonicalAttempts = "canonical_attempts"
        case canonicalExperiments = "canonical_experiments"
        case experimentsIngested = "experiments_ingested"
        case experimentsRetained = "experiments_retained"
        case project
        case rolledUp = "rolled_up"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(attemptsIngested, forKey: .attemptsIngested)
        try container.encodeIfPresent(attemptsRetained, forKey: .attemptsRetained)
        try container.encodeIfPresent(canonicalAttempts, forKey: .canonicalAttempts)
        try container.encodeIfPresent(canonicalExperiments, forKey: .canonicalExperiments)
        try container.encodeIfPresent(experimentsIngested, forKey: .experimentsIngested)
        try container.encodeIfPresent(experimentsRetained, forKey: .experimentsRetained)
        try container.encodeIfPresent(project, forKey: .project)
        try container.encodeIfPresent(rolledUp, forKey: .rolledUp)
    }
}

