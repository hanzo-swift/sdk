//
// MlResource.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct MlResource: Sendable, Codable, ParameterConvertible, Hashable {

    /** CreatedAt is when Kubernetes admitted the object, RFC 3339 in UTC. */
    public var createdAt: String?
    /** Name is the object's metadata.name, unique within the caller's namespace. */
    public var name: String?
    /** Spec is the resource spec, verbatim as Kubernetes stores it. Present on a single-object read, absent from a list. */
    public var spec: [String: JSONValue]?
    /** Status is the live status kserve owns, verbatim. Absent until kserve has written one. */
    public var status: [String: JSONValue]?

    public init(createdAt: String? = nil, name: String? = nil, spec: [String: JSONValue]? = nil, status: [String: JSONValue]? = nil) {
        self.createdAt = createdAt
        self.name = name
        self.spec = spec
        self.status = status
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case createdAt
        case name
        case spec
        case status
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(createdAt, forKey: .createdAt)
        try container.encodeIfPresent(name, forKey: .name)
        try container.encodeIfPresent(spec, forKey: .spec)
        try container.encodeIfPresent(status, forKey: .status)
    }
}

