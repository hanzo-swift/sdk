//
// ProjectsDomainsBind.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct ProjectsDomainsBind: Sendable, Codable, ParameterConvertible, Hashable {

    /** Domains are the custom hostnames to attach, in order. An empty list is a 400 rather than a clear — releasing a host is its own call. */
    public var domains: [String]?
    /** Slug is the site the hosts attach to, from the path. */
    public var slug: String?

    public init(domains: [String]? = nil, slug: String? = nil) {
        self.domains = domains
        self.slug = slug
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case domains
        case slug
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(domains, forKey: .domains)
        try container.encodeIfPresent(slug, forKey: .slug)
    }
}

