//
// BackfillResult.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct BackfillResult: Sendable, Codable, ParameterConvertible, Hashable {

    /** Forced is true when the caller overrode the already-populated guard. */
    public var forced: Bool?
    /** SeededBefore is the RFC3339 upper bound the seed actually used. */
    public var seededBefore: String?
    /** Status is \"ok\" — a seed that did not run answered an error instead. */
    public var status: String?

    public init(forced: Bool? = nil, seededBefore: String? = nil, status: String? = nil) {
        self.forced = forced
        self.seededBefore = seededBefore
        self.status = status
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case forced
        case seededBefore
        case status
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(forced, forKey: .forced)
        try container.encodeIfPresent(seededBefore, forKey: .seededBefore)
        try container.encodeIfPresent(status, forKey: .status)
    }
}

