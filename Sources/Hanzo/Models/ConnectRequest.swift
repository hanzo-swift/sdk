//
// ConnectRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct ConnectRequest: Sendable, Codable, ParameterConvertible, Hashable {

    /** GithubLogin is the account to link. Used only when IAM holds no linked account for the provider — a linked account is stronger proof and always wins. */
    public var githubLogin: String?
    /** Login is the provider-neutral alias for GithubLogin, preferred when both are sent. */
    public var login: String?
    /** Provider is the forge to enrol with: github (the default) or gitlab. */
    public var provider: String?

    public init(githubLogin: String? = nil, login: String? = nil, provider: String? = nil) {
        self.githubLogin = githubLogin
        self.login = login
        self.provider = provider
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case githubLogin
        case login
        case provider
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(githubLogin, forKey: .githubLogin)
        try container.encodeIfPresent(login, forKey: .login)
        try container.encodeIfPresent(provider, forKey: .provider)
    }
}

