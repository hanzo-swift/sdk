//
// SummaryResp.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct SummaryResp: Sendable, Codable, ParameterConvertible, Hashable {

    /** Account and Hanzo report each ledger's own availability, so a partial warehouse never fabricates the other half. */
    public var account: SourceState?
    /** From and To are the one [from, to) window BOTH halves resolved, RFC 3339 UTC. */
    public var from: String?
    public var hanzo: SourceState?
    /** Range is the resolved period label. */
    public var range: String?
    /** Rows is the union of both ledgers, each row labelled by source and scope — concatenated, NEVER summed: a plan's percentage is not money. */
    public var rows: [TotalView]?
    public var to: String?

    public init(account: SourceState? = nil, from: String? = nil, hanzo: SourceState? = nil, range: String? = nil, rows: [TotalView]? = nil, to: String? = nil) {
        self.account = account
        self.from = from
        self.hanzo = hanzo
        self.range = range
        self.rows = rows
        self.to = to
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case account
        case from
        case hanzo
        case range
        case rows
        case to
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(account, forKey: .account)
        try container.encodeIfPresent(from, forKey: .from)
        try container.encodeIfPresent(hanzo, forKey: .hanzo)
        try container.encodeIfPresent(range, forKey: .range)
        try container.encodeIfPresent(rows, forKey: .rows)
        try container.encodeIfPresent(to, forKey: .to)
    }
}

