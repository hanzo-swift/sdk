//
// Assignment.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct Assignment: Sendable, Codable, ParameterConvertible, Hashable {

    /** Trial is the experiment that was evaluated. */
    public var experiment: String?
    /** On is false when the flag returned nothing for this subject, which means the subject is not enrolled — not an error. */
    public var on: Bool?
    public var payload: JSONValue?
    /** Subject is the unit that was bucketed. */
    public var subject: String?
    /** Arm is the arm the subject falls in, empty when the flag enrolled it in none. */
    public var variant: String?

    public init(experiment: String? = nil, on: Bool? = nil, payload: JSONValue? = nil, subject: String? = nil, variant: String? = nil) {
        self.experiment = experiment
        self.on = on
        self.payload = payload
        self.subject = subject
        self.variant = variant
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case experiment
        case on
        case payload
        case subject
        case variant
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(experiment, forKey: .experiment)
        try container.encodeIfPresent(on, forKey: .on)
        try container.encodeIfPresent(payload, forKey: .payload)
        try container.encodeIfPresent(subject, forKey: .subject)
        try container.encodeIfPresent(variant, forKey: .variant)
    }
}

