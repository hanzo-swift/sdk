//
// Claim.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct Claim: Sendable, Codable, ParameterConvertible, Hashable {

    /** Created reports whether this call recorded a new claim (201) or found an existing one (200). */
    public var created: Bool?
    /** Org is the verified owner-wide claim, present when an owner was claimed. It covers every repository the author publishes under that owner. */
    public var org: OrgView?
    /** Repo is the verified repository claim, present when a repository was claimed. */
    public var repo: AuthorRepo?

    public init(created: Bool? = nil, org: OrgView? = nil, repo: AuthorRepo? = nil) {
        self.created = created
        self.org = org
        self.repo = repo
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case created
        case org
        case repo
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(created, forKey: .created)
        try container.encodeIfPresent(org, forKey: .org)
        try container.encodeIfPresent(repo, forKey: .repo)
    }
}

