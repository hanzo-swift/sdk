//
// CollabResult.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct CollabResult: Sendable, Codable, ParameterConvertible, Hashable {

    /** Content maps each document field to its value for the verb: the new blob ref after a createContent, the stored markup after a getContent. */
    public var content: [String: String]?
    /** Error carries a SEMANTIC refusal, which this RPC reports under 200 because the client throws on result.error — auth and tenancy failures are HTTP statuses instead. */
    public var error: String?

    public init(content: [String: String]? = nil, error: String? = nil) {
        self.content = content
        self.error = error
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case content
        case error
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(content, forKey: .content)
        try container.encodeIfPresent(error, forKey: .error)
    }
}

