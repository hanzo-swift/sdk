//
// PipelineRow.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct PipelineRow: Sendable, Codable, ParameterConvertible, Hashable {

    /** Duration is how long that run took; empty while it is still queued or building. */
    public var duration: String?
    /** ID is the application id — one pipeline is one application. */
    public var id: String?
    /** LastRun is when the most recent deployment started, RFC3339 UTC. */
    public var lastRun: String?
    /** Name is the application's name. */
    public var name: String?
    /** Repo is the git repo or image the pipeline builds from. */
    public var repo: String?
    /** Status is the latest deployment's status, or the app's when it has none. */
    public var status: String?

    public init(duration: String? = nil, id: String? = nil, lastRun: String? = nil, name: String? = nil, repo: String? = nil, status: String? = nil) {
        self.duration = duration
        self.id = id
        self.lastRun = lastRun
        self.name = name
        self.repo = repo
        self.status = status
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case duration
        case id
        case lastRun
        case name
        case repo
        case status
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(duration, forKey: .duration)
        try container.encodeIfPresent(id, forKey: .id)
        try container.encodeIfPresent(lastRun, forKey: .lastRun)
        try container.encodeIfPresent(name, forKey: .name)
        try container.encodeIfPresent(repo, forKey: .repo)
        try container.encodeIfPresent(status, forKey: .status)
    }
}


@available(iOS 13, tvOS 13, watchOS 6, macOS 10.15, *)
extension PipelineRow: Identifiable {}
