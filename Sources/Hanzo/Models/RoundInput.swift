//
// RoundInput.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct RoundInput: Sendable, Codable, ParameterConvertible, Hashable {

    /** Name is the round's name on the cap table, e.g. \"Seed\". Required. */
    public var name: String?
    /** PreMoneyValuation is the valuation the round prices off, before the new money. */
    public var preMoneyValuation: Double?
    /** PricePerShare is the per-share price of a priced round. */
    public var pricePerShare: Double?
    /** RoundType is PRICED, SAFE or CONVERTIBLE_NOTE. Defaults to PRICED. */
    public var roundType: String?
    /** ShareClassID is the cap table's share class the round issues into. */
    public var shareClassId: String?
    /** TargetAmount is the amount the round is raising, recorded verbatim on the canonical cap table's rounds.create contract. */
    public var targetAmount: Double?

    public init(name: String? = nil, preMoneyValuation: Double? = nil, pricePerShare: Double? = nil, roundType: String? = nil, shareClassId: String? = nil, targetAmount: Double? = nil) {
        self.name = name
        self.preMoneyValuation = preMoneyValuation
        self.pricePerShare = pricePerShare
        self.roundType = roundType
        self.shareClassId = shareClassId
        self.targetAmount = targetAmount
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case name
        case preMoneyValuation
        case pricePerShare
        case roundType
        case shareClassId
        case targetAmount
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(name, forKey: .name)
        try container.encodeIfPresent(preMoneyValuation, forKey: .preMoneyValuation)
        try container.encodeIfPresent(pricePerShare, forKey: .pricePerShare)
        try container.encodeIfPresent(roundType, forKey: .roundType)
        try container.encodeIfPresent(shareClassId, forKey: .shareClassId)
        try container.encodeIfPresent(targetAmount, forKey: .targetAmount)
    }
}

