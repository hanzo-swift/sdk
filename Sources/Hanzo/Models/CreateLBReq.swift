//
// CreateLBReq.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct CreateLBReq: Sendable, Codable, ParameterConvertible, Hashable {

    /** ForwardingRules are the listen→backend port mappings. Empty defaults to plain HTTP 80→80, the same default DigitalOcean's own console applies. */
    public var forwardingRules: [FwdRule]?
    /** Name is the FRIENDLY name, a DNS-safe slug of at most 40 characters. The physical DigitalOcean name is derived from it and the caller's org. */
    public var name: String?
    /** Region is the DigitalOcean region slug (nyc3, sfo3, …). Required. */
    public var region: String?
    /** Size is the DigitalOcean size slug. Empty takes DO's default. */
    public var size: String?
    /** Type is the DigitalOcean load-balancer type. Empty takes DO's default (REGIONAL). */
    public var type: String?

    public init(forwardingRules: [FwdRule]? = nil, name: String? = nil, region: String? = nil, size: String? = nil, type: String? = nil) {
        self.forwardingRules = forwardingRules
        self.name = name
        self.region = region
        self.size = size
        self.type = type
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case forwardingRules = "forwarding_rules"
        case name
        case region
        case size
        case type
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(forwardingRules, forKey: .forwardingRules)
        try container.encodeIfPresent(name, forKey: .name)
        try container.encodeIfPresent(region, forKey: .region)
        try container.encodeIfPresent(size, forKey: .size)
        try container.encodeIfPresent(type, forKey: .type)
    }
}

