//
// EvaluatorReq.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct EvaluatorReq: Sendable, Codable, ParameterConvertible, Hashable {

    /** Criteria is the written standard the judge applies; over 64 KiB is refused. */
    public var criteria: String?
    /** Model is the model that will do the grading. */
    public var model: String?
    /** Name is the judge's org-unique handle, matching ^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$. Re-posting a name edits that judge rather than adding a second one. */
    public var name: String
    /** ScoreName is the name the resulting scores are filed under. It defaults to the judge's own name and must match the same pattern. */
    public var scoreName: String?

    public init(criteria: String? = nil, model: String? = nil, name: String, scoreName: String? = nil) {
        self.criteria = criteria
        self.model = model
        self.name = name
        self.scoreName = scoreName
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case criteria
        case model
        case name
        case scoreName
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(criteria, forKey: .criteria)
        try container.encodeIfPresent(model, forKey: .model)
        try container.encode(name, forKey: .name)
        try container.encodeIfPresent(scoreName, forKey: .scoreName)
    }
}

