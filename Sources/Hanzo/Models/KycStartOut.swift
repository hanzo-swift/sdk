//
// KycStartOut.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct KycStartOut: Sendable, Codable, ParameterConvertible, Hashable {

    /** Formation is the org's incorporation record, with each founder's session reference and status recorded on it. */
    public var formation: Formation?
    /** Provider is the wired identity-verification provider's name. */
    public var provider: String?
    /** Sessions is one entry per founder, in the order the founders are recorded. */
    public var sessions: [KycSession]?

    public init(formation: Formation? = nil, provider: String? = nil, sessions: [KycSession]? = nil) {
        self.formation = formation
        self.provider = provider
        self.sessions = sessions
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case formation
        case provider
        case sessions
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(formation, forKey: .formation)
        try container.encodeIfPresent(provider, forKey: .provider)
        try container.encodeIfPresent(sessions, forKey: .sessions)
    }
}

