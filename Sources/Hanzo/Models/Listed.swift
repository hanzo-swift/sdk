//
// Listed.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct Listed: Sendable, Codable, ParameterConvertible, Hashable {

    /** Members counts the room, and never names anybody in it. */
    public var members: Int64?
    /** Name is what a person sees, without the sigil a client draws. */
    public var name: String?
    /** Org owns the room. It is also what a caller filters by to browse one org. */
    public var org: String?
    /** Room addresses it in the owning store — what a join is called with. */
    public var room: String?
    /** Space is where the room lives inside that org. */
    public var space: String?
    /** Topic is the room's one-line subject, empty when it has none. */
    public var topic: String?
    /** Updated is when this row was last written, unix seconds. */
    public var updated: Int64?

    public init(members: Int64? = nil, name: String? = nil, org: String? = nil, room: String? = nil, space: String? = nil, topic: String? = nil, updated: Int64? = nil) {
        self.members = members
        self.name = name
        self.org = org
        self.room = room
        self.space = space
        self.topic = topic
        self.updated = updated
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case members
        case name
        case org
        case room
        case space
        case topic
        case updated
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(members, forKey: .members)
        try container.encodeIfPresent(name, forKey: .name)
        try container.encodeIfPresent(org, forKey: .org)
        try container.encodeIfPresent(room, forKey: .room)
        try container.encodeIfPresent(space, forKey: .space)
        try container.encodeIfPresent(topic, forKey: .topic)
        try container.encodeIfPresent(updated, forKey: .updated)
    }
}

