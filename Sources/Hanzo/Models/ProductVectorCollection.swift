//
// ProductVectorCollection.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** A single Qdrant collection as decoded by the console&#39;s vector panel. */
public struct ProductVectorCollection: Codable, JSONEncodable, Hashable {

    /** Collection name. */
    public var name: String
    /** Number of points/vectors in the collection. */
    public var vectorCount: Int64
    /** Vector dimension. */
    public var dimension: Int64
    /** Distance metric (defaults to \"cosine\" when the upstream omits it). */
    public var distanceMetric: String
    /** Storage size in bytes (omitted when zero). */
    public var storageBytes: Int64?
    /** Created timestamp (RFC 3339). */
    public var createdAt: String

    public init(name: String, vectorCount: Int64, dimension: Int64, distanceMetric: String, storageBytes: Int64? = nil, createdAt: String) {
        self.name = name
        self.vectorCount = vectorCount
        self.dimension = dimension
        self.distanceMetric = distanceMetric
        self.storageBytes = storageBytes
        self.createdAt = createdAt
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case name
        case vectorCount
        case dimension
        case distanceMetric
        case storageBytes
        case createdAt
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(name, forKey: .name)
        try container.encode(vectorCount, forKey: .vectorCount)
        try container.encode(dimension, forKey: .dimension)
        try container.encode(distanceMetric, forKey: .distanceMetric)
        try container.encodeIfPresent(storageBytes, forKey: .storageBytes)
        try container.encode(createdAt, forKey: .createdAt)
    }
}

