//
// ItemList.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct ItemList: Sendable, Codable, ParameterConvertible, Hashable {

    /** Data is the examples of the one dataset named in the path, archived ones included, so the caller sees the whole set rather than what a run would use. */
    public var data: [ItemView]?

    public init(data: [ItemView]? = nil) {
        self.data = data
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case data
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(data, forKey: .data)
    }
}

