//
// GithubPagesEnableReq.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct GithubPagesEnableReq: Sendable, Codable, ParameterConvertible, Hashable {

    /** Branch is the legacy source branch; empty defaults to the repo's own default branch. Ignored when buildType is \"workflow\". */
    public var branch: String?
    /** BuildType selects the builder: \"workflow\" builds via GitHub Actions, anything else builds from the branch source above. */
    public var buildType: String?
    /** Path is the source directory within the branch: \"/\" (the default) or \"/docs\". GitHub allows no others. */
    public var path: String?
    /** Repo is the repository, from the :repo path segment. */
    public var repo: String?

    public init(branch: String? = nil, buildType: String? = nil, path: String? = nil, repo: String? = nil) {
        self.branch = branch
        self.buildType = buildType
        self.path = path
        self.repo = repo
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case branch
        case buildType
        case path
        case repo
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(branch, forKey: .branch)
        try container.encodeIfPresent(buildType, forKey: .buildType)
        try container.encodeIfPresent(path, forKey: .path)
        try container.encodeIfPresent(repo, forKey: .repo)
    }
}

