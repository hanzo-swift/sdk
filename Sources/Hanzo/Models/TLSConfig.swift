//
// TLSConfig.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct TLSConfig: Sendable, Codable, ParameterConvertible, Hashable {

    /** ACMEEmail is the ACME account email. It binds an account for the lifetime of an edge process, so it applies only when the edge (re)starts. */
    public var acmeEmail: String?
    /** ExtraHosts get certificates without owning a route — at most 256. They feed the ACME HostPolicy and hot-apply on the next reload. */
    public var extraHosts: [String]?
    /** Staging issues from Let's Encrypt's staging directory (untrusted certs, high rate limits). Like ACMEEmail it applies only when the edge (re)starts. */
    public var staging: Bool?

    public init(acmeEmail: String? = nil, extraHosts: [String]? = nil, staging: Bool? = nil) {
        self.acmeEmail = acmeEmail
        self.extraHosts = extraHosts
        self.staging = staging
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case acmeEmail
        case extraHosts
        case staging
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(acmeEmail, forKey: .acmeEmail)
        try container.encodeIfPresent(extraHosts, forKey: .extraHosts)
        try container.encodeIfPresent(staging, forKey: .staging)
    }
}

