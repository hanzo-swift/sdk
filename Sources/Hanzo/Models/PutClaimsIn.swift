//
// PutClaimsIn.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct PutClaimsIn: Sendable, Codable, ParameterConvertible, Hashable {

    /** By is who is recording them — a person, or the importer's name. */
    public var by: String?
    /** Data is the claims to record. One row is a correction; many is an import. There is no separate bulk endpoint because there is no separate operation: importing a leaderboard and fixing one number are the same write. */
    public var data: [PublishedClaim]?

    public init(by: String? = nil, data: [PublishedClaim]? = nil) {
        self.by = by
        self.data = data
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case by
        case data
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(by, forKey: .by)
        try container.encodeIfPresent(data, forKey: .data)
    }
}

