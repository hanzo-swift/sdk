//
// SeedData.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct SeedData: Sendable, Codable, ParameterConvertible, Hashable {

    /** Created is false when this ref had already been seeded — the injection is at-most-once. */
    public var created: Bool?
    /** Entry is the journal entry the injection wrote. */
    public var entry: JournalEntry?
    /** ReserveCents is the fund balance after the injection. */
    public var reserveCents: Int?

    public init(created: Bool? = nil, entry: JournalEntry? = nil, reserveCents: Int? = nil) {
        self.created = created
        self.entry = entry
        self.reserveCents = reserveCents
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case created
        case entry
        case reserveCents
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(created, forKey: .created)
        try container.encodeIfPresent(entry, forKey: .entry)
        try container.encodeIfPresent(reserveCents, forKey: .reserveCents)
    }
}

