//
// SearchResults.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct SearchResults: Sendable, Codable, ParameterConvertible, Hashable {

    /** Degraded is true when retrieval failed and the empty result set is an outage rather than a real absence of matches. Absent on a healthy answer. */
    public var degraded: Bool?
    /** Query echoes the query that was run. */
    public var query: String?
    /** Results are the matching spans, best first. Never null — an empty search is an empty array. */
    public var results: [Span]?
    /** Type echoes the retrieval tier that ran, after defaulting. */
    public var type: String?

    public init(degraded: Bool? = nil, query: String? = nil, results: [Span]? = nil, type: String? = nil) {
        self.degraded = degraded
        self.query = query
        self.results = results
        self.type = type
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case degraded
        case query
        case results
        case type
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(degraded, forKey: .degraded)
        try container.encodeIfPresent(query, forKey: .query)
        try container.encodeIfPresent(results, forKey: .results)
        try container.encodeIfPresent(type, forKey: .type)
    }
}

