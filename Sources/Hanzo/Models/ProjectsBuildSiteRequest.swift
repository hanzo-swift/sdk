//
// ProjectsBuildSiteRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Body for POST /v1/sites — build a site from a brief. */
public struct ProjectsBuildSiteRequest: Codable, JSONEncodable, Hashable {

    public static let briefRule = StringRule(minLength: nil, maxLength: 8192, pattern: nil)
    public static let slugRule = StringRule(minLength: nil, maxLength: nil, pattern: "/^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$/")
    /** Natural-language description of the site to generate (required; capped at 8 KiB). */
    public var brief: String
    /** Target project slug; derived from name (or minted \"site-<token>\") when omitted. */
    public var slug: String?
    /** Display name; defaults to the generated site name, else \"Site\". */
    public var name: String?
    /** Inference model override; empty selects the gateway default. */
    public var model: String?

    public init(brief: String, slug: String? = nil, name: String? = nil, model: String? = nil) {
        self.brief = brief
        self.slug = slug
        self.name = name
        self.model = model
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case brief
        case slug
        case name
        case model
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(brief, forKey: .brief)
        try container.encodeIfPresent(slug, forKey: .slug)
        try container.encodeIfPresent(name, forKey: .name)
        try container.encodeIfPresent(model, forKey: .model)
    }
}

