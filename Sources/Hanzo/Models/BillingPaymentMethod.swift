//
// BillingPaymentMethod.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Masked card descriptor — brand + last4 + expiry only. */
public struct BillingPaymentMethod: Codable, JSONEncodable, Hashable {

    public var brand: String?
    public var last4: String?
    public var expMonth: Int?
    public var expYear: Int?

    public init(brand: String? = nil, last4: String? = nil, expMonth: Int? = nil, expYear: Int? = nil) {
        self.brand = brand
        self.last4 = last4
        self.expMonth = expMonth
        self.expYear = expYear
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case brand
        case last4
        case expMonth
        case expYear
    }

    public var additionalProperties: [String: AnyCodable] = [:]

    public subscript(key: String) -> AnyCodable? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            additionalProperties[key] = newValue
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(brand, forKey: .brand)
        try container.encodeIfPresent(last4, forKey: .last4)
        try container.encodeIfPresent(expMonth, forKey: .expMonth)
        try container.encodeIfPresent(expYear, forKey: .expYear)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(additionalProperties)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        brand = try container.decodeIfPresent(String.self, forKey: .brand)
        last4 = try container.decodeIfPresent(String.self, forKey: .last4)
        expMonth = try container.decodeIfPresent(Int.self, forKey: .expMonth)
        expYear = try container.decodeIfPresent(Int.self, forKey: .expYear)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("brand")
        nonAdditionalPropertyKeys.insert("last4")
        nonAdditionalPropertyKeys.insert("expMonth")
        nonAdditionalPropertyKeys.insert("expYear")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        additionalProperties = try additionalPropertiesContainer.decodeMap(AnyCodable.self, excludedKeys: nonAdditionalPropertyKeys)
    }
}

