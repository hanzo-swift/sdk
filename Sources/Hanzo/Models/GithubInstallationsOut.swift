//
// GithubInstallationsOut.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct GithubInstallationsOut: Sendable, Codable, ParameterConvertible, Hashable {

    /** InstallURL is where to grant a new account, so a UI with an empty list has somewhere to send the reader instead of a dead end. */
    public var installUrl: String?
    /** Installations is every account the caller may see: the ones its own org has bound, or — for a super admin — every account the App is installed on. Never null; [] when none. */
    public var installations: [GithubInstallationView]?

    public init(installUrl: String? = nil, installations: [GithubInstallationView]? = nil) {
        self.installUrl = installUrl
        self.installations = installations
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case installUrl
        case installations
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(installUrl, forKey: .installUrl)
        try container.encodeIfPresent(installations, forKey: .installations)
    }
}

