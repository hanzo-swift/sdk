//
// SweepRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct SweepRequest: Sendable, Codable, ParameterConvertible, Hashable {

    /** Period is the accrual period as YYYY-MM. Empty takes the current UTC month. */
    public var period: String?
    /** RevenueCents is the net platform revenue measured for the period, in minor units. Must be >= 0. */
    public var revenueCents: Int?

    public init(period: String? = nil, revenueCents: Int? = nil) {
        self.period = period
        self.revenueCents = revenueCents
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case period
        case revenueCents
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(period, forKey: .period)
        try container.encodeIfPresent(revenueCents, forKey: .revenueCents)
    }
}

