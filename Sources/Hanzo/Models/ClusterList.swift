//
// ClusterList.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct ClusterList: Sendable, Codable, ParameterConvertible, Hashable {

    /** Clusters is the merged fleet — kind \"managed\" for Visor-provisioned, \"byo\" for an attached kubeconfig. */
    public var clusters: [ClusterView]?
    /** Degraded names any source that did not answer, so an empty Clusters means \"you have none\" only when this is absent. Omitted when everything answered, so a healthy response is unchanged. See degraded.go. */
    public var degraded: [SourceFailure]?

    public init(clusters: [ClusterView]? = nil, degraded: [SourceFailure]? = nil) {
        self.clusters = clusters
        self.degraded = degraded
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case clusters
        case degraded
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(clusters, forKey: .clusters)
        try container.encodeIfPresent(degraded, forKey: .degraded)
    }
}

