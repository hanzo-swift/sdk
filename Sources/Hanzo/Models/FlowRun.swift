//
// FlowRun.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct FlowRun: Sendable, Codable, ParameterConvertible, Hashable {

    /** Input is the run's chat input value, handed to the graph's input node. */
    public var input: String?
    /** Session groups runs into one conversation; the product mints one when absent and returns it in the response. */
    public var session: String?
    public var tweaks: JSONValue?
    /** Workflow is the UUID of the workflow to run. */
    public var workflow: String?

    public init(input: String? = nil, session: String? = nil, tweaks: JSONValue? = nil, workflow: String? = nil) {
        self.input = input
        self.session = session
        self.tweaks = tweaks
        self.workflow = workflow
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case input
        case session
        case tweaks
        case workflow
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(input, forKey: .input)
        try container.encodeIfPresent(session, forKey: .session)
        try container.encodeIfPresent(tweaks, forKey: .tweaks)
        try container.encodeIfPresent(workflow, forKey: .workflow)
    }
}

