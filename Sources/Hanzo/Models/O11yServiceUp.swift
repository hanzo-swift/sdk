//
// O11yServiceUp.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct O11yServiceUp: Sendable, Codable, ParameterConvertible, Hashable {

    /** Name is the service as the fleet prober knows it (probes.go's target name, which is the `service` label on hanzo_service_up). */
    public var name: String?
    /** Up is true when the service answered its own health URL on the last cycle. */
    public var up: Bool?

    public init(name: String? = nil, up: Bool? = nil) {
        self.name = name
        self.up = up
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case name
        case up
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(name, forKey: .name)
        try container.encodeIfPresent(up, forKey: .up)
    }
}

