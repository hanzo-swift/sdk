//
// Health.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct Health: Sendable, Codable, ParameterConvertible, Hashable {

    /** OK is true whenever this route answers at all: reaching the handler IS the proof that the routes are registered and dispatching. */
    public var ok: Bool?
    /** Subsystem names what answered, so a health response read out of context still says which surface it came from. */
    public var subsystem: String?

    public init(ok: Bool? = nil, subsystem: String? = nil) {
        self.ok = ok
        self.subsystem = subsystem
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case ok
        case subsystem
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(ok, forKey: .ok)
        try container.encodeIfPresent(subsystem, forKey: .subsystem)
    }
}

