//
// FetchQuery.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct FetchQuery: Sendable, Codable, ParameterConvertible, Hashable {

    /** Batch is the most messages to return. 0 or less means 1; anything above 100 is clamped to 100. */
    public var batch: Int?
    /** Name is the consumer, from the path. */
    public var name: String?
    /** Stream is the stream, from the path. */
    public var stream: String?
    /** WaitMs is how long to wait for the batch to fill before answering with what arrived. 0 or less means the default of 5000; clamped to 30000. */
    public var waitMs: Int?

    public init(batch: Int? = nil, name: String? = nil, stream: String? = nil, waitMs: Int? = nil) {
        self.batch = batch
        self.name = name
        self.stream = stream
        self.waitMs = waitMs
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case batch
        case name
        case stream
        case waitMs
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(batch, forKey: .batch)
        try container.encodeIfPresent(name, forKey: .name)
        try container.encodeIfPresent(stream, forKey: .stream)
        try container.encodeIfPresent(waitMs, forKey: .waitMs)
    }
}

