//
// RiskBand.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct RiskBand: Sendable, Codable, ParameterConvertible, Hashable {

    /** Day is the day the band covers. */
    public var day: Date?
    /** Dim is the dimension, named as this API publishes it. */
    public var dim: String?
    /** Kind is the subject kind it was computed over. */
    public var kind: String?
    /** N is how many subject-days went into it. */
    public var n: Int?
    /** Orgs is how many organisations contributed, each weighted exactly one vote whatever its size. It is published so a reader can judge the band rather than trust it. */
    public var orgs: Int?
    /** Q10 is the quiet end of the network's day: a tenth of contributing organisations sit at or below it. */
    public var q10: Double?
    /** Q50 is the network's median day. */
    public var q50: Double?
    /** Q90 is the busy end: a tenth of contributing organisations sit at or above it. It is the highest level published. */
    public var q90: Double?

    public init(day: Date? = nil, dim: String? = nil, kind: String? = nil, n: Int? = nil, orgs: Int? = nil, q10: Double? = nil, q50: Double? = nil, q90: Double? = nil) {
        self.day = day
        self.dim = dim
        self.kind = kind
        self.n = n
        self.orgs = orgs
        self.q10 = q10
        self.q50 = q50
        self.q90 = q90
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case day
        case dim
        case kind
        case n
        case orgs
        case q10
        case q50
        case q90
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(day, forKey: .day)
        try container.encodeIfPresent(dim, forKey: .dim)
        try container.encodeIfPresent(kind, forKey: .kind)
        try container.encodeIfPresent(n, forKey: .n)
        try container.encodeIfPresent(orgs, forKey: .orgs)
        try container.encodeIfPresent(q10, forKey: .q10)
        try container.encodeIfPresent(q50, forKey: .q50)
        try container.encodeIfPresent(q90, forKey: .q90)
    }
}

