//
// MqKVBucketConfig.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** KV bucket creation configuration. */
public struct MqKVBucketConfig: Codable, JSONEncodable, Hashable {

    public enum Storage: String, Codable, CaseIterable {
        case file = "file"
        case memory = "memory"
    }
    public static let nameRule = StringRule(minLength: nil, maxLength: 256, pattern: "/^[a-zA-Z0-9_-]+$/")
    public static let historyRule = NumericRule<Int>(minimum: 1, exclusiveMinimum: false, maximum: 64, exclusiveMaximum: false, multipleOf: nil)
    public static let numReplicasRule = NumericRule<Int>(minimum: 1, exclusiveMinimum: false, maximum: 5, exclusiveMaximum: false, multipleOf: nil)
    /** Bucket name. */
    public var name: String
    /** Maximum number of revisions per key. */
    public var history: Int? = 1
    /** Default TTL for keys (e.g., \"1h\", \"7d\", \"0\" for no expiry).  */
    public var ttl: String? = "0"
    /** Maximum value size in bytes (-1 for default). */
    public var maxValueSize: Int? = -1
    /** Maximum total bucket size (-1 for unlimited). */
    public var maxBytes: Int64? = -1
    /** Storage backend. */
    public var storage: Storage? = .file
    /** Number of replicas. */
    public var numReplicas: Int? = 1
    /** Optional human-readable description. */
    public var description: String?

    public init(name: String, history: Int? = 1, ttl: String? = "0", maxValueSize: Int? = -1, maxBytes: Int64? = -1, storage: Storage? = .file, numReplicas: Int? = 1, description: String? = nil) {
        self.name = name
        self.history = history
        self.ttl = ttl
        self.maxValueSize = maxValueSize
        self.maxBytes = maxBytes
        self.storage = storage
        self.numReplicas = numReplicas
        self.description = description
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case name
        case history
        case ttl
        case maxValueSize = "max_value_size"
        case maxBytes = "max_bytes"
        case storage
        case numReplicas = "num_replicas"
        case description
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(name, forKey: .name)
        try container.encodeIfPresent(history, forKey: .history)
        try container.encodeIfPresent(ttl, forKey: .ttl)
        try container.encodeIfPresent(maxValueSize, forKey: .maxValueSize)
        try container.encodeIfPresent(maxBytes, forKey: .maxBytes)
        try container.encodeIfPresent(storage, forKey: .storage)
        try container.encodeIfPresent(numReplicas, forKey: .numReplicas)
        try container.encodeIfPresent(description, forKey: .description)
    }
}

