//
// LimitsView.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct LimitsView: Sendable, Codable, ParameterConvertible, Hashable {

    /** Limits is the plan's decision. */
    public var limits: LimitsBlock?
    /** Plan echoes the plan id the limits were resolved for, after the empty-means- world-free default. */
    public var plan: String?
    /** Unit names what the two rate numbers are counted in: requests/minute. */
    public var unit: String?

    public init(limits: LimitsBlock? = nil, plan: String? = nil, unit: String? = nil) {
        self.limits = limits
        self.plan = plan
        self.unit = unit
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case limits
        case plan
        case unit
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(limits, forKey: .limits)
        try container.encodeIfPresent(plan, forKey: .plan)
        try container.encodeIfPresent(unit, forKey: .unit)
    }
}

