//
// ReindexOut.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct ReindexOut: Sendable, Codable, ParameterConvertible, Hashable {

    /** Failed is how many documents could not be embedded; each is logged with its name, and the rest of the rebuild went on without it. */
    public var failed: Int64?
    /** Lexical is how many rows the org's lexical index holds now; 0 in a deployment without the index app. */
    public var lexical: Int64?
    /** Removed is how many rows the lexical index held for documents that no longer exist; 0 without the index app. */
    public var removed: Int64?
    /** Vectors is how many documents were embedded and written to the org's collection, which was dropped and created again at the configured size. */
    public var vectors: Int64?

    public init(failed: Int64? = nil, lexical: Int64? = nil, removed: Int64? = nil, vectors: Int64? = nil) {
        self.failed = failed
        self.lexical = lexical
        self.removed = removed
        self.vectors = vectors
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case failed
        case lexical
        case removed
        case vectors
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(failed, forKey: .failed)
        try container.encodeIfPresent(lexical, forKey: .lexical)
        try container.encodeIfPresent(removed, forKey: .removed)
        try container.encodeIfPresent(vectors, forKey: .vectors)
    }
}

