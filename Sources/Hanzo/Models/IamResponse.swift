//
// IamResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct IamResponse: Sendable, Codable, ParameterConvertible, Hashable {

    /** Code is a STABLE machine-readable reason, where the human `msg` is deliberately generic. `msg` is prose for a person and several distinct causes legitimately share one sentence; a caller that must BRANCH on the cause — or tell its own user which of them happened — cannot parse prose. Optional, so every existing envelope is byte-identical and no SDK changes. */
    public var code: String?
    public var data: JSONValue?
    public var data2: JSONValue?
    public var data3: JSONValue?
    public var msg: String?
    public var name: String?
    public var status: String?
    public var sub: String?

    public init(code: String? = nil, data: JSONValue? = nil, data2: JSONValue? = nil, data3: JSONValue? = nil, msg: String? = nil, name: String? = nil, status: String? = nil, sub: String? = nil) {
        self.code = code
        self.data = data
        self.data2 = data2
        self.data3 = data3
        self.msg = msg
        self.name = name
        self.status = status
        self.sub = sub
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case code
        case data
        case data2
        case data3
        case msg
        case name
        case status
        case sub
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(code, forKey: .code)
        try container.encodeIfPresent(data, forKey: .data)
        try container.encodeIfPresent(data2, forKey: .data2)
        try container.encodeIfPresent(data3, forKey: .data3)
        try container.encodeIfPresent(msg, forKey: .msg)
        try container.encodeIfPresent(name, forKey: .name)
        try container.encodeIfPresent(status, forKey: .status)
        try container.encodeIfPresent(sub, forKey: .sub)
    }
}

