//
// Envelope.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** The resource surface&#39;s response. &#x60;status&#x60; is the verdict, not the HTTP code: a handled failure is still 200. */
public struct Envelope: Sendable, Codable, ParameterConvertible, Hashable {

    public enum Status: String, Sendable, Codable, CaseIterable {
        case ok = "ok"
        case error = "error"
    }
    public var data: JSONValue?
    public var data2: JSONValue?
    /** Empty on success, the reason on failure. */
    public var msg: String
    public var status: Status

    public init(data: JSONValue? = nil, data2: JSONValue? = nil, msg: String, status: Status) {
        self.data = data
        self.data2 = data2
        self.msg = msg
        self.status = status
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case data
        case data2
        case msg
        case status
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(data, forKey: .data)
        try container.encodeIfPresent(data2, forKey: .data2)
        try container.encode(msg, forKey: .msg)
        try container.encode(status, forKey: .status)
    }
}

