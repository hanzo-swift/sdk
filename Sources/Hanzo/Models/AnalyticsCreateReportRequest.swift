//
// AnalyticsCreateReportRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct AnalyticsCreateReportRequest: Codable, JSONEncodable, Hashable {

    public enum ModelType: String, Codable, CaseIterable {
        case funnel = "funnel"
        case insights = "insights"
        case retention = "retention"
        case utm = "utm"
        case goals = "goals"
        case journey = "journey"
        case revenue = "revenue"
        case attribution = "attribution"
    }
    public static let nameRule = StringRule(minLength: nil, maxLength: 200, pattern: nil)
    public static let descriptionRule = StringRule(minLength: nil, maxLength: 500, pattern: nil)
    public var websiteId: UUID
    public var name: String
    public var type: ModelType
    public var description: String
    public var parameters: AnyCodable

    public init(websiteId: UUID, name: String, type: ModelType, description: String, parameters: AnyCodable) {
        self.websiteId = websiteId
        self.name = name
        self.type = type
        self.description = description
        self.parameters = parameters
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case websiteId
        case name
        case type
        case description
        case parameters
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(websiteId, forKey: .websiteId)
        try container.encode(name, forKey: .name)
        try container.encode(type, forKey: .type)
        try container.encode(description, forKey: .description)
        try container.encode(parameters, forKey: .parameters)
    }
}

