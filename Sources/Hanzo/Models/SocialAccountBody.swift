//
// SocialAccountBody.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct SocialAccountBody: Sendable, Codable, ParameterConvertible, Hashable {

    /** Handle is the account's public name on the network, as the customer knows it (`@acme`). Trimmed and bounded at 1024 characters.  Example: \"@acme\" */
    public var handle: String?
    /** Provider is the network this account is on: x, facebook, instagram, linkedin, tiktok, youtube or threads. Omitted means x. Anything else is refused rather than coerced, because a stored account on a network that does not exist can never be published through.  Example: \"x\" */
    public var provider: String?
    /** Status is the connection lifecycle: connected, disconnected or error. Omitted means connected. Only a connected account is a publish target. */
    public var status: String?

    public init(handle: String? = nil, provider: String? = nil, status: String? = nil) {
        self.handle = handle
        self.provider = provider
        self.status = status
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case handle
        case provider
        case status
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(handle, forKey: .handle)
        try container.encodeIfPresent(provider, forKey: .provider)
        try container.encodeIfPresent(status, forKey: .status)
    }
}

