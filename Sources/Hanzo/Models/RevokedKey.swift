//
// RevokedKey.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct RevokedKey: Sendable, Codable, ParameterConvertible, Hashable {

    /** OK is true when the key was revoked. A failure is an error status, never a false here. */
    public var ok: Bool?
    /** Type is the key class that was revoked, resolved — so a caller that named nothing can see it revoked the secret key. */
    public var type: String?

    public init(ok: Bool? = nil, type: String? = nil) {
        self.ok = ok
        self.type = type
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case ok
        case type
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(ok, forKey: .ok)
        try container.encodeIfPresent(type, forKey: .type)
    }
}

