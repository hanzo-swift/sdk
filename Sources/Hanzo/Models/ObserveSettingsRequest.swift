//
// ObserveSettingsRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct ObserveSettingsRequest: Codable, JSONEncodable, Hashable {

    /** Non-secret config; stored verbatim (bounded at 64KiB). */
    public var config: [String: AnyCodable]?
    /** Secret fields; VALUES routed to KMS, never SQLite. Each key must match `^[a-z0-9][a-z0-9._-]{0,62}$`; each value is bounded at 8KiB. An empty value or the mask sentinel (`••••••••`) means \"unchanged\". Max 64 fields.  */
    public var secrets: [String: String]?

    public init(config: [String: AnyCodable]? = nil, secrets: [String: String]? = nil) {
        self.config = config
        self.secrets = secrets
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case config
        case secrets
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(config, forKey: .config)
        try container.encodeIfPresent(secrets, forKey: .secrets)
    }
}

