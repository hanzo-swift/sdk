//
// DirectoryData.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct DirectoryData: Sendable, Codable, ParameterConvertible, Hashable {

    /** Affiliates is one row per affiliate across the whole fleet, ORG EXPOSED, oldest first and bounded by the request's limit. */
    public var affiliates: [AdminAffiliateView]?
    /** Summary tallies exactly the rows above — not the whole table — so a limit that truncates the page truncates the tally with it. */
    public var summary: Totals?

    public init(affiliates: [AdminAffiliateView]? = nil, summary: Totals? = nil) {
        self.affiliates = affiliates
        self.summary = summary
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case affiliates
        case summary
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(affiliates, forKey: .affiliates)
        try container.encodeIfPresent(summary, forKey: .summary)
    }
}

