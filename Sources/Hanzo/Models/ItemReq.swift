//
// ItemReq.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct ItemReq: Sendable, Codable, ParameterConvertible, Hashable {

    public var expectedOutput: JSONValue?
    /** ID makes the write idempotent — re-posting the same id replaces that example in place. Omit it and one is generated. An id that already exists in a DIFFERENT dataset is 409 rather than a move. */
    public var id: String?
    public var input: JSONValue?
    /** Metadata is a free-form object stored with the example. */
    public var metadata: [String: JSONValue]?
    /** Status is ACTIVE (the default) or ARCHIVED. Only ACTIVE examples are fed to a run, which is how an example is retired without being deleted. */
    public var status: String?

    public init(expectedOutput: JSONValue? = nil, id: String? = nil, input: JSONValue? = nil, metadata: [String: JSONValue]? = nil, status: String? = nil) {
        self.expectedOutput = expectedOutput
        self.id = id
        self.input = input
        self.metadata = metadata
        self.status = status
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case expectedOutput
        case id
        case input
        case metadata
        case status
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(expectedOutput, forKey: .expectedOutput)
        try container.encodeIfPresent(id, forKey: .id)
        try container.encodeIfPresent(input, forKey: .input)
        try container.encodeIfPresent(metadata, forKey: .metadata)
        try container.encodeIfPresent(status, forKey: .status)
    }
}


@available(iOS 13, tvOS 13, watchOS 6, macOS 10.15, *)
extension ItemReq: Identifiable {}
