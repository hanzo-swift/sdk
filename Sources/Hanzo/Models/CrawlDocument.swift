//
// CrawlDocument.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct CrawlDocument: Sendable, Codable, ParameterConvertible, Hashable {

    /** Markdown is the page's content, extracted and rendered to markdown. This is the field to read. */
    public var markdown: String?
    public var metadata: JSONValue?
    /** Title is the document's title, when it carried one. */
    public var title: String?
    /** URL is the address actually read, after redirects. */
    public var url: String?

    public init(markdown: String? = nil, metadata: JSONValue? = nil, title: String? = nil, url: String? = nil) {
        self.markdown = markdown
        self.metadata = metadata
        self.title = title
        self.url = url
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case markdown
        case metadata
        case title
        case url
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(markdown, forKey: .markdown)
        try container.encodeIfPresent(metadata, forKey: .metadata)
        try container.encodeIfPresent(title, forKey: .title)
        try container.encodeIfPresent(url, forKey: .url)
    }
}

