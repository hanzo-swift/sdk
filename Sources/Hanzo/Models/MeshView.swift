//
// MeshView.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct MeshView: Sendable, Codable, ParameterConvertible, Hashable {

    /** ID is the ZT edge service's id. */
    public var id: String?
    /** Mtls is \"required\" when the service mandates end-to-end encryption, else \"enabled\" — the fabric mutually authenticates every link, so it is never truly off. */
    public var mtls: String?
    /** Service is the edge service's name. */
    public var service: String?
    /** Status is \"active\": a listed service is a configured, dialable mesh entry. */
    public var status: String?

    public init(id: String? = nil, mtls: String? = nil, service: String? = nil, status: String? = nil) {
        self.id = id
        self.mtls = mtls
        self.service = service
        self.status = status
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case id
        case mtls
        case service
        case status
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(id, forKey: .id)
        try container.encodeIfPresent(mtls, forKey: .mtls)
        try container.encodeIfPresent(service, forKey: .service)
        try container.encodeIfPresent(status, forKey: .status)
    }
}


@available(iOS 13, tvOS 13, watchOS 6, macOS 10.15, *)
extension MeshView: Identifiable {}
