//
// GrantResult.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct GrantResult: Sendable, Codable, ParameterConvertible, Hashable {

    /** BalanceCents is the account balance AFTER the grant, in whole cents. */
    public var balanceCents: Int?
    /** BalanceExact is that same balance at full 18-decimal precision, so a sub-cent debit is visible rather than rounded away. */
    public var balanceExact: String?
    /** Currency is the lower-cased ISO code the grant was denominated in. */
    public var currency: String?
    /** GrantedCents is the amount actually credited. */
    public var grantedCents: Int?
    /** Org is the tenant whose ledger was credited. */
    public var org: String?
    /** Source is the money bucket: \"trial\" (non-cash comp) or \"prepaid\" (real money). */
    public var source: String?
    /** Subject is the ACCOUNT the credit landed on inside that ledger: the org slug for a pooled org, \"<org>/<name>\" for a member of a per-member one. It is echoed because the operator does not choose it — account.Payer does — so naming a member of a pooled org credits the pool and the receipt has to say so. */
    public var subject: String?
    /** TransactionID is the ledger entry id, for reconciliation against commerce. */
    public var transactionId: String?

    public init(balanceCents: Int? = nil, balanceExact: String? = nil, currency: String? = nil, grantedCents: Int? = nil, org: String? = nil, source: String? = nil, subject: String? = nil, transactionId: String? = nil) {
        self.balanceCents = balanceCents
        self.balanceExact = balanceExact
        self.currency = currency
        self.grantedCents = grantedCents
        self.org = org
        self.source = source
        self.subject = subject
        self.transactionId = transactionId
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case balanceCents
        case balanceExact
        case currency
        case grantedCents
        case org
        case source
        case subject
        case transactionId
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(balanceCents, forKey: .balanceCents)
        try container.encodeIfPresent(balanceExact, forKey: .balanceExact)
        try container.encodeIfPresent(currency, forKey: .currency)
        try container.encodeIfPresent(grantedCents, forKey: .grantedCents)
        try container.encodeIfPresent(org, forKey: .org)
        try container.encodeIfPresent(source, forKey: .source)
        try container.encodeIfPresent(subject, forKey: .subject)
        try container.encodeIfPresent(transactionId, forKey: .transactionId)
    }
}

