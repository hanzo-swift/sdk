//
// DeployReq.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct DeployReq: Sendable, Codable, ParameterConvertible, Hashable {

    /** App is the application's slug, from the path. */
    public var app: String?
    /** Commit is the git commit or ref to build, for a git-source app. Defaults to the app's branch. */
    public var commit: String?
    /** Project is the project the application lives under, from the path. */
    public var project: String?
    /** Tag is the image tag to deploy, for an image-source app. Defaults to the app's tag, then `latest`. */
    public var tag: String?

    public init(app: String? = nil, commit: String? = nil, project: String? = nil, tag: String? = nil) {
        self.app = app
        self.commit = commit
        self.project = project
        self.tag = tag
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case app
        case commit
        case project
        case tag
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(app, forKey: .app)
        try container.encodeIfPresent(commit, forKey: .commit)
        try container.encodeIfPresent(project, forKey: .project)
        try container.encodeIfPresent(tag, forKey: .tag)
    }
}

