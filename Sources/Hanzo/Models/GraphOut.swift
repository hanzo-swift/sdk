//
// GraphOut.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct GraphOut: Sendable, Codable, ParameterConvertible, Hashable {

    /** Degraded is true when the store was unreachable and this graph is honestly empty rather than wrong. Absent on a normal answer. */
    public var degraded: Bool?
    /** Edges are the parent tree, the resolved wikilinks and the connector provenance. */
    public var edges: [GraphEdge]?
    /** Nodes are the pages, memories, sources, connectors and unresolved link targets. */
    public var nodes: [GraphNode]?

    public init(degraded: Bool? = nil, edges: [GraphEdge]? = nil, nodes: [GraphNode]? = nil) {
        self.degraded = degraded
        self.edges = edges
        self.nodes = nodes
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case degraded
        case edges
        case nodes
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(degraded, forKey: .degraded)
        try container.encodeIfPresent(edges, forKey: .edges)
        try container.encodeIfPresent(nodes, forKey: .nodes)
    }
}

