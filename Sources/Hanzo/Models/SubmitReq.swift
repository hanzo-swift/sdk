//
// SubmitReq.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct SubmitReq: Sendable, Codable, ParameterConvertible, Hashable {

    /** Files is the batch to scan, at most 500 files and 8 MiB of content in total. */
    public var files: [Scan]
    /** Project names the sub-scope the scan is filed under. It must be a slug; omit it and the caller's project header is used instead. */
    public var project: String?

    public init(files: [Scan], project: String? = nil) {
        self.files = files
        self.project = project
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case files
        case project
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(files, forKey: .files)
        try container.encodeIfPresent(project, forKey: .project)
    }
}

