//
// AimActorStat.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct AimActorStat: Sendable, Codable, ParameterConvertible, Hashable {

    /** Actor is the principal the ledger recorded, \"<org>/<sub>\". A value naming an IAM APPLICATION rather than a person means that spend has no human owner — which is the row to read first. */
    public var actor: String?
    /** CostCents is what they cost, in US cents, as the ledger recorded it. */
    public var costCents: Int?
    /** Requests is how many calls that principal made in the window. */
    public var requests: Int?
    /** Tokens is the total tokens those calls consumed. */
    public var tokens: Int?

    public init(actor: String? = nil, costCents: Int? = nil, requests: Int? = nil, tokens: Int? = nil) {
        self.actor = actor
        self.costCents = costCents
        self.requests = requests
        self.tokens = tokens
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case actor
        case costCents
        case requests
        case tokens
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(actor, forKey: .actor)
        try container.encodeIfPresent(costCents, forKey: .costCents)
        try container.encodeIfPresent(requests, forKey: .requests)
        try container.encodeIfPresent(tokens, forKey: .tokens)
    }
}

