//
// IndexVersion.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct IndexVersion: Sendable, Codable, ParameterConvertible, Hashable {

    /** CommitDate is empty here: this surface is a dialect implementation, not a build of Meilisearch, so there is no upstream commit to date. */
    public var commitDate: String?
    /** CommitSha names the implementation (`hanzo-cloud`) rather than a build hash, so a client logging it records which server answered instead of implying a Meilisearch release. */
    public var commitSha: String?
    /** PkgVersion is this dialect implementation's own version. */
    public var pkgVersion: String?

    public init(commitDate: String? = nil, commitSha: String? = nil, pkgVersion: String? = nil) {
        self.commitDate = commitDate
        self.commitSha = commitSha
        self.pkgVersion = pkgVersion
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case commitDate
        case commitSha
        case pkgVersion
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(commitDate, forKey: .commitDate)
        try container.encodeIfPresent(commitSha, forKey: .commitSha)
        try container.encodeIfPresent(pkgVersion, forKey: .pkgVersion)
    }
}

