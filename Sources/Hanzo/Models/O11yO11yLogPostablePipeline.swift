//
// O11yO11yLogPostablePipeline.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct O11yO11yLogPostablePipeline: Sendable, Codable, ParameterConvertible, Hashable {

    /** Alias is the pipeline's short name. */
    public var alias: String?
    /** Config is the pipeline's processors, in order. */
    public var config: [O11yO11yLogPipelineOperator]?
    /** Description says what the pipeline is for. */
    public var description: String?
    /** Enabled turns the pipeline on. */
    public var enabled: Bool?
    /** Filter selects which records the pipeline processes. */
    public var filter: O11yO11yLogFilter?
    /** ID is the pipeline's id. Empty on a new pipeline; the id it was listed with to keep an existing one. */
    public var id: String?
    /** Name is the pipeline's display name. */
    public var name: String?
    /** OrderID is the pipeline's 1-based position in the set. */
    public var orderId: Int?

    public init(alias: String? = nil, config: [O11yO11yLogPipelineOperator]? = nil, description: String? = nil, enabled: Bool? = nil, filter: O11yO11yLogFilter? = nil, id: String? = nil, name: String? = nil, orderId: Int? = nil) {
        self.alias = alias
        self.config = config
        self.description = description
        self.enabled = enabled
        self.filter = filter
        self.id = id
        self.name = name
        self.orderId = orderId
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case alias
        case config
        case description
        case enabled
        case filter
        case id
        case name
        case orderId
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(alias, forKey: .alias)
        try container.encodeIfPresent(config, forKey: .config)
        try container.encodeIfPresent(description, forKey: .description)
        try container.encodeIfPresent(enabled, forKey: .enabled)
        try container.encodeIfPresent(filter, forKey: .filter)
        try container.encodeIfPresent(id, forKey: .id)
        try container.encodeIfPresent(name, forKey: .name)
        try container.encodeIfPresent(orderId, forKey: .orderId)
    }
}


@available(iOS 13, tvOS 13, watchOS 6, macOS 10.15, *)
extension O11yO11yLogPostablePipeline: Identifiable {}
