//
// UsageScope.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct UsageScope: Sendable, Codable, ParameterConvertible, Hashable {

    /** Org is the IAM org slug the rows were read under — the validated principal's, never a client header. */
    public var org: String?
    /** User is the caller's own subject, whose linked-account rows the accounts block carries. Absent on a read that is org-scoped only. */
    public var user: String?

    public init(org: String? = nil, user: String? = nil) {
        self.org = org
        self.user = user
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case org
        case user
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(org, forKey: .org)
        try container.encodeIfPresent(user, forKey: .user)
    }
}

