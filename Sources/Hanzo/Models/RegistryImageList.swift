//
// RegistryImageList.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct RegistryImageList: Sendable, Codable, ParameterConvertible, Hashable {

    /** Data is the org's repositories. */
    public var data: [RegistryImage]?
    /** Truncated is true when the catalog walk hit its page bound before the registry was exhausted — the list is a prefix, not the whole. */
    public var truncated: Bool?

    public init(data: [RegistryImage]? = nil, truncated: Bool? = nil) {
        self.data = data
        self.truncated = truncated
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case data
        case truncated
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(data, forKey: .data)
        try container.encodeIfPresent(truncated, forKey: .truncated)
    }
}

