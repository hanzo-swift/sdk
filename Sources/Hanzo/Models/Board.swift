//
// Board.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct Board: Sendable, Codable, ParameterConvertible, Hashable {

    /** the top models by spend */
    public var byModel: [ModelStat]?
    /** overall latency percentiles from the GenAI spans */
    public var latency: LatencyStat?
    /** the long tail beyond the top models, folded into one row */
    public var other: ModelStat?
    /** the window they were computed over, echoed back */
    public var range: BoardRange?
    /** whose numbers these are */
    public var scope: BoardScope?
    /** one gap-filled bucket per interval, so a chart never breaks */
    public var series: [BoardPoint]?
    /** the window's headline numbers */
    public var totals: BoardTotals?

    public init(byModel: [ModelStat]? = nil, latency: LatencyStat? = nil, other: ModelStat? = nil, range: BoardRange? = nil, scope: BoardScope? = nil, series: [BoardPoint]? = nil, totals: BoardTotals? = nil) {
        self.byModel = byModel
        self.latency = latency
        self.other = other
        self.range = range
        self.scope = scope
        self.series = series
        self.totals = totals
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case byModel
        case latency
        case other
        case range
        case scope
        case series
        case totals
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(byModel, forKey: .byModel)
        try container.encodeIfPresent(latency, forKey: .latency)
        try container.encodeIfPresent(other, forKey: .other)
        try container.encodeIfPresent(range, forKey: .range)
        try container.encodeIfPresent(scope, forKey: .scope)
        try container.encodeIfPresent(series, forKey: .series)
        try container.encodeIfPresent(totals, forKey: .totals)
    }
}

