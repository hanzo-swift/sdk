//
// ReferenceSetsOut.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct ReferenceSetsOut: Sendable, Codable, ParameterConvertible, Hashable {

    /** Refused names the sets that cannot be consulted at all. A key checked against one of these is UNKNOWN, not clean. */
    public var refused: [String]?
    /** Sets is the whole catalog, in a stable order. */
    public var sets: [ReferenceSet]?
    /** Stale names the sets past their freshness bound — the list to alarm on. */
    public var stale: [String]?

    public init(refused: [String]? = nil, sets: [ReferenceSet]? = nil, stale: [String]? = nil) {
        self.refused = refused
        self.sets = sets
        self.stale = stale
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case refused
        case sets
        case stale
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(refused, forKey: .refused)
        try container.encodeIfPresent(sets, forKey: .sets)
        try container.encodeIfPresent(stale, forKey: .stale)
    }
}

