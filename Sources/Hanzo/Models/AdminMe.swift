//
// AdminMe.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct AdminMe: Sendable, Codable, ParameterConvertible, Hashable {

    public var displayName: String?
    public var email: String?
    public var isSuperAdmin: Bool?
    /** IsWhiteLabel marks the admitted NON-super tier: an admin of an enabled white-label tenant org. Mutually exclusive with IsSuperAdmin (the gate lets exactly one tier through). The operator SPA reads it to render the SUBTREE cockpit — the fleet god-view nav (finance/revenue/metrics/o11y/providers) is hidden — while a super sees the whole fleet. */
    public var isWhiteLabel: Bool?
    public var name: String?
    public var owner: String?
    /** ScopeOrgs is the caller's visible tenant window: empty for a SuperAdmin (means ALL orgs), or the WL tenant's own subtree (today the singleton {org}). The SPA threads it through the faceting/drill-down layer so a WL tenant can never widen a filter past their subtree. */
    public var scopeOrgs: [String]?

    public init(displayName: String? = nil, email: String? = nil, isSuperAdmin: Bool? = nil, isWhiteLabel: Bool? = nil, name: String? = nil, owner: String? = nil, scopeOrgs: [String]? = nil) {
        self.displayName = displayName
        self.email = email
        self.isSuperAdmin = isSuperAdmin
        self.isWhiteLabel = isWhiteLabel
        self.name = name
        self.owner = owner
        self.scopeOrgs = scopeOrgs
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case displayName
        case email
        case isSuperAdmin
        case isWhiteLabel
        case name
        case owner
        case scopeOrgs
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(displayName, forKey: .displayName)
        try container.encodeIfPresent(email, forKey: .email)
        try container.encodeIfPresent(isSuperAdmin, forKey: .isSuperAdmin)
        try container.encodeIfPresent(isWhiteLabel, forKey: .isWhiteLabel)
        try container.encodeIfPresent(name, forKey: .name)
        try container.encodeIfPresent(owner, forKey: .owner)
        try container.encodeIfPresent(scopeOrgs, forKey: .scopeOrgs)
    }
}

