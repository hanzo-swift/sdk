//
// RiskSplitCounts.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct RiskSplitCounts: Sendable, Codable, ParameterConvertible, Hashable {

    /** Judged is how many rows carry a disposition. It is zero until a label plane writes one, and reporting it plainly is what lets a model plane refuse to rank rather than name a winner it cannot justify. */
    public var judged: Int?
    /** Productive and Unproductive are the two judged classes, so the imbalance is visible before anyone trains on it. */
    public var productive: Int?
    public var rows: Int?
    /** Subjects is how many distinct subjects the rows belong to. Every row of one subject is in ONE split, so this is the real sample size — the row count flatters it whenever a subject is active. */
    public var subjects: Int?
    public var test: Int?
    public var train: Int?
    public var unproductive: Int?
    public var val: Int?

    public init(judged: Int? = nil, productive: Int? = nil, rows: Int? = nil, subjects: Int? = nil, test: Int? = nil, train: Int? = nil, unproductive: Int? = nil, val: Int? = nil) {
        self.judged = judged
        self.productive = productive
        self.rows = rows
        self.subjects = subjects
        self.test = test
        self.train = train
        self.unproductive = unproductive
        self.val = val
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case judged
        case productive
        case rows
        case subjects
        case test
        case train
        case unproductive
        case val
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(judged, forKey: .judged)
        try container.encodeIfPresent(productive, forKey: .productive)
        try container.encodeIfPresent(rows, forKey: .rows)
        try container.encodeIfPresent(subjects, forKey: .subjects)
        try container.encodeIfPresent(test, forKey: .test)
        try container.encodeIfPresent(train, forKey: .train)
        try container.encodeIfPresent(unproductive, forKey: .unproductive)
        try container.encodeIfPresent(val, forKey: .val)
    }
}

