//
// AgentsRegisterSessionRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct AgentsRegisterSessionRequest: Codable, JSONEncodable, Hashable {

    public enum Status: String, Codable, CaseIterable {
        case running = "running"
        case paused = "paused"
        case done = "done"
        case error = "error"
    }
    /** Agent label (max 128 chars). */
    public var agent: String
    /** Defaults to the validated principal (org/sub). */
    public var actor: String?
    public var title: String?
    /** Defaults to running. */
    public var status: Status?
    /** Parent session id in the same org (for subagent linkage). */
    public var parentSessionId: String?
    public var taskWorkflowId: String?
    public var taskRunId: String?

    public init(agent: String, actor: String? = nil, title: String? = nil, status: Status? = nil, parentSessionId: String? = nil, taskWorkflowId: String? = nil, taskRunId: String? = nil) {
        self.agent = agent
        self.actor = actor
        self.title = title
        self.status = status
        self.parentSessionId = parentSessionId
        self.taskWorkflowId = taskWorkflowId
        self.taskRunId = taskRunId
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case agent
        case actor
        case title
        case status
        case parentSessionId
        case taskWorkflowId
        case taskRunId
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(agent, forKey: .agent)
        try container.encodeIfPresent(actor, forKey: .actor)
        try container.encodeIfPresent(title, forKey: .title)
        try container.encodeIfPresent(status, forKey: .status)
        try container.encodeIfPresent(parentSessionId, forKey: .parentSessionId)
        try container.encodeIfPresent(taskWorkflowId, forKey: .taskWorkflowId)
        try container.encodeIfPresent(taskRunId, forKey: .taskRunId)
    }
}

