//
// Balances.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct Balances: Sendable, Codable, ParameterConvertible, Hashable {

    /** Address is the account they belong to. */
    public var address: String?
    /** Chain is the chain the balances were read from. */
    public var chain: String?
    /** Native is the chain's own currency, as a 0x-quantity — the RPC's own encoding, not a float, because a wei value does not survive float64. */
    public var native: String?

    public init(address: String? = nil, chain: String? = nil, native: String? = nil) {
        self.address = address
        self.chain = chain
        self.native = native
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case address
        case chain
        case native
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(address, forKey: .address)
        try container.encodeIfPresent(chain, forKey: .chain)
        try container.encodeIfPresent(native, forKey: .native)
    }
}

