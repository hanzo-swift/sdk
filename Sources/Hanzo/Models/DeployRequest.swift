//
// DeployRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct DeployRequest: Sendable, Codable, ParameterConvertible, Hashable {

    /** Project is the deployed project's id. Required. */
    public var project: String?
    /** RepoURL is the source repository the project was built from. Empty means a hand-built project with nothing to attribute — an honest no-op, not an error. */
    public var repoUrl: String?

    public init(project: String? = nil, repoUrl: String? = nil) {
        self.project = project
        self.repoUrl = repoUrl
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case project
        case repoUrl
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(project, forKey: .project)
        try container.encodeIfPresent(repoUrl, forKey: .repoUrl)
    }
}

