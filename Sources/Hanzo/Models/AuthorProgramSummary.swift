//
// AuthorProgramSummary.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct AuthorProgramSummary: Sendable, Codable, ParameterConvertible, Hashable {

    /** AccruedCents is the page's lifetime royalty accrued, in integer USD cents. */
    public var accruedCents: Int?
    /** Approved is how many are admitted and accruing. */
    public var approved: Int?
    /** Connected is how many of those are enrolled but not yet admitted to earning. */
    public var connected: Int?
    /** PaidCents is what has been RECORDED as paid across the page, in integer USD cents. Recorded, not settled: the money leaves in a human's hands. */
    public var paidCents: Int?
    /** PendingCents is what the platform still owes across the page, in integer USD cents — the sum of each author's own accrued − paid, each floored at zero. */
    public var pendingCents: Int?
    /** Suspended is how many have been stopped from accruing further. An author holds exactly one status, so the three buckets never overlap and connected + approved + suspended = total. */
    public var suspended: Int?
    /** Total is how many author records this response actually carried. The roll-up is folded over the SAME page as authors — newest first, bounded by limit (default 500, ceiling 1000) — so on a program larger than the page it summarizes that page, not the fleet. */
    public var total: Int?

    public init(accruedCents: Int? = nil, approved: Int? = nil, connected: Int? = nil, paidCents: Int? = nil, pendingCents: Int? = nil, suspended: Int? = nil, total: Int? = nil) {
        self.accruedCents = accruedCents
        self.approved = approved
        self.connected = connected
        self.paidCents = paidCents
        self.pendingCents = pendingCents
        self.suspended = suspended
        self.total = total
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case accruedCents
        case approved
        case connected
        case paidCents
        case pendingCents
        case suspended
        case total
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(accruedCents, forKey: .accruedCents)
        try container.encodeIfPresent(approved, forKey: .approved)
        try container.encodeIfPresent(connected, forKey: .connected)
        try container.encodeIfPresent(paidCents, forKey: .paidCents)
        try container.encodeIfPresent(pendingCents, forKey: .pendingCents)
        try container.encodeIfPresent(suspended, forKey: .suspended)
        try container.encodeIfPresent(total, forKey: .total)
    }
}

