//
// PubsubConsumerConfig.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct PubsubConsumerConfig: Codable, JSONEncodable, Hashable {

    public enum DeliverPolicy: String, Codable, CaseIterable {
        case all = "all"
        case last = "last"
        case new = "new"
        case byStartSequence = "by_start_sequence"
        case byStartTime = "by_start_time"
        case lastPerSubject = "last_per_subject"
    }
    public enum AckPolicy: String, Codable, CaseIterable {
        case explicit = "explicit"
        case _none = "none"
        case all = "all"
    }
    /** Durable consumer name */
    public var durableName: String
    public var deliverPolicy: DeliverPolicy? = .all
    public var ackPolicy: AckPolicy? = .explicit
    /** Subject filter for this consumer */
    public var filterSubject: String?
    /** Maximum delivery attempts per message */
    public var maxDeliver: Int? = -1
    /** Ack wait timeout in nanoseconds */
    public var ackWait: Int64? = 30000000000

    public init(durableName: String, deliverPolicy: DeliverPolicy? = .all, ackPolicy: AckPolicy? = .explicit, filterSubject: String? = nil, maxDeliver: Int? = -1, ackWait: Int64? = 30000000000) {
        self.durableName = durableName
        self.deliverPolicy = deliverPolicy
        self.ackPolicy = ackPolicy
        self.filterSubject = filterSubject
        self.maxDeliver = maxDeliver
        self.ackWait = ackWait
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case durableName = "durable_name"
        case deliverPolicy = "deliver_policy"
        case ackPolicy = "ack_policy"
        case filterSubject = "filter_subject"
        case maxDeliver = "max_deliver"
        case ackWait = "ack_wait"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(durableName, forKey: .durableName)
        try container.encodeIfPresent(deliverPolicy, forKey: .deliverPolicy)
        try container.encodeIfPresent(ackPolicy, forKey: .ackPolicy)
        try container.encodeIfPresent(filterSubject, forKey: .filterSubject)
        try container.encodeIfPresent(maxDeliver, forKey: .maxDeliver)
        try container.encodeIfPresent(ackWait, forKey: .ackWait)
    }
}

