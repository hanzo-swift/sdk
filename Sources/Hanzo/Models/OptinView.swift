//
// OptinView.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct OptinView: Sendable, Codable, ParameterConvertible, Hashable {

    /** Org is the caller's org's listing preference on the cross-org board, and whether this caller is allowed to change it. It is read for every caller — a member sees where their org stands even though only an admin may edit it. */
    public var org: OrgOptinView?
    /** User is the caller's OWN listing preference, and whether they may change it. */
    public var user: UserOptinView?

    public init(org: OrgOptinView? = nil, user: UserOptinView? = nil) {
        self.org = org
        self.user = user
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case org
        case user
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(org, forKey: .org)
        try container.encodeIfPresent(user, forKey: .user)
    }
}

