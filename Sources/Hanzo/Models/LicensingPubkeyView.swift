//
// LicensingPubkeyView.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct LicensingPubkeyView: Sendable, Codable, ParameterConvertible, Hashable {

    /** Alg is always \"Ed25519\". */
    public var alg: String?
    /** Keys is the same key as a single-entry JWKS (OKP/Ed25519), for JWKS-shaped consumers. */
    public var keys: [LicensingJWK]?
    /** Provider names the KMS holding the private half (\"local\" | \"aws\" | ...). \"local\" means a development key — never trust it in production. */
    public var provider: String?
    /** PublicKey is the 32-byte Ed25519 public key, standard base64. This is the form the engine embeds for offline verification. */
    public var publicKey: String?
    /** Schema is the license payload schema version this key signs. */
    public var schema: Int?
    /** TokenFormat states the wire layout so an implementer can verify a token without this service's source. */
    public var tokenFormat: String?

    public init(alg: String? = nil, keys: [LicensingJWK]? = nil, provider: String? = nil, publicKey: String? = nil, schema: Int? = nil, tokenFormat: String? = nil) {
        self.alg = alg
        self.keys = keys
        self.provider = provider
        self.publicKey = publicKey
        self.schema = schema
        self.tokenFormat = tokenFormat
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case alg
        case keys
        case provider
        case publicKey = "public_key"
        case schema
        case tokenFormat = "token_format"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(alg, forKey: .alg)
        try container.encodeIfPresent(keys, forKey: .keys)
        try container.encodeIfPresent(provider, forKey: .provider)
        try container.encodeIfPresent(publicKey, forKey: .publicKey)
        try container.encodeIfPresent(schema, forKey: .schema)
        try container.encodeIfPresent(tokenFormat, forKey: .tokenFormat)
    }
}

