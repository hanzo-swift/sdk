//
// StatsOut.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct StatsOut: Sendable, Codable, ParameterConvertible, Hashable {

    /** Admin is the upstream service's server-panel flag, always false here. */
    public var admin: Bool?
    /** Metrics is the upstream transactor's metrics block. This server does not populate it, so it is always the empty object — the front reads the key, not its contents. */
    public var metrics: JSONValue?
    /** Statistics carries the live sessions. */
    public var statistics: StatsSessions?

    public init(admin: Bool? = nil, metrics: JSONValue? = nil, statistics: StatsSessions? = nil) {
        self.admin = admin
        self.metrics = metrics
        self.statistics = statistics
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case admin
        case metrics
        case statistics
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(admin, forKey: .admin)
        try container.encodeIfPresent(metrics, forKey: .metrics)
        try container.encodeIfPresent(statistics, forKey: .statistics)
    }
}

