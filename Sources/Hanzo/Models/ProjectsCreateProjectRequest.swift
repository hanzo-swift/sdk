//
// ProjectsCreateProjectRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct ProjectsCreateProjectRequest: Codable, JSONEncodable, Hashable {

    public enum Framework: String, Codable, CaseIterable {
        case _static = "static"
        case vite = "vite"
        case next = "next"
        case react = "react"
        case astro = "astro"
        case svelte = "svelte"
        case vue = "vue"
        case remix = "remix"
        case nuxt = "nuxt"
        case unity = "unity"
        case unreal = "unreal"
        case godot = "godot"
    }
    public static let slugRule = StringRule(minLength: nil, maxLength: nil, pattern: "/^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$/")
    /** Display name (required). */
    public var name: String
    /** Org-unique handle; derived from name when omitted. */
    public var slug: String?
    public var description: String?
    /** Build hint; defaults to \"static\". */
    public var framework: Framework?
    public var repo: ProjectsCreateProjectRequestRepo?

    public init(name: String, slug: String? = nil, description: String? = nil, framework: Framework? = nil, repo: ProjectsCreateProjectRequestRepo? = nil) {
        self.name = name
        self.slug = slug
        self.description = description
        self.framework = framework
        self.repo = repo
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case name
        case slug
        case description
        case framework
        case repo
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(name, forKey: .name)
        try container.encodeIfPresent(slug, forKey: .slug)
        try container.encodeIfPresent(description, forKey: .description)
        try container.encodeIfPresent(framework, forKey: .framework)
        try container.encodeIfPresent(repo, forKey: .repo)
    }
}

