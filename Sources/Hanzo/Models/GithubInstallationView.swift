//
// GithubInstallationView.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct GithubInstallationView: Sendable, Codable, ParameterConvertible, Hashable {

    /** Connected reports whether THIS org has already bound this account. */
    public var connected: Bool?
    /** Grant is \"all\" or \"selected\" — how many of the account's repositories the install covers. A reader deciding what to import needs the reach, not just the name. */
    public var grant: String?
    /** HTMLURL is the account's page on GitHub. */
    public var htmlUrl: String?
    /** Login is the GitHub account name — the org or user the App is installed on. */
    public var login: String?
    /** Type is \"Organization\" or \"User\". */
    public var type: String?

    public init(connected: Bool? = nil, grant: String? = nil, htmlUrl: String? = nil, login: String? = nil, type: String? = nil) {
        self.connected = connected
        self.grant = grant
        self.htmlUrl = htmlUrl
        self.login = login
        self.type = type
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case connected
        case grant
        case htmlUrl
        case login
        case type
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(connected, forKey: .connected)
        try container.encodeIfPresent(grant, forKey: .grant)
        try container.encodeIfPresent(htmlUrl, forKey: .htmlUrl)
        try container.encodeIfPresent(login, forKey: .login)
        try container.encodeIfPresent(type, forKey: .type)
    }
}

