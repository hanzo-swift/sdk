//
// ConsumerRecord.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct ConsumerRecord: Sendable, Codable, ParameterConvertible, Hashable {

    /** Ack is the acknowledgement discipline: explicit, none or all. */
    public var ack: String?
    /** AckWait is the redelivery timeout in seconds. */
    public var ackWait: Int?
    /** Acked is the stream sequence acknowledged furthest. */
    public var acked: Int?
    /** Deliver is the starting point: all, last, new or lastPerSubject. */
    public var deliver: String?
    /** Delivered is the stream sequence delivered furthest. */
    public var delivered: Int?
    /** Filter is the subject filter, in the org's namespace; empty means all. */
    public var filter: String?
    /** MaxDeliver is the delivery-attempt cap; -1 means unlimited. */
    public var maxDeliver: Int?
    /** Name is the durable consumer name. */
    public var name: String?
    /** Pending is how many messages await delivery. */
    public var pending: Int?
    /** Redelivered is how many messages are being redelivered. */
    public var redelivered: Int?
    /** Stream is the stream it consumes, in the org's view. */
    public var stream: String?

    public init(ack: String? = nil, ackWait: Int? = nil, acked: Int? = nil, deliver: String? = nil, delivered: Int? = nil, filter: String? = nil, maxDeliver: Int? = nil, name: String? = nil, pending: Int? = nil, redelivered: Int? = nil, stream: String? = nil) {
        self.ack = ack
        self.ackWait = ackWait
        self.acked = acked
        self.deliver = deliver
        self.delivered = delivered
        self.filter = filter
        self.maxDeliver = maxDeliver
        self.name = name
        self.pending = pending
        self.redelivered = redelivered
        self.stream = stream
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case ack
        case ackWait
        case acked
        case deliver
        case delivered
        case filter
        case maxDeliver
        case name
        case pending
        case redelivered
        case stream
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(ack, forKey: .ack)
        try container.encodeIfPresent(ackWait, forKey: .ackWait)
        try container.encodeIfPresent(acked, forKey: .acked)
        try container.encodeIfPresent(deliver, forKey: .deliver)
        try container.encodeIfPresent(delivered, forKey: .delivered)
        try container.encodeIfPresent(filter, forKey: .filter)
        try container.encodeIfPresent(maxDeliver, forKey: .maxDeliver)
        try container.encodeIfPresent(name, forKey: .name)
        try container.encodeIfPresent(pending, forKey: .pending)
        try container.encodeIfPresent(redelivered, forKey: .redelivered)
        try container.encodeIfPresent(stream, forKey: .stream)
    }
}

