//
// DriveList.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct DriveList: Sendable, Codable, ParameterConvertible, Hashable {

    /** Drives are the drives at the space's root. */
    public var drives: [DriveItem]?
    /** Space is the space that was listed. */
    public var space: String?
    /** Total is how many drives came back. The listing is BOUNDED, so it is what came back and not a count of what the space holds. */
    public var total: Int?

    public init(drives: [DriveItem]? = nil, space: String? = nil, total: Int? = nil) {
        self.drives = drives
        self.space = space
        self.total = total
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case drives
        case space
        case total
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(drives, forKey: .drives)
        try container.encodeIfPresent(space, forKey: .space)
        try container.encodeIfPresent(total, forKey: .total)
    }
}

