//
// BillingBalance.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Prepaid wallet balance in USD cents. */
public struct BillingBalance: Codable, JSONEncodable, Hashable {

    public var balance: Int64?
    public var holds: Int64?
    public var available: Int64?

    public init(balance: Int64? = nil, holds: Int64? = nil, available: Int64? = nil) {
        self.balance = balance
        self.holds = holds
        self.available = available
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case balance
        case holds
        case available
    }

    public var additionalProperties: [String: AnyCodable] = [:]

    public subscript(key: String) -> AnyCodable? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            additionalProperties[key] = newValue
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(balance, forKey: .balance)
        try container.encodeIfPresent(holds, forKey: .holds)
        try container.encodeIfPresent(available, forKey: .available)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(additionalProperties)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        balance = try container.decodeIfPresent(Int64.self, forKey: .balance)
        holds = try container.decodeIfPresent(Int64.self, forKey: .holds)
        available = try container.decodeIfPresent(Int64.self, forKey: .available)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("balance")
        nonAdditionalPropertyKeys.insert("holds")
        nonAdditionalPropertyKeys.insert("available")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        additionalProperties = try additionalPropertiesContainer.decodeMap(AnyCodable.self, excludedKeys: nonAdditionalPropertyKeys)
    }
}

