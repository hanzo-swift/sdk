//
// O11yO11yDomainsData.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct O11yO11yDomainsData: Sendable, Codable, ParameterConvertible, Hashable {

    /** Results is one entry per query. Each entry's shape follows the answer's type — time-series data, scalar data or raw rows — so the bytes pass through verbatim. */
    public var results: [JSONValue]?

    public init(results: [JSONValue]? = nil) {
        self.results = results
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case results
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(results, forKey: .results)
    }
}

