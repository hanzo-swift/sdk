//
// SearchOut.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct SearchOut: Sendable, Codable, ParameterConvertible, Hashable {

    /** Degraded is true when the index was unreachable and this answer is honestly empty rather than wrong — a RAG caller continues with no context instead of failing the turn. Absent on a normal answer. */
    public var degraded: Bool?
    /** Hits are the matching passages, most relevant first. */
    public var hits: [Hit]?

    public init(degraded: Bool? = nil, hits: [Hit]? = nil) {
        self.degraded = degraded
        self.hits = hits
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case degraded
        case hits
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(degraded, forKey: .degraded)
        try container.encodeIfPresent(hits, forKey: .hits)
    }
}

