//
// SuggestResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct SuggestResponse: Sendable, Codable, ParameterConvertible, Hashable {

    /** Funnel is the org's trailing-window traffic → signups → orders. */
    public var funnel: Funnel?
    /** Narrative is the AI's grounded prose over those quests and numbers. Absent when no AI plane is wired or the completion failed — never fabricated. */
    public var narrative: String?
    /** Next is the id of the single next step the static journey names — the linear answer the ranked Suggestions refine. */
    public var next: String?
    /** Recommendations are the next-best GTM actions derived from that funnel. */
    public var recommendations: [String]?
    /** Suggestions are the available, non-terminal quests ranked best-first by how much downstream work each unblocks. */
    public var suggestions: [Suggestion]?

    public init(funnel: Funnel? = nil, narrative: String? = nil, next: String? = nil, recommendations: [String]? = nil, suggestions: [Suggestion]? = nil) {
        self.funnel = funnel
        self.narrative = narrative
        self.next = next
        self.recommendations = recommendations
        self.suggestions = suggestions
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case funnel
        case narrative
        case next
        case recommendations
        case suggestions
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(funnel, forKey: .funnel)
        try container.encodeIfPresent(narrative, forKey: .narrative)
        try container.encodeIfPresent(next, forKey: .next)
        try container.encodeIfPresent(recommendations, forKey: .recommendations)
        try container.encodeIfPresent(suggestions, forKey: .suggestions)
    }
}

