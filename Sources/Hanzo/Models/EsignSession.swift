//
// EsignSession.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct EsignSession: Sendable, Codable, ParameterConvertible, Hashable {

    /** Document is what is being signed. */
    public var document: EsignState?
    /** Fields is only the fields this recipient must fill — never another party's, so the layout a signer sees cannot reveal what anyone else was asked for. */
    public var fields: [EsignField]?
    /** PdfBase64 is the PDF to display, base64-encoded. Null when the document's stored bytes are missing. */
    public var pdfBase64: String?
    /** Recipient is who the token says you are. */
    public var recipient: EsignSigner?

    public init(document: EsignState? = nil, fields: [EsignField]? = nil, pdfBase64: String? = nil, recipient: EsignSigner? = nil) {
        self.document = document
        self.fields = fields
        self.pdfBase64 = pdfBase64
        self.recipient = recipient
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case document
        case fields
        case pdfBase64
        case recipient
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(document, forKey: .document)
        try container.encodeIfPresent(fields, forKey: .fields)
        try container.encodeIfPresent(pdfBase64, forKey: .pdfBase64)
        try container.encodeIfPresent(recipient, forKey: .recipient)
    }
}

