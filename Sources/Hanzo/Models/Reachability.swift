//
// Reachability.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct Reachability: Sendable, Codable, ParameterConvertible, Hashable {

    /** Configured is whether the wholesale credentials are present at all. */
    public var configured: Bool?
    /** Env is the registrar environment. It is the fact that decides whether money moves: only \"prod\" reaches the live, billable registrar — anything else, including unset, is the sandbox. */
    public var env: String?
    /** Error is the blocker, so an operator reads it instead of guessing at it. */
    public var error: String?
    /** Reachable is whether the registrar accepted those credentials on a live call made while the caller waited. */
    public var reachable: Bool?
    /** Registrar names the wholesale registrar behind it. */
    public var registrar: String?
    /** Service names the subsystem answering. */
    public var service: String?
    /** Status is \"ok\" when a live call succeeded, else \"degraded\". */
    public var status: String?

    public init(configured: Bool? = nil, env: String? = nil, error: String? = nil, reachable: Bool? = nil, registrar: String? = nil, service: String? = nil, status: String? = nil) {
        self.configured = configured
        self.env = env
        self.error = error
        self.reachable = reachable
        self.registrar = registrar
        self.service = service
        self.status = status
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case configured
        case env
        case error
        case reachable
        case registrar
        case service
        case status
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(configured, forKey: .configured)
        try container.encodeIfPresent(env, forKey: .env)
        try container.encodeIfPresent(error, forKey: .error)
        try container.encodeIfPresent(reachable, forKey: .reachable)
        try container.encodeIfPresent(registrar, forKey: .registrar)
        try container.encodeIfPresent(service, forKey: .service)
        try container.encodeIfPresent(status, forKey: .status)
    }
}

