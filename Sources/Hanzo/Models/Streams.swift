//
// Streams.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct Streams: Sendable, Codable, ParameterConvertible, Hashable {

    /** Streams is the page, ordered by name. */
    public var streams: [ModelStream]?
    /** Total is the org's stream count before paging. */
    public var total: Int64?

    public init(streams: [ModelStream]? = nil, total: Int64? = nil) {
        self.streams = streams
        self.total = total
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case streams
        case total
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(streams, forKey: .streams)
        try container.encodeIfPresent(total, forKey: .total)
    }
}

