//
// Survey.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct Survey: Sendable, Codable, ParameterConvertible, Hashable {

    /** Carries is all four where the node answered and `null` where it did not. It is never a short list: a partial read reporting three would let a reader count them and conclude the chain is missing one. */
    public var carries: [Precompile]?
    public var chain: String?
    public var reach: Reach?
    /** RPC is the endpoint that was asked, so an answer names where it came from. */
    public var rpc: String?

    public init(carries: [Precompile]? = nil, chain: String? = nil, reach: Reach? = nil, rpc: String? = nil) {
        self.carries = carries
        self.chain = chain
        self.reach = reach
        self.rpc = rpc
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case carries
        case chain
        case reach
        case rpc
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(carries, forKey: .carries)
        try container.encodeIfPresent(chain, forKey: .chain)
        try container.encodeIfPresent(reach, forKey: .reach)
        try container.encodeIfPresent(rpc, forKey: .rpc)
    }
}

