//
// CommercePaymentRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct CommercePaymentRequest: Codable, JSONEncodable, Hashable {

    public enum ModelType: String, Codable, CaseIterable {
        case card = "card"
        case paypal = "paypal"
        case stripe = "stripe"
        case bitcoin = "bitcoin"
        case ethereum = "ethereum"
        case balance = "balance"
    }
    public var type: ModelType?
    /** Payment processor token (e.g., Stripe token) */
    public var token: String?
    public var paymentMethodId: String?
    /** Amount in cents (optional, defaults to order total) */
    public var amount: Int?

    public init(type: ModelType? = nil, token: String? = nil, paymentMethodId: String? = nil, amount: Int? = nil) {
        self.type = type
        self.token = token
        self.paymentMethodId = paymentMethodId
        self.amount = amount
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case type
        case token
        case paymentMethodId
        case amount
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(type, forKey: .type)
        try container.encodeIfPresent(token, forKey: .token)
        try container.encodeIfPresent(paymentMethodId, forKey: .paymentMethodId)
        try container.encodeIfPresent(amount, forKey: .amount)
    }
}

