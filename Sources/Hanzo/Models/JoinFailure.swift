//
// JoinFailure.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct JoinFailure: Sendable, Codable, ParameterConvertible, Hashable {

    /** Channel is Slack's id for the room that refused. */
    public var channel: String?
    /** Error is Slack's own code, carried through unchanged. */
    public var error: String?
    /** Name is that room's human name, so the operator does not have to look the id up. */
    public var name: String?

    public init(channel: String? = nil, error: String? = nil, name: String? = nil) {
        self.channel = channel
        self.error = error
        self.name = name
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case channel
        case error
        case name
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(channel, forKey: .channel)
        try container.encodeIfPresent(error, forKey: .error)
        try container.encodeIfPresent(name, forKey: .name)
    }
}

