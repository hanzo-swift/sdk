//
// ProductSearchIndex.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** A single Meilisearch index as decoded by the console&#39;s search panel. */
public struct ProductSearchIndex: Codable, JSONEncodable, Hashable {

    /** Index UID. */
    public var name: String
    /** Number of documents in the index. */
    public var docCount: Int64
    /** Last-indexed timestamp (RFC 3339); null if unknown. */
    public var lastIndexedAt: String
    /** Created timestamp (RFC 3339); falls back to now() if the upstream omits it. */
    public var createdAt: String

    public init(name: String, docCount: Int64, lastIndexedAt: String, createdAt: String) {
        self.name = name
        self.docCount = docCount
        self.lastIndexedAt = lastIndexedAt
        self.createdAt = createdAt
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case name
        case docCount
        case lastIndexedAt
        case createdAt
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(name, forKey: .name)
        try container.encode(docCount, forKey: .docCount)
        try container.encode(lastIndexedAt, forKey: .lastIndexedAt)
        try container.encode(createdAt, forKey: .createdAt)
    }
}

