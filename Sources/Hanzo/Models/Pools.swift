//
// Pools.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct Pools: Sendable, Codable, ParameterConvertible, Hashable {

    public var chain: String?
    /** Pools is `[]` where the chain has none and `null` where the read failed.  The two are different sentences and the wire says which: an empty ARRAY is the indexer answering that nothing is deployed there, and `null` is nobody having answered. `omitempty` would collapse both to an absent key — which is the exact flattening the reach beside it exists to prevent, reintroduced one struct tag lower down. */
    public var pools: [Pool]?
    public var reach: Reach?

    public init(chain: String? = nil, pools: [Pool]? = nil, reach: Reach? = nil) {
        self.chain = chain
        self.pools = pools
        self.reach = reach
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case chain
        case pools
        case reach
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(chain, forKey: .chain)
        try container.encodeIfPresent(pools, forKey: .pools)
        try container.encodeIfPresent(reach, forKey: .reach)
    }
}

