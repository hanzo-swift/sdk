//
// ChainList.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct ChainList: Sendable, Codable, ParameterConvertible, Hashable {

    /** Chains is every chain this deployment is configured to reach, sorted by id. Empty when none are configured — never a fabricated entry. */
    public var chains: [Chain]?

    public init(chains: [Chain]? = nil) {
        self.chains = chains
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case chains
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(chains, forKey: .chains)
    }
}

