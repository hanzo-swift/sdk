//
// BusMessage.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct BusMessage: Sendable, Codable, ParameterConvertible, Hashable {

    /** Data is the payload as UTF-8 text. */
    public var data: String?
    /** Headers are the message's headers, when it carries any. */
    public var headers: [String: [String]]?
    /** Seq is the message's stream sequence — fetched messages only. */
    public var seq: Int?
    /** Subject is the message's subject in the org's own namespace. */
    public var subject: String?
    /** Time is when the stream stored the message, RFC3339 — fetched messages only. */
    public var time: String?

    public init(data: String? = nil, headers: [String: [String]]? = nil, seq: Int? = nil, subject: String? = nil, time: String? = nil) {
        self.data = data
        self.headers = headers
        self.seq = seq
        self.subject = subject
        self.time = time
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case data
        case headers
        case seq
        case subject
        case time
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(data, forKey: .data)
        try container.encodeIfPresent(headers, forKey: .headers)
        try container.encodeIfPresent(seq, forKey: .seq)
        try container.encodeIfPresent(subject, forKey: .subject)
        try container.encodeIfPresent(time, forKey: .time)
    }
}

