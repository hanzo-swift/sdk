//
// CurriculumView.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct CurriculumView: Sendable, Codable, ParameterConvertible, Hashable {

    /** Curriculum is the enabled journey: its version, title and ordered steps. */
    public var curriculum: Curriculum?
    /** Custom is true when the org's OWN curriculum override is active; false when the journey comes from the brand blueprint or the embedded fixture. */
    public var custom: Bool?

    public init(curriculum: Curriculum? = nil, custom: Bool? = nil) {
        self.curriculum = curriculum
        self.custom = custom
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case curriculum
        case custom
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(curriculum, forKey: .curriculum)
        try container.encodeIfPresent(custom, forKey: .custom)
    }
}

