//
// ProductSearchStats.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Aggregate search statistics across all indexes. */
public struct ProductSearchStats: Codable, JSONEncodable, Hashable {

    /** Total documents across all indexes. */
    public var totalDocuments: Int64
    /** Always 0 — Meilisearch keeps no query-history counters. */
    public var totalSearches: Int64
    /** Always 0 — not derivable from the index. */
    public var totalSessions: Int64
    /** Always empty — no per-day query history is available. */
    public var searchesPerDay: [ProductDayCount]

    public init(totalDocuments: Int64, totalSearches: Int64, totalSessions: Int64, searchesPerDay: [ProductDayCount]) {
        self.totalDocuments = totalDocuments
        self.totalSearches = totalSearches
        self.totalSessions = totalSessions
        self.searchesPerDay = searchesPerDay
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case totalDocuments
        case totalSearches
        case totalSessions
        case searchesPerDay
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(totalDocuments, forKey: .totalDocuments)
        try container.encode(totalSearches, forKey: .totalSearches)
        try container.encode(totalSessions, forKey: .totalSessions)
        try container.encode(searchesPerDay, forKey: .searchesPerDay)
    }
}

