//
// KvClusterCreate.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct KvClusterCreate: Codable, JSONEncodable, Hashable {

    public enum Mode: String, Codable, CaseIterable {
        case standalone = "standalone"
        case sentinel = "sentinel"
        case cluster = "cluster"
    }
    public enum EvictionPolicy: String, Codable, CaseIterable {
        case noeviction = "noeviction"
        case allkeysLru = "allkeys-lru"
        case allkeysLfu = "allkeys-lfu"
        case volatileLru = "volatile-lru"
        case volatileLfu = "volatile-lfu"
        case allkeysRandom = "allkeys-random"
    }
    public var name: String
    public var mode: Mode? = .standalone
    public var version: String? = "8.0"
    public var maxMemoryMb: Int? = 256
    public var replicas: Int? = 0
    public var tls: Bool? = true
    public var evictionPolicy: EvictionPolicy? = .allkeysLru

    public init(name: String, mode: Mode? = .standalone, version: String? = "8.0", maxMemoryMb: Int? = 256, replicas: Int? = 0, tls: Bool? = true, evictionPolicy: EvictionPolicy? = .allkeysLru) {
        self.name = name
        self.mode = mode
        self.version = version
        self.maxMemoryMb = maxMemoryMb
        self.replicas = replicas
        self.tls = tls
        self.evictionPolicy = evictionPolicy
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case name
        case mode
        case version
        case maxMemoryMb = "max_memory_mb"
        case replicas
        case tls
        case evictionPolicy = "eviction_policy"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(name, forKey: .name)
        try container.encodeIfPresent(mode, forKey: .mode)
        try container.encodeIfPresent(version, forKey: .version)
        try container.encodeIfPresent(maxMemoryMb, forKey: .maxMemoryMb)
        try container.encodeIfPresent(replicas, forKey: .replicas)
        try container.encodeIfPresent(tls, forKey: .tls)
        try container.encodeIfPresent(evictionPolicy, forKey: .evictionPolicy)
    }
}

