//
// TemplateCatalog.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct TemplateCatalog: Sendable, Codable, ParameterConvertible, Hashable {

    /** Data is the catalog, metadata and merge fields only — never the template bodies, which are fetched one at a time. */
    public var data: [TemplateView]?
    /** Disclaimer is the boundary made visible on the wire: Hanzo Legal is document tooling, not legal advice. */
    public var disclaimer: String?

    public init(data: [TemplateView]? = nil, disclaimer: String? = nil) {
        self.data = data
        self.disclaimer = disclaimer
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case data
        case disclaimer
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(data, forKey: .data)
        try container.encodeIfPresent(disclaimer, forKey: .disclaimer)
    }
}

