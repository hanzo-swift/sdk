//
// StreamRecord.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct StreamRecord: Sendable, Codable, ParameterConvertible, Hashable {

    /** Bytes is how many bytes the stream holds right now. */
    public var bytes: Int?
    /** Consumers is how many consumers the stream carries. */
    public var consumers: Int?
    /** Created is when the stream was created, RFC3339. */
    public var created: String?
    /** Discard says which end gives way at the limits: old or new. */
    public var discard: String?
    /** FirstSeq is the sequence of the oldest retained message. */
    public var firstSeq: Int?
    /** LastSeq is the sequence of the newest message. */
    public var lastSeq: Int?
    /** MaxAge is the age cap in seconds; 0 means unlimited. */
    public var maxAge: Int?
    /** MaxBytes is the retained-byte cap; -1 means unlimited. */
    public var maxBytes: Int?
    /** MaxMsgs is the retained-message cap; -1 means unlimited. */
    public var maxMsgs: Int?
    /** Messages is how many messages the stream holds right now. */
    public var messages: Int?
    /** Name is the stream's name within the org. */
    public var name: String?
    /** Retention is the discipline: limits, interest or workqueue. */
    public var retention: String?
    /** Storage is the backend: file or memory. */
    public var storage: String?
    /** Subjects are the subjects it captures, in the org's namespace. */
    public var subjects: [String]?

    public init(bytes: Int? = nil, consumers: Int? = nil, created: String? = nil, discard: String? = nil, firstSeq: Int? = nil, lastSeq: Int? = nil, maxAge: Int? = nil, maxBytes: Int? = nil, maxMsgs: Int? = nil, messages: Int? = nil, name: String? = nil, retention: String? = nil, storage: String? = nil, subjects: [String]? = nil) {
        self.bytes = bytes
        self.consumers = consumers
        self.created = created
        self.discard = discard
        self.firstSeq = firstSeq
        self.lastSeq = lastSeq
        self.maxAge = maxAge
        self.maxBytes = maxBytes
        self.maxMsgs = maxMsgs
        self.messages = messages
        self.name = name
        self.retention = retention
        self.storage = storage
        self.subjects = subjects
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case bytes
        case consumers
        case created
        case discard
        case firstSeq
        case lastSeq
        case maxAge
        case maxBytes
        case maxMsgs
        case messages
        case name
        case retention
        case storage
        case subjects
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(bytes, forKey: .bytes)
        try container.encodeIfPresent(consumers, forKey: .consumers)
        try container.encodeIfPresent(created, forKey: .created)
        try container.encodeIfPresent(discard, forKey: .discard)
        try container.encodeIfPresent(firstSeq, forKey: .firstSeq)
        try container.encodeIfPresent(lastSeq, forKey: .lastSeq)
        try container.encodeIfPresent(maxAge, forKey: .maxAge)
        try container.encodeIfPresent(maxBytes, forKey: .maxBytes)
        try container.encodeIfPresent(maxMsgs, forKey: .maxMsgs)
        try container.encodeIfPresent(messages, forKey: .messages)
        try container.encodeIfPresent(name, forKey: .name)
        try container.encodeIfPresent(retention, forKey: .retention)
        try container.encodeIfPresent(storage, forKey: .storage)
        try container.encodeIfPresent(subjects, forKey: .subjects)
    }
}

