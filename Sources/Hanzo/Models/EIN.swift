//
// EIN.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct EIN: Sendable, Codable, ParameterConvertible, Hashable {

    /** Expedited reports that prioritised handling was asked for. */
    public var expedited: Bool?
    /** Forms are the forms this application owes, with what each is for. */
    public var forms: [Form]?
    /** NAICS is the six-digit code for what the business does. The SS-4 asks it and the IRS will not process an application without one. */
    public var naics: String?
    /** Number is the issued EIN, absent until the IRS issues it. */
    public var number: String?
    /** Online reports that this application can be filed with the IRS online and issued in a sitting, rather than signed and posted. It is the single fact that decides how long a customer waits, so it is answered rather than implied by the absence of forms. */
    public var online: Bool?
    /** Responsible is the person the IRS holds answerable. */
    public var responsible: Responsible?
    /** Status is how far it has got. */
    public var status: String?

    public init(expedited: Bool? = nil, forms: [Form]? = nil, naics: String? = nil, number: String? = nil, online: Bool? = nil, responsible: Responsible? = nil, status: String? = nil) {
        self.expedited = expedited
        self.forms = forms
        self.naics = naics
        self.number = number
        self.online = online
        self.responsible = responsible
        self.status = status
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case expedited
        case forms
        case naics
        case number
        case online
        case responsible
        case status
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(expedited, forKey: .expedited)
        try container.encodeIfPresent(forms, forKey: .forms)
        try container.encodeIfPresent(naics, forKey: .naics)
        try container.encodeIfPresent(number, forKey: .number)
        try container.encodeIfPresent(online, forKey: .online)
        try container.encodeIfPresent(responsible, forKey: .responsible)
        try container.encodeIfPresent(status, forKey: .status)
    }
}

