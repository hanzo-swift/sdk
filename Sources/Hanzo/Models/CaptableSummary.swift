//
// CaptableSummary.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct CaptableSummary: Sendable, Codable, ParameterConvertible, Hashable {

    /** ByShareClass is each share class's authorized-versus-issued position, in class creation order. */
    public var byShareClass: [CaptableClassHolding]?
    /** ByStakeholder is each stakeholder's position, largest holding first. */
    public var byStakeholder: [CaptableHolding]?
    /** Company names the company the cap table is computed for. */
    public var company: CaptableSummaryCompany?
    /** Convertibles is the capital on SAFEs and notes that have not converted. */
    public var convertibles: CaptableConvertibles?
    /** Rounds is the fundraising rollup. */
    public var rounds: CaptableRoundTotals?
    /** Totals is the company-wide share count. */
    public var totals: CaptableTotals?

    public init(byShareClass: [CaptableClassHolding]? = nil, byStakeholder: [CaptableHolding]? = nil, company: CaptableSummaryCompany? = nil, convertibles: CaptableConvertibles? = nil, rounds: CaptableRoundTotals? = nil, totals: CaptableTotals? = nil) {
        self.byShareClass = byShareClass
        self.byStakeholder = byStakeholder
        self.company = company
        self.convertibles = convertibles
        self.rounds = rounds
        self.totals = totals
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case byShareClass
        case byStakeholder
        case company
        case convertibles
        case rounds
        case totals
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(byShareClass, forKey: .byShareClass)
        try container.encodeIfPresent(byStakeholder, forKey: .byStakeholder)
        try container.encodeIfPresent(company, forKey: .company)
        try container.encodeIfPresent(convertibles, forKey: .convertibles)
        try container.encodeIfPresent(rounds, forKey: .rounds)
        try container.encodeIfPresent(totals, forKey: .totals)
    }
}

