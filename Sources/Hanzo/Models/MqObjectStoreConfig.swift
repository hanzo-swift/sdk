//
// MqObjectStoreConfig.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Object store creation configuration. */
public struct MqObjectStoreConfig: Codable, JSONEncodable, Hashable {

    public enum Storage: String, Codable, CaseIterable {
        case file = "file"
        case memory = "memory"
    }
    public static let nameRule = StringRule(minLength: nil, maxLength: 256, pattern: "/^[a-zA-Z0-9_-]+$/")
    public static let numReplicasRule = NumericRule<Int>(minimum: 1, exclusiveMinimum: false, maximum: 5, exclusiveMaximum: false, multipleOf: nil)
    /** Store name. */
    public var name: String
    /** Maximum chunk size in bytes (default 128KB). */
    public var maxChunkSize: Int? = 131072
    /** Maximum total store size (-1 for unlimited). */
    public var maxBytes: Int64? = -1
    /** Storage backend. */
    public var storage: Storage? = .file
    /** Number of replicas. */
    public var numReplicas: Int? = 1
    /** Optional human-readable description. */
    public var description: String?

    public init(name: String, maxChunkSize: Int? = 131072, maxBytes: Int64? = -1, storage: Storage? = .file, numReplicas: Int? = 1, description: String? = nil) {
        self.name = name
        self.maxChunkSize = maxChunkSize
        self.maxBytes = maxBytes
        self.storage = storage
        self.numReplicas = numReplicas
        self.description = description
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case name
        case maxChunkSize = "max_chunk_size"
        case maxBytes = "max_bytes"
        case storage
        case numReplicas = "num_replicas"
        case description
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(name, forKey: .name)
        try container.encodeIfPresent(maxChunkSize, forKey: .maxChunkSize)
        try container.encodeIfPresent(maxBytes, forKey: .maxBytes)
        try container.encodeIfPresent(storage, forKey: .storage)
        try container.encodeIfPresent(numReplicas, forKey: .numReplicas)
        try container.encodeIfPresent(description, forKey: .description)
    }
}

