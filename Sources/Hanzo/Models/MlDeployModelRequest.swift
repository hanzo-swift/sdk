//
// MlDeployModelRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct MlDeployModelRequest: Codable, JSONEncodable, Hashable {

    public enum Runtime: String, Codable, CaseIterable {
        case vllm = "vllm"
        case triton = "triton"
        case onnx = "onnx"
        case custom = "custom"
    }
    public enum Gpu: String, Codable, CaseIterable {
        case a100 = "a100"
        case h100 = "h100"
        case l40s = "l40s"
        case t4 = "t4"
    }
    public enum Environment: String, Codable, CaseIterable {
        case dev = "dev"
        case staging = "staging"
        case production = "production"
    }
    public var modelId: String
    public var modelVersion: String?
    public var runtime: Runtime? = .vllm
    public var gpu: Gpu? = .a100
    public var replicas: Int? = 1
    public var environment: Environment? = .dev

    public init(modelId: String, modelVersion: String? = nil, runtime: Runtime? = .vllm, gpu: Gpu? = .a100, replicas: Int? = 1, environment: Environment? = .dev) {
        self.modelId = modelId
        self.modelVersion = modelVersion
        self.runtime = runtime
        self.gpu = gpu
        self.replicas = replicas
        self.environment = environment
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case modelId = "model_id"
        case modelVersion = "model_version"
        case runtime
        case gpu
        case replicas
        case environment
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(modelId, forKey: .modelId)
        try container.encodeIfPresent(modelVersion, forKey: .modelVersion)
        try container.encodeIfPresent(runtime, forKey: .runtime)
        try container.encodeIfPresent(gpu, forKey: .gpu)
        try container.encodeIfPresent(replicas, forKey: .replicas)
        try container.encodeIfPresent(environment, forKey: .environment)
    }
}

