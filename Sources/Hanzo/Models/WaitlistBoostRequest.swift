//
// WaitlistBoostRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct WaitlistBoostRequest: Sendable, Codable, ParameterConvertible, Hashable {

    /** Email identifies the entry to boost. Either this or RefCode is required. */
    public var email: String?
    /** Points is how many points to award. Must be positive — this seam exists to move someone UP toward the cutoff. */
    public var points: Int?
    /** Reason is the operator's justification. Not sent to the engine; it is recorded on the audit row, which is the point of asking for it. */
    public var reason: String?
    /** RefCode identifies the entry by its referral code, when the email is unknown. */
    public var refCode: String?
    /** Waitlist is the waitlist slug the grant lands on. Required. */
    public var waitlist: String?

    public init(email: String? = nil, points: Int? = nil, reason: String? = nil, refCode: String? = nil, waitlist: String? = nil) {
        self.email = email
        self.points = points
        self.reason = reason
        self.refCode = refCode
        self.waitlist = waitlist
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case email
        case points
        case reason
        case refCode
        case waitlist
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(email, forKey: .email)
        try container.encodeIfPresent(points, forKey: .points)
        try container.encodeIfPresent(reason, forKey: .reason)
        try container.encodeIfPresent(refCode, forKey: .refCode)
        try container.encodeIfPresent(waitlist, forKey: .waitlist)
    }
}

