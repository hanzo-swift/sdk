//
// SlackJoinOut.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct SlackJoinOut: Sendable, Codable, ParameterConvertible, Hashable {

    /** Already counts the channels it was a member of before, kept apart from Joined because only one of the two is a change. */
    public var already: Int64?
    /** Failed is per-channel, so one refusal does not hide the rest of the walk. */
    public var failed: [JoinFailure]?
    /** Joined names the channels this run walked into — the change it made. */
    public var joined: [String]?
    /** Listed is every public, unarchived channel the workspace has. */
    public var listed: Int64?

    public init(already: Int64? = nil, failed: [JoinFailure]? = nil, joined: [String]? = nil, listed: Int64? = nil) {
        self.already = already
        self.failed = failed
        self.joined = joined
        self.listed = listed
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case already
        case failed
        case joined
        case listed
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(already, forKey: .already)
        try container.encodeIfPresent(failed, forKey: .failed)
        try container.encodeIfPresent(joined, forKey: .joined)
        try container.encodeIfPresent(listed, forKey: .listed)
    }
}

