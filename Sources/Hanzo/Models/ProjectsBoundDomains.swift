//
// ProjectsBoundDomains.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct ProjectsBoundDomains: Sendable, Codable, ParameterConvertible, Hashable {

    /** Bound is the result of THIS call, one row per host in the request: live for an already-vouched host, pending with the DNS records to publish otherwise. */
    public var bound: [ProjectsDomain]?
    /** Domains are the hostnames that are VERIFIED and routing right now, after this bind. */
    public var domains: [String]?
    /** Org is the organisation that owns the site. */
    public var org: String?
    /** Slug is the site the hosts were bound to. */
    public var slug: String?

    public init(bound: [ProjectsDomain]? = nil, domains: [String]? = nil, org: String? = nil, slug: String? = nil) {
        self.bound = bound
        self.domains = domains
        self.org = org
        self.slug = slug
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case bound
        case domains
        case org
        case slug
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(bound, forKey: .bound)
        try container.encodeIfPresent(domains, forKey: .domains)
        try container.encodeIfPresent(org, forKey: .org)
        try container.encodeIfPresent(slug, forKey: .slug)
    }
}

