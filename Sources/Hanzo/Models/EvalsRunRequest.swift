//
// EvalsRunRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct EvalsRunRequest: Codable, JSONEncodable, Hashable {

    public enum Backend: String, Codable, CaseIterable {
        case hanzo = "hanzo"
        case _do = "do"
    }
    public static let limitRule = NumericRule<Int>(minimum: nil, exclusiveMinimum: false, maximum: 100, exclusiveMaximum: false, multipleOf: nil)
    public var dataset: String
    /** Model-under-test (catalog / fine-tuned / BYOM / router) */
    public var model: String
    public var runName: String?
    public var limit: Int? = 20
    public var judge: EvalsJudgeSpec?
    public var metrics: [EvalsMetric]?
    /** Evaluation backend — native Hanzo engine or DigitalOcean GenAI */
    public var backend: Backend? = .hanzo
    /** Named preset to reuse a saved configuration */
    public var preset: String?

    public init(dataset: String, model: String, runName: String? = nil, limit: Int? = 20, judge: EvalsJudgeSpec? = nil, metrics: [EvalsMetric]? = nil, backend: Backend? = .hanzo, preset: String? = nil) {
        self.dataset = dataset
        self.model = model
        self.runName = runName
        self.limit = limit
        self.judge = judge
        self.metrics = metrics
        self.backend = backend
        self.preset = preset
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case dataset
        case model
        case runName
        case limit
        case judge
        case metrics
        case backend
        case preset
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(dataset, forKey: .dataset)
        try container.encode(model, forKey: .model)
        try container.encodeIfPresent(runName, forKey: .runName)
        try container.encodeIfPresent(limit, forKey: .limit)
        try container.encodeIfPresent(judge, forKey: .judge)
        try container.encodeIfPresent(metrics, forKey: .metrics)
        try container.encodeIfPresent(backend, forKey: .backend)
        try container.encodeIfPresent(preset, forKey: .preset)
    }
}

