//
// PubsubAPI.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

open class PubsubAPI {

    /**
     Removes one stream of the caller's org — its retained messages and its consumers with it — and answers 204 with no body.
     
     - parameter stream: (path) Stream is the stream&#39;s name, from the path. 
     - parameter apiConfiguration: The configuration for the http request.
     - returns: Void
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func deletePubsubJetstreamStreamsByStream(stream: String, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) async throws(ErrorResponse) {
        return try await deletePubsubJetstreamStreamsByStreamWithRequestBuilder(stream: stream, apiConfiguration: apiConfiguration).execute().body
    }

    /**
     Removes one stream of the caller's org — its retained messages and its consumers with it — and answers 204 with no body.
     - DELETE /v1/pubsub/jetstream/streams/{stream}
     - Removes one stream of the caller's org — its retained messages and its consumers with it — and answers 204 with no body. 404 when the org has no stream of that name.
     - Bearer Token:
       - type: http
       - name: bearer
     - parameter stream: (path) Stream is the stream&#39;s name, from the path. 
     - parameter apiConfiguration: The configuration for the http request.
     - returns: RequestBuilder<Void> 
     */
    open class func deletePubsubJetstreamStreamsByStreamWithRequestBuilder(stream: String, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) -> RequestBuilder<Void> {
        var localVariablePath = "/v1/pubsub/jetstream/streams/{stream}"
        let streamPreEscape = "\(APIHelper.mapValueToPathItem(stream))"
        let streamPostEscape = streamPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{stream}", with: streamPostEscape, options: .literal, range: nil)
        let localVariableURLString = apiConfiguration.basePath + localVariablePath
        let localVariableParameters: [String: any Sendable]? = nil

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: (any Sendable)?] = [
            :
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<Void>.Type = apiConfiguration.requestBuilderFactory.getNonDecodableBuilder()

        return localVariableRequestBuilder.init(method: "DELETE", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true, apiConfiguration: apiConfiguration)
    }

    /**
     Removes one consumer — its cursor, not the stream's messages — and answers 204 with no body.
     
     - parameter stream: (path) Stream is the stream, from the path. 
     - parameter name: (path) Name is the consumer, from the path. 
     - parameter apiConfiguration: The configuration for the http request.
     - returns: Void
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func deletePubsubJetstreamStreamsByStreamConsumersByName(stream: String, name: String, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) async throws(ErrorResponse) {
        return try await deletePubsubJetstreamStreamsByStreamConsumersByNameWithRequestBuilder(stream: stream, name: name, apiConfiguration: apiConfiguration).execute().body
    }

    /**
     Removes one consumer — its cursor, not the stream's messages — and answers 204 with no body.
     - DELETE /v1/pubsub/jetstream/streams/{stream}/consumers/{name}
     - Removes one consumer — its cursor, not the stream's messages — and answers 204 with no body. 404 when the stream or the consumer does not exist.
     - Bearer Token:
       - type: http
       - name: bearer
     - parameter stream: (path) Stream is the stream, from the path. 
     - parameter name: (path) Name is the consumer, from the path. 
     - parameter apiConfiguration: The configuration for the http request.
     - returns: RequestBuilder<Void> 
     */
    open class func deletePubsubJetstreamStreamsByStreamConsumersByNameWithRequestBuilder(stream: String, name: String, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) -> RequestBuilder<Void> {
        var localVariablePath = "/v1/pubsub/jetstream/streams/{stream}/consumers/{name}"
        let streamPreEscape = "\(APIHelper.mapValueToPathItem(stream))"
        let streamPostEscape = streamPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{stream}", with: streamPostEscape, options: .literal, range: nil)
        let namePreEscape = "\(APIHelper.mapValueToPathItem(name))"
        let namePostEscape = namePreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{name}", with: namePostEscape, options: .literal, range: nil)
        let localVariableURLString = apiConfiguration.basePath + localVariablePath
        let localVariableParameters: [String: any Sendable]? = nil

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: (any Sendable)?] = [
            :
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<Void>.Type = apiConfiguration.requestBuilderFactory.getNonDecodableBuilder()

        return localVariableRequestBuilder.init(method: "DELETE", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true, apiConfiguration: apiConfiguration)
    }

    /**
     Removes one bucket of the caller's org — every key and every revision with it — and answers 204 with no body.
     
     - parameter bucket: (path) Bucket is the bucket&#39;s name, from the path. 
     - parameter apiConfiguration: The configuration for the http request.
     - returns: Void
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func deletePubsubKvByBucket(bucket: String, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) async throws(ErrorResponse) {
        return try await deletePubsubKvByBucketWithRequestBuilder(bucket: bucket, apiConfiguration: apiConfiguration).execute().body
    }

    /**
     Removes one bucket of the caller's org — every key and every revision with it — and answers 204 with no body.
     - DELETE /v1/pubsub/kv/{bucket}
     - Removes one bucket of the caller's org — every key and every revision with it — and answers 204 with no body. 404 when the org has no bucket of that name.
     - Bearer Token:
       - type: http
       - name: bearer
     - parameter bucket: (path) Bucket is the bucket&#39;s name, from the path. 
     - parameter apiConfiguration: The configuration for the http request.
     - returns: RequestBuilder<Void> 
     */
    open class func deletePubsubKvByBucketWithRequestBuilder(bucket: String, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) -> RequestBuilder<Void> {
        var localVariablePath = "/v1/pubsub/kv/{bucket}"
        let bucketPreEscape = "\(APIHelper.mapValueToPathItem(bucket))"
        let bucketPostEscape = bucketPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{bucket}", with: bucketPostEscape, options: .literal, range: nil)
        let localVariableURLString = apiConfiguration.basePath + localVariablePath
        let localVariableParameters: [String: any Sendable]? = nil

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: (any Sendable)?] = [
            :
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<Void>.Type = apiConfiguration.requestBuilderFactory.getNonDecodableBuilder()

        return localVariableRequestBuilder.init(method: "DELETE", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true, apiConfiguration: apiConfiguration)
    }

    /**
     Delete removes one key — a delete marker in the key's history, so watchers see it and Get answers 404 — and answers 204 with no body.
     
     - parameter bucket: (path) Bucket is the bucket, from the path. 
     - parameter key: (path) Key is the key, from the path. 
     - parameter apiConfiguration: The configuration for the http request.
     - returns: Void
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func deletePubsubKvByBucketByKey(bucket: String, key: String, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) async throws(ErrorResponse) {
        return try await deletePubsubKvByBucketByKeyWithRequestBuilder(bucket: bucket, key: key, apiConfiguration: apiConfiguration).execute().body
    }

    /**
     Delete removes one key — a delete marker in the key's history, so watchers see it and Get answers 404 — and answers 204 with no body.
     - DELETE /v1/pubsub/kv/{bucket}/{key}
     - Delete removes one key — a delete marker in the key's history, so watchers see it and Get answers 404 — and answers 204 with no body. 404 when the bucket does not exist.
     - Bearer Token:
       - type: http
       - name: bearer
     - parameter bucket: (path) Bucket is the bucket, from the path. 
     - parameter key: (path) Key is the key, from the path. 
     - parameter apiConfiguration: The configuration for the http request.
     - returns: RequestBuilder<Void> 
     */
    open class func deletePubsubKvByBucketByKeyWithRequestBuilder(bucket: String, key: String, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) -> RequestBuilder<Void> {
        var localVariablePath = "/v1/pubsub/kv/{bucket}/{key}"
        let bucketPreEscape = "\(APIHelper.mapValueToPathItem(bucket))"
        let bucketPostEscape = bucketPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{bucket}", with: bucketPostEscape, options: .literal, range: nil)
        let keyPreEscape = "\(APIHelper.mapValueToPathItem(key))"
        let keyPostEscape = keyPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{key}", with: keyPostEscape, options: .literal, range: nil)
        let localVariableURLString = apiConfiguration.basePath + localVariablePath
        let localVariableParameters: [String: any Sendable]? = nil

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: (any Sendable)?] = [
            :
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<Void>.Type = apiConfiguration.requestBuilderFactory.getNonDecodableBuilder()

        return localVariableRequestBuilder.init(method: "DELETE", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true, apiConfiguration: apiConfiguration)
    }

    /**
     Returns the org's streams, sorted by name.
     
     - parameter apiConfiguration: The configuration for the http request.
     - returns: StreamPage
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func getPubsubJetstreamStreams(apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) async throws(ErrorResponse) -> StreamPage {
        return try await getPubsubJetstreamStreamsWithRequestBuilder(apiConfiguration: apiConfiguration).execute().body
    }

    /**
     Returns the org's streams, sorted by name.
     - GET /v1/pubsub/jetstream/streams
     - Returns the org's streams, sorted by name.  A stream is the durable log: it captures every message published to its subjects and retains them by its own limits, independent of any consumer. The listing is org-scoped server-side — one org can never see another's streams, and the platform's own planes never appear.
     - Bearer Token:
       - type: http
       - name: bearer
     - parameter apiConfiguration: The configuration for the http request.
     - returns: RequestBuilder<StreamPage> 
     */
    open class func getPubsubJetstreamStreamsWithRequestBuilder(apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) -> RequestBuilder<StreamPage> {
        let localVariablePath = "/v1/pubsub/jetstream/streams"
        let localVariableURLString = apiConfiguration.basePath + localVariablePath
        let localVariableParameters: [String: any Sendable]? = nil

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: (any Sendable)?] = [
            :
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<StreamPage>.Type = apiConfiguration.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true, apiConfiguration: apiConfiguration)
    }

    /**
     Returns one stream of the caller's org — its configuration and its live state (messages, bytes, sequence range, consumer count).
     
     - parameter stream: (path) Stream is the stream&#39;s name, from the path. 
     - parameter apiConfiguration: The configuration for the http request.
     - returns: StreamRecord
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func getPubsubJetstreamStreamsByStream(stream: String, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) async throws(ErrorResponse) -> StreamRecord {
        return try await getPubsubJetstreamStreamsByStreamWithRequestBuilder(stream: stream, apiConfiguration: apiConfiguration).execute().body
    }

    /**
     Returns one stream of the caller's org — its configuration and its live state (messages, bytes, sequence range, consumer count).
     - GET /v1/pubsub/jetstream/streams/{stream}
     - Returns one stream of the caller's org — its configuration and its live state (messages, bytes, sequence range, consumer count). 404 when the org has no stream of that name.
     - Bearer Token:
       - type: http
       - name: bearer
     - parameter stream: (path) Stream is the stream&#39;s name, from the path. 
     - parameter apiConfiguration: The configuration for the http request.
     - returns: RequestBuilder<StreamRecord> 
     */
    open class func getPubsubJetstreamStreamsByStreamWithRequestBuilder(stream: String, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) -> RequestBuilder<StreamRecord> {
        var localVariablePath = "/v1/pubsub/jetstream/streams/{stream}"
        let streamPreEscape = "\(APIHelper.mapValueToPathItem(stream))"
        let streamPostEscape = streamPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{stream}", with: streamPostEscape, options: .literal, range: nil)
        let localVariableURLString = apiConfiguration.basePath + localVariablePath
        let localVariableParameters: [String: any Sendable]? = nil

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: (any Sendable)?] = [
            :
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<StreamRecord>.Type = apiConfiguration.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true, apiConfiguration: apiConfiguration)
    }

    /**
     Returns one stream's consumers, sorted by name.
     
     - parameter stream: (path) Stream is the stream&#39;s name, from the path. 
     - parameter apiConfiguration: The configuration for the http request.
     - returns: ConsumerPage
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func getPubsubJetstreamStreamsByStreamConsumers(stream: String, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) async throws(ErrorResponse) -> ConsumerPage {
        return try await getPubsubJetstreamStreamsByStreamConsumersWithRequestBuilder(stream: stream, apiConfiguration: apiConfiguration).execute().body
    }

    /**
     Returns one stream's consumers, sorted by name.
     - GET /v1/pubsub/jetstream/streams/{stream}/consumers
     - Returns one stream's consumers, sorted by name. 404 when the org has no stream of that name.
     - Bearer Token:
       - type: http
       - name: bearer
     - parameter stream: (path) Stream is the stream&#39;s name, from the path. 
     - parameter apiConfiguration: The configuration for the http request.
     - returns: RequestBuilder<ConsumerPage> 
     */
    open class func getPubsubJetstreamStreamsByStreamConsumersWithRequestBuilder(stream: String, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) -> RequestBuilder<ConsumerPage> {
        var localVariablePath = "/v1/pubsub/jetstream/streams/{stream}/consumers"
        let streamPreEscape = "\(APIHelper.mapValueToPathItem(stream))"
        let streamPostEscape = streamPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{stream}", with: streamPostEscape, options: .literal, range: nil)
        let localVariableURLString = apiConfiguration.basePath + localVariablePath
        let localVariableParameters: [String: any Sendable]? = nil

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: (any Sendable)?] = [
            :
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<ConsumerPage>.Type = apiConfiguration.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true, apiConfiguration: apiConfiguration)
    }

    /**
     Returns one consumer of one org stream — its configuration and its cursor: delivered and acknowledged sequences, pending and redelivered counts.
     
     - parameter stream: (path) Stream is the stream, from the path. 
     - parameter name: (path) Name is the consumer, from the path. 
     - parameter apiConfiguration: The configuration for the http request.
     - returns: ConsumerRecord
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func getPubsubJetstreamStreamsByStreamConsumersByName(stream: String, name: String, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) async throws(ErrorResponse) -> ConsumerRecord {
        return try await getPubsubJetstreamStreamsByStreamConsumersByNameWithRequestBuilder(stream: stream, name: name, apiConfiguration: apiConfiguration).execute().body
    }

    /**
     Returns one consumer of one org stream — its configuration and its cursor: delivered and acknowledged sequences, pending and redelivered counts.
     - GET /v1/pubsub/jetstream/streams/{stream}/consumers/{name}
     - Returns one consumer of one org stream — its configuration and its cursor: delivered and acknowledged sequences, pending and redelivered counts. 404 when the stream or the consumer does not exist.
     - Bearer Token:
       - type: http
       - name: bearer
     - parameter stream: (path) Stream is the stream, from the path. 
     - parameter name: (path) Name is the consumer, from the path. 
     - parameter apiConfiguration: The configuration for the http request.
     - returns: RequestBuilder<ConsumerRecord> 
     */
    open class func getPubsubJetstreamStreamsByStreamConsumersByNameWithRequestBuilder(stream: String, name: String, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) -> RequestBuilder<ConsumerRecord> {
        var localVariablePath = "/v1/pubsub/jetstream/streams/{stream}/consumers/{name}"
        let streamPreEscape = "\(APIHelper.mapValueToPathItem(stream))"
        let streamPostEscape = streamPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{stream}", with: streamPostEscape, options: .literal, range: nil)
        let namePreEscape = "\(APIHelper.mapValueToPathItem(name))"
        let namePostEscape = namePreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{name}", with: namePostEscape, options: .literal, range: nil)
        let localVariableURLString = apiConfiguration.basePath + localVariablePath
        let localVariableParameters: [String: any Sendable]? = nil

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: (any Sendable)?] = [
            :
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<ConsumerRecord>.Type = apiConfiguration.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true, apiConfiguration: apiConfiguration)
    }

    /**
     Get returns one key's current value and revision.
     
     - parameter bucket: (path) Bucket is the bucket, from the path. 
     - parameter key: (path) Key is the key, from the path. 
     - parameter apiConfiguration: The configuration for the http request.
     - returns: KvEntry
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func getPubsubKvByBucketByKey(bucket: String, key: String, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) async throws(ErrorResponse) -> KvEntry {
        return try await getPubsubKvByBucketByKeyWithRequestBuilder(bucket: bucket, key: key, apiConfiguration: apiConfiguration).execute().body
    }

    /**
     Get returns one key's current value and revision.
     - GET /v1/pubsub/kv/{bucket}/{key}
     - Get returns one key's current value and revision. 404 when the bucket does not exist, the key was never written, or its latest revision is a delete.
     - Bearer Token:
       - type: http
       - name: bearer
     - parameter bucket: (path) Bucket is the bucket, from the path. 
     - parameter key: (path) Key is the key, from the path. 
     - parameter apiConfiguration: The configuration for the http request.
     - returns: RequestBuilder<KvEntry> 
     */
    open class func getPubsubKvByBucketByKeyWithRequestBuilder(bucket: String, key: String, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) -> RequestBuilder<KvEntry> {
        var localVariablePath = "/v1/pubsub/kv/{bucket}/{key}"
        let bucketPreEscape = "\(APIHelper.mapValueToPathItem(bucket))"
        let bucketPostEscape = bucketPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{bucket}", with: bucketPostEscape, options: .literal, range: nil)
        let keyPreEscape = "\(APIHelper.mapValueToPathItem(key))"
        let keyPostEscape = keyPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{key}", with: keyPostEscape, options: .literal, range: nil)
        let localVariableURLString = apiConfiguration.basePath + localVariablePath
        let localVariableParameters: [String: any Sendable]? = nil

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: (any Sendable)?] = [
            :
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<KvEntry>.Type = apiConfiguration.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true, apiConfiguration: apiConfiguration)
    }

    /**
     History returns one key's retained revisions, oldest first — every put and every delete marker up to the bucket's History depth.
     
     - parameter bucket: (path) Bucket is the bucket, from the path. 
     - parameter key: (path) Key is the key, from the path. 
     - parameter apiConfiguration: The configuration for the http request.
     - returns: KvPage
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func getPubsubKvByBucketByKeyHistory(bucket: String, key: String, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) async throws(ErrorResponse) -> KvPage {
        return try await getPubsubKvByBucketByKeyHistoryWithRequestBuilder(bucket: bucket, key: key, apiConfiguration: apiConfiguration).execute().body
    }

    /**
     History returns one key's retained revisions, oldest first — every put and every delete marker up to the bucket's History depth.
     - GET /v1/pubsub/kv/{bucket}/{key}/history
     - History returns one key's retained revisions, oldest first — every put and every delete marker up to the bucket's History depth. 404 when the bucket does not exist or the key was never written.
     - Bearer Token:
       - type: http
       - name: bearer
     - parameter bucket: (path) Bucket is the bucket, from the path. 
     - parameter key: (path) Key is the key, from the path. 
     - parameter apiConfiguration: The configuration for the http request.
     - returns: RequestBuilder<KvPage> 
     */
    open class func getPubsubKvByBucketByKeyHistoryWithRequestBuilder(bucket: String, key: String, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) -> RequestBuilder<KvPage> {
        var localVariablePath = "/v1/pubsub/kv/{bucket}/{key}/history"
        let bucketPreEscape = "\(APIHelper.mapValueToPathItem(bucket))"
        let bucketPostEscape = bucketPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{bucket}", with: bucketPostEscape, options: .literal, range: nil)
        let keyPreEscape = "\(APIHelper.mapValueToPathItem(key))"
        let keyPostEscape = keyPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{key}", with: keyPostEscape, options: .literal, range: nil)
        let localVariableURLString = apiConfiguration.basePath + localVariablePath
        let localVariableParameters: [String: any Sendable]? = nil

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: (any Sendable)?] = [
            :
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<KvPage>.Type = apiConfiguration.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true, apiConfiguration: apiConfiguration)
    }

    /**
     Creates a durable stream capturing the given subjects and returns it.
     
     - parameter streamWrite: (body)  
     - parameter apiConfiguration: The configuration for the http request.
     - returns: StreamRecord
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func postPubsubJetstreamStreams(streamWrite: StreamWrite, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) async throws(ErrorResponse) -> StreamRecord {
        return try await postPubsubJetstreamStreamsWithRequestBuilder(streamWrite: streamWrite, apiConfiguration: apiConfiguration).execute().body
    }

    /**
     Creates a durable stream capturing the given subjects and returns it.
     - POST /v1/pubsub/jetstream/streams
     - Creates a durable stream capturing the given subjects and returns it. 409 when the org already has a stream of that name; the subjects are the org's own and cannot collide with another org's.
     - Bearer Token:
       - type: http
       - name: bearer
     - parameter streamWrite: (body)  
     - parameter apiConfiguration: The configuration for the http request.
     - returns: RequestBuilder<StreamRecord> 
     */
    open class func postPubsubJetstreamStreamsWithRequestBuilder(streamWrite: StreamWrite, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) -> RequestBuilder<StreamRecord> {
        let localVariablePath = "/v1/pubsub/jetstream/streams"
        let localVariableURLString = apiConfiguration.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: streamWrite, codableHelper: apiConfiguration.codableHelper)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: (any Sendable)?] = [
            "Content-Type": "application/json",
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<StreamRecord>.Type = apiConfiguration.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true, apiConfiguration: apiConfiguration)
    }

    /**
     Creates a durable consumer on one stream and returns it.
     
     - parameter stream: (path) Stream is the stream to consume, from the path. 
     - parameter consumerWrite: (body)  
     - parameter apiConfiguration: The configuration for the http request.
     - returns: ConsumerRecord
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func postPubsubJetstreamStreamsByStreamConsumers(stream: String, consumerWrite: ConsumerWrite, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) async throws(ErrorResponse) -> ConsumerRecord {
        return try await postPubsubJetstreamStreamsByStreamConsumersWithRequestBuilder(stream: stream, consumerWrite: consumerWrite, apiConfiguration: apiConfiguration).execute().body
    }

    /**
     Creates a durable consumer on one stream and returns it.
     - POST /v1/pubsub/jetstream/streams/{stream}/consumers
     - Creates a durable consumer on one stream and returns it. A consumer is a named cursor: it tracks what has been delivered and what is acknowledged, so many workers can share it and none sees a message twice outside redelivery. 409 when the stream already has a consumer of that name with a different configuration.
     - Bearer Token:
       - type: http
       - name: bearer
     - parameter stream: (path) Stream is the stream to consume, from the path. 
     - parameter consumerWrite: (body)  
     - parameter apiConfiguration: The configuration for the http request.
     - returns: RequestBuilder<ConsumerRecord> 
     */
    open class func postPubsubJetstreamStreamsByStreamConsumersWithRequestBuilder(stream: String, consumerWrite: ConsumerWrite, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) -> RequestBuilder<ConsumerRecord> {
        var localVariablePath = "/v1/pubsub/jetstream/streams/{stream}/consumers"
        let streamPreEscape = "\(APIHelper.mapValueToPathItem(stream))"
        let streamPostEscape = streamPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{stream}", with: streamPostEscape, options: .literal, range: nil)
        let localVariableURLString = apiConfiguration.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: consumerWrite, codableHelper: apiConfiguration.codableHelper)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: (any Sendable)?] = [
            "Content-Type": "application/json",
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<ConsumerRecord>.Type = apiConfiguration.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true, apiConfiguration: apiConfiguration)
    }

    /**
     Fetch pulls the next batch from a consumer and acknowledges it — the request/response way to consume a stream.
     
     - parameter stream: (path) Stream is the stream, from the path. 
     - parameter name: (path) Name is the consumer, from the path. 
     - parameter fetchQuery: (body)  
     - parameter apiConfiguration: The configuration for the http request.
     - returns: MessagePage
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func postPubsubJetstreamStreamsByStreamConsumersByNameNext(stream: String, name: String, fetchQuery: FetchQuery, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) async throws(ErrorResponse) -> MessagePage {
        return try await postPubsubJetstreamStreamsByStreamConsumersByNameNextWithRequestBuilder(stream: stream, name: name, fetchQuery: fetchQuery, apiConfiguration: apiConfiguration).execute().body
    }

    /**
     Fetch pulls the next batch from a consumer and acknowledges it — the request/response way to consume a stream.
     - POST /v1/pubsub/jetstream/streams/{stream}/consumers/{name}/next
     - Fetch pulls the next batch from a consumer and acknowledges it — the request/response way to consume a stream. The hand-off is at-most-once: a message returned here is acked here, so a caller that loses the response does not see it again. Workers needing at-least-once delivery consume the same consumer over the NATS port, where acks are theirs to send. An empty batch after the wait is an empty page, not an error.
     - Bearer Token:
       - type: http
       - name: bearer
     - parameter stream: (path) Stream is the stream, from the path. 
     - parameter name: (path) Name is the consumer, from the path. 
     - parameter fetchQuery: (body)  
     - parameter apiConfiguration: The configuration for the http request.
     - returns: RequestBuilder<MessagePage> 
     */
    open class func postPubsubJetstreamStreamsByStreamConsumersByNameNextWithRequestBuilder(stream: String, name: String, fetchQuery: FetchQuery, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) -> RequestBuilder<MessagePage> {
        var localVariablePath = "/v1/pubsub/jetstream/streams/{stream}/consumers/{name}/next"
        let streamPreEscape = "\(APIHelper.mapValueToPathItem(stream))"
        let streamPostEscape = streamPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{stream}", with: streamPostEscape, options: .literal, range: nil)
        let namePreEscape = "\(APIHelper.mapValueToPathItem(name))"
        let namePostEscape = namePreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{name}", with: namePostEscape, options: .literal, range: nil)
        let localVariableURLString = apiConfiguration.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: fetchQuery, codableHelper: apiConfiguration.codableHelper)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: (any Sendable)?] = [
            "Content-Type": "application/json",
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<MessagePage>.Type = apiConfiguration.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true, apiConfiguration: apiConfiguration)
    }

    /**
     Creates a KV bucket and returns it.
     
     - parameter bucket: (path) Bucket is the bucket&#39;s name within the org, from the path: 1–64 of [A-Za-z0-9_], no dash. 
     - parameter bucketWrite: (body)  
     - parameter apiConfiguration: The configuration for the http request.
     - returns: BucketRecord
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func postPubsubKvByBucket(bucket: String, bucketWrite: BucketWrite, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) async throws(ErrorResponse) -> BucketRecord {
        return try await postPubsubKvByBucketWithRequestBuilder(bucket: bucket, bucketWrite: bucketWrite, apiConfiguration: apiConfiguration).execute().body
    }

    /**
     Creates a KV bucket and returns it.
     - POST /v1/pubsub/kv/{bucket}
     - Creates a KV bucket and returns it. A bucket is keyed state on the same durable plane as the streams: each key holds up to History revisions, entries can expire by TTL, and watchers on the NATS port see every write. 409 when the org already has a bucket of that name.
     - Bearer Token:
       - type: http
       - name: bearer
     - parameter bucket: (path) Bucket is the bucket&#39;s name within the org, from the path: 1–64 of [A-Za-z0-9_], no dash. 
     - parameter bucketWrite: (body)  
     - parameter apiConfiguration: The configuration for the http request.
     - returns: RequestBuilder<BucketRecord> 
     */
    open class func postPubsubKvByBucketWithRequestBuilder(bucket: String, bucketWrite: BucketWrite, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) -> RequestBuilder<BucketRecord> {
        var localVariablePath = "/v1/pubsub/kv/{bucket}"
        let bucketPreEscape = "\(APIHelper.mapValueToPathItem(bucket))"
        let bucketPostEscape = bucketPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{bucket}", with: bucketPostEscape, options: .literal, range: nil)
        let localVariableURLString = apiConfiguration.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: bucketWrite, codableHelper: apiConfiguration.codableHelper)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: (any Sendable)?] = [
            "Content-Type": "application/json",
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<BucketRecord>.Type = apiConfiguration.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true, apiConfiguration: apiConfiguration)
    }

    /**
     Publish puts one message on the org's bus.
     
     - parameter busPublish: (body)  
     - parameter apiConfiguration: The configuration for the http request.
     - returns: BusAck
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func postPubsubPublish(busPublish: BusPublish, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) async throws(ErrorResponse) -> BusAck {
        return try await postPubsubPublishWithRequestBuilder(busPublish: busPublish, apiConfiguration: apiConfiguration).execute().body
    }

    /**
     Publish puts one message on the org's bus.
     - POST /v1/pubsub/publish
     - Publish puts one message on the org's bus. When a stream captures the subject the write is DURABLE — the receipt names the stream and sequence only after JetStream has it on storage, and a repeated Nats-Msg-Id header within the dedup window answers duplicate instead of storing twice. When nothing captures it, the message goes out core NATS: delivered to current subscribers, receipt {ok}, nothing retained.
     - Bearer Token:
       - type: http
       - name: bearer
     - parameter busPublish: (body)  
     - parameter apiConfiguration: The configuration for the http request.
     - returns: RequestBuilder<BusAck> 
     */
    open class func postPubsubPublishWithRequestBuilder(busPublish: BusPublish, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) -> RequestBuilder<BusAck> {
        let localVariablePath = "/v1/pubsub/publish"
        let localVariableURLString = apiConfiguration.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: busPublish, codableHelper: apiConfiguration.codableHelper)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: (any Sendable)?] = [
            "Content-Type": "application/json",
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<BusAck>.Type = apiConfiguration.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true, apiConfiguration: apiConfiguration)
    }

    /**
     Request sends one request on the org's bus and waits for one reply — the synchronous half of pub/sub, for callers speaking to a responder subscribed on the NATS port.
     
     - parameter busRequest: (body)  
     - parameter apiConfiguration: The configuration for the http request.
     - returns: BusMessage
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func postPubsubRequest(busRequest: BusRequest, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) async throws(ErrorResponse) -> BusMessage {
        return try await postPubsubRequestWithRequestBuilder(busRequest: busRequest, apiConfiguration: apiConfiguration).execute().body
    }

    /**
     Request sends one request on the org's bus and waits for one reply — the synchronous half of pub/sub, for callers speaking to a responder subscribed on the NATS port.
     - POST /v1/pubsub/request
     - Request sends one request on the org's bus and waits for one reply — the synchronous half of pub/sub, for callers speaking to a responder subscribed on the NATS port. 404 when nobody is listening on the subject; 408 when a responder exists but no reply arrived within the timeout.
     - Bearer Token:
       - type: http
       - name: bearer
     - parameter busRequest: (body)  
     - parameter apiConfiguration: The configuration for the http request.
     - returns: RequestBuilder<BusMessage> 
     */
    open class func postPubsubRequestWithRequestBuilder(busRequest: BusRequest, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) -> RequestBuilder<BusMessage> {
        let localVariablePath = "/v1/pubsub/request"
        let localVariableURLString = apiConfiguration.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: busRequest, codableHelper: apiConfiguration.codableHelper)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: (any Sendable)?] = [
            "Content-Type": "application/json",
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<BusMessage>.Type = apiConfiguration.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true, apiConfiguration: apiConfiguration)
    }

    /**
     Rewrites a stream's configuration — subjects, limits, discard — and returns the updated stream.
     
     - parameter stream: (path) Stream is the stream to update, from the path. 
     - parameter streamUpdate: (body)  
     - parameter apiConfiguration: The configuration for the http request.
     - returns: StreamRecord
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func putPubsubJetstreamStreamsByStream(stream: String, streamUpdate: StreamUpdate, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) async throws(ErrorResponse) -> StreamRecord {
        return try await putPubsubJetstreamStreamsByStreamWithRequestBuilder(stream: stream, streamUpdate: streamUpdate, apiConfiguration: apiConfiguration).execute().body
    }

    /**
     Rewrites a stream's configuration — subjects, limits, discard — and returns the updated stream.
     - PUT /v1/pubsub/jetstream/streams/{stream}
     - Rewrites a stream's configuration — subjects, limits, discard — and returns the updated stream. It is a PUT: the spec sent replaces the spec held, with one reading for the enums a caller omits — an empty storage, retention or discard keeps the stream's current one, because JetStream holds storage and retention immutable and refuses a change with a 400 rather than this door pretending it took.
     - Bearer Token:
       - type: http
       - name: bearer
     - parameter stream: (path) Stream is the stream to update, from the path. 
     - parameter streamUpdate: (body)  
     - parameter apiConfiguration: The configuration for the http request.
     - returns: RequestBuilder<StreamRecord> 
     */
    open class func putPubsubJetstreamStreamsByStreamWithRequestBuilder(stream: String, streamUpdate: StreamUpdate, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) -> RequestBuilder<StreamRecord> {
        var localVariablePath = "/v1/pubsub/jetstream/streams/{stream}"
        let streamPreEscape = "\(APIHelper.mapValueToPathItem(stream))"
        let streamPostEscape = streamPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{stream}", with: streamPostEscape, options: .literal, range: nil)
        let localVariableURLString = apiConfiguration.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: streamUpdate, codableHelper: apiConfiguration.codableHelper)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: (any Sendable)?] = [
            "Content-Type": "application/json",
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<StreamRecord>.Type = apiConfiguration.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "PUT", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true, apiConfiguration: apiConfiguration)
    }

    /**
     Put sets one key to one value and returns the revision the write created.
     
     - parameter bucket: (path) Bucket is the bucket, from the path. 
     - parameter key: (path) Key is the key, from the path. 
     - parameter kvWrite: (body)  
     - parameter apiConfiguration: The configuration for the http request.
     - returns: KvAck
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func putPubsubKvByBucketByKey(bucket: String, key: String, kvWrite: KvWrite, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) async throws(ErrorResponse) -> KvAck {
        return try await putPubsubKvByBucketByKeyWithRequestBuilder(bucket: bucket, key: key, kvWrite: kvWrite, apiConfiguration: apiConfiguration).execute().body
    }

    /**
     Put sets one key to one value and returns the revision the write created.
     - PUT /v1/pubsub/kv/{bucket}/{key}
     - Put sets one key to one value and returns the revision the write created. Writes are versioned: each put is a new revision and the bucket retains up to its History of them per key.
     - Bearer Token:
       - type: http
       - name: bearer
     - parameter bucket: (path) Bucket is the bucket, from the path. 
     - parameter key: (path) Key is the key, from the path. 
     - parameter kvWrite: (body)  
     - parameter apiConfiguration: The configuration for the http request.
     - returns: RequestBuilder<KvAck> 
     */
    open class func putPubsubKvByBucketByKeyWithRequestBuilder(bucket: String, key: String, kvWrite: KvWrite, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) -> RequestBuilder<KvAck> {
        var localVariablePath = "/v1/pubsub/kv/{bucket}/{key}"
        let bucketPreEscape = "\(APIHelper.mapValueToPathItem(bucket))"
        let bucketPostEscape = bucketPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{bucket}", with: bucketPostEscape, options: .literal, range: nil)
        let keyPreEscape = "\(APIHelper.mapValueToPathItem(key))"
        let keyPostEscape = keyPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{key}", with: keyPostEscape, options: .literal, range: nil)
        let localVariableURLString = apiConfiguration.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: kvWrite, codableHelper: apiConfiguration.codableHelper)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: (any Sendable)?] = [
            "Content-Type": "application/json",
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<KvAck>.Type = apiConfiguration.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "PUT", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true, apiConfiguration: apiConfiguration)
    }
}
