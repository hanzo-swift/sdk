//
// SitesAPI.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

open class SitesAPI {

    /**
     Build a site from a brief and deploy it
     
     - parameter projectsBuildSiteRequest: (body)  
     - returns: ProjectsSiteDeployResult
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func projectsBuildSite(projectsBuildSiteRequest: ProjectsBuildSiteRequest) async throws -> ProjectsSiteDeployResult {
        return try await projectsBuildSiteWithRequestBuilder(projectsBuildSiteRequest: projectsBuildSiteRequest).execute().body
    }

    /**
     Build a site from a brief and deploy it
     - POST /v1/sites
     - Generate a self-contained, mobile-responsive static site from a natural-language `brief` (one inference call), then deploy it through the one publish core. Writes into the SAME org-scoped projects store as `/v1/projects`: it ensures a `project` (framework `static`) for the resolved slug and records a `deployment`. `/v1/sites` is the brief-build/raw-manifest entrypoint; `/v1/projects/{slug}/deploy` is the artifact/git entrypoint — one store, one publish core, not a second copy of project state. The brief is capped at 8 KiB. Responds `200` with the published-site result. 
     - Bearer Token:
       - type: http
       - name: bearerAuth
     - parameter projectsBuildSiteRequest: (body)  
     - returns: RequestBuilder<ProjectsSiteDeployResult> 
     */
    open class func projectsBuildSiteWithRequestBuilder(projectsBuildSiteRequest: ProjectsBuildSiteRequest) -> RequestBuilder<ProjectsSiteDeployResult> {
        let localVariablePath = "/v1/sites"
        let localVariableURLString = HanzoAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: projectsBuildSiteRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<ProjectsSiteDeployResult>.Type = HanzoAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Deploy a raw file manifest
     
     - parameter projectsDeploySiteRequest: (body)  
     - returns: ProjectsSiteDeployResult
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func projectsDeploySite(projectsDeploySiteRequest: ProjectsDeploySiteRequest) async throws -> ProjectsSiteDeployResult {
        return try await projectsDeploySiteWithRequestBuilder(projectsDeploySiteRequest: projectsDeploySiteRequest).execute().body
    }

    /**
     Deploy a raw file manifest
     - POST /v1/sites/deploy
     - Deploy a caller-supplied file manifest (the same `{path, content}` shape the brief build emits) through the one publish core. Every file passes the same guards as the brief build: index.html required at the root; absolute and traversal paths rejected; a mobile viewport tag injected into HTML when absent. Writes into the SAME org-scoped projects store as `/v1/projects`: it ensures a `project` (framework `static`) for the resolved slug and records a `deployment`. `/v1/sites` is the brief-build/raw-manifest entrypoint; `/v1/projects/{slug}/deploy` is the artifact/git entrypoint — one store, one publish core, not a second copy of project state. Responds `200` with the published-site result. 
     - Bearer Token:
       - type: http
       - name: bearerAuth
     - parameter projectsDeploySiteRequest: (body)  
     - returns: RequestBuilder<ProjectsSiteDeployResult> 
     */
    open class func projectsDeploySiteWithRequestBuilder(projectsDeploySiteRequest: ProjectsDeploySiteRequest) -> RequestBuilder<ProjectsSiteDeployResult> {
        let localVariablePath = "/v1/sites/deploy"
        let localVariableURLString = HanzoAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: projectsDeploySiteRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<ProjectsSiteDeployResult>.Type = HanzoAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     List the org's live sites
     
     - returns: [ProjectsSite]
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func projectsListSites() async throws -> [ProjectsSite] {
        return try await projectsListSitesWithRequestBuilder().execute().body
    }

    /**
     List the org's live sites
     - GET /v1/sites
     - List the org's `live` projects as sites at their canonical live URL `https://<slug>.<apex>`. Reads the SAME org-scoped projects store as `/v1/projects`; a project that is not `live` is omitted. 
     - Bearer Token:
       - type: http
       - name: bearerAuth
     - returns: RequestBuilder<[ProjectsSite]> 
     */
    open class func projectsListSitesWithRequestBuilder() -> RequestBuilder<[ProjectsSite]> {
        let localVariablePath = "/v1/sites"
        let localVariableURLString = HanzoAPI.basePath + localVariablePath
        let localVariableParameters: [String: Any]? = nil

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            :
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<[ProjectsSite]>.Type = HanzoAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }
}
