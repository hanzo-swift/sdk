//
// StatusAPI.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

open class StatusAPI {

    /**
     Live health status for a product
     
     - parameter product: (query) Console product slug. Must match &#x60;^[a-z0-9][a-z0-9._-]{0,62}$&#x60;. 
     - returns: ObserveStatusResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func observeGetStatus(product: String) async throws -> ObserveStatusResponse {
        return try await observeGetStatusWithRequestBuilder(product: product).execute().body
    }

    /**
     Live health status for a product
     - GET /v1/o11y/status
     - Returns a live health signal for a product: an in-cluster health probe (measured latency, `/health` then `/healthz`) corroborated by the VictoriaMetrics `up{service}` scrape gauge. `up` is set by the probe when it succeeds, else by the scrape gauge, else false. Status is infra-level (no per-org data) but still requires a validated principal. 
     - Bearer Token:
       - type: http
       - name: bearerAuth
     - parameter product: (query) Console product slug. Must match &#x60;^[a-z0-9][a-z0-9._-]{0,62}$&#x60;. 
     - returns: RequestBuilder<ObserveStatusResponse> 
     */
    open class func observeGetStatusWithRequestBuilder(product: String) -> RequestBuilder<ObserveStatusResponse> {
        let localVariablePath = "/v1/o11y/status"
        let localVariableURLString = HanzoAPI.basePath + localVariablePath
        let localVariableParameters: [String: Any]? = nil

        var localVariableUrlComponents = URLComponents(string: localVariableURLString)
        localVariableUrlComponents?.queryItems = APIHelper.mapValuesToQueryItems([
            "product": (wrappedValue: product.encodeToJSON(), isExplode: true),
        ])

        let localVariableNillableHeaders: [String: Any?] = [
            :
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<ObserveStatusResponse>.Type = HanzoAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }
}
