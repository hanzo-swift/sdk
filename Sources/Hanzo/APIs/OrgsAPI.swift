//
// OrgsAPI.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

open class OrgsAPI {

    /**
     Get lists the products an org has ENABLED — its own intent, which the console's paid-product sidebar reads to decide what to show.
     
     - parameter org: (path)  
     - parameter apiConfiguration: The configuration for the http request.
     - returns: EntitlementsView
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func getOrgsByOrgEntitlements(org: String, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) async throws(ErrorResponse) -> EntitlementsView {
        return try await getOrgsByOrgEntitlementsWithRequestBuilder(org: org, apiConfiguration: apiConfiguration).execute().body
    }

    /**
     Get lists the products an org has ENABLED — its own intent, which the console's paid-product sidebar reads to decide what to show.
     - GET /v1/orgs/{org}/entitlements
     - Get lists the products an org has ENABLED — its own intent, which the console's paid-product sidebar reads to decide what to show. It is distinct from what the org's plan ENTITLES it to (that is GET /v1/entitlements, resolved from commerce).  A caller may only read its OWN org's row; a platform super admin may read any.
     - Bearer Token:
       - type: http
       - name: bearer
     - parameter org: (path)  
     - parameter apiConfiguration: The configuration for the http request.
     - returns: RequestBuilder<EntitlementsView> 
     */
    open class func getOrgsByOrgEntitlementsWithRequestBuilder(org: String, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) -> RequestBuilder<EntitlementsView> {
        var localVariablePath = "/v1/orgs/{org}/entitlements"
        let orgPreEscape = "\(APIHelper.mapValueToPathItem(org))"
        let orgPostEscape = orgPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{org}", with: orgPostEscape, options: .literal, range: nil)
        let localVariableURLString = apiConfiguration.basePath + localVariablePath
        let localVariableParameters: [String: any Sendable]? = nil

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: (any Sendable)?] = [
            :
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<EntitlementsView>.Type = apiConfiguration.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true, apiConfiguration: apiConfiguration)
    }

    /**
     Onboard creates the caller's organization.
     
     - parameter onboardReq: (body)  
     - parameter apiConfiguration: The configuration for the http request.
     - returns: OnboardResp
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func postOrgs(onboardReq: OnboardReq, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) async throws(ErrorResponse) -> OnboardResp {
        return try await postOrgsWithRequestBuilder(onboardReq: onboardReq, apiConfiguration: apiConfiguration).execute().body
    }

    /**
     Onboard creates the caller's organization.
     - POST /v1/orgs
     - Onboard creates the caller's organization. Two flows, keyed on whether the caller already has a home org (mirrors app/onboard/route.ts):    - FIRST-RUN (no home org): create + MOVE the user in as admin, so their next     JWT carries the new owner and the cloud scopes everything to it. This is the     path a fresh OAuth sign-up takes, from the sign-up application's org.   - ADDITIONAL (owner set): create the org but do NOT move the user — a move     changes their IAM owner (stripping a SuperAdmin's status + orphaning their     current org). They reach the new org via the OrgSwitcher, which re-scopes     X-Org-Id without touching IAM membership. A personal-org request from someone     who already has an org is meaningless → 409.
     - Bearer Token:
       - type: http
       - name: bearer
     - parameter onboardReq: (body)  
     - parameter apiConfiguration: The configuration for the http request.
     - returns: RequestBuilder<OnboardResp> 
     */
    open class func postOrgsWithRequestBuilder(onboardReq: OnboardReq, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) -> RequestBuilder<OnboardResp> {
        let localVariablePath = "/v1/orgs"
        let localVariableURLString = apiConfiguration.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: onboardReq, codableHelper: apiConfiguration.codableHelper)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: (any Sendable)?] = [
            "Content-Type": "application/json",
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<OnboardResp>.Type = apiConfiguration.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true, apiConfiguration: apiConfiguration)
    }

    /**
     Post turns products on or off for an org and returns the enabled set afterwards.
     
     - parameter org: (path)  
     - parameter mutateReq: (body)  
     - parameter apiConfiguration: The configuration for the http request.
     - returns: EntitlementsView
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func postOrgsByOrgEntitlements(org: String, mutateReq: MutateReq, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) async throws(ErrorResponse) -> EntitlementsView {
        return try await postOrgsByOrgEntitlementsWithRequestBuilder(org: org, mutateReq: mutateReq, apiConfiguration: apiConfiguration).execute().body
    }

    /**
     Post turns products on or off for an org and returns the enabled set afterwards.
     - POST /v1/orgs/{org}/entitlements
     - Post turns products on or off for an org and returns the enabled set afterwards.  A product may only be ENABLED if the org's plan already ENTITLES it, so enabling never spends new money — a product the plan does not grant answers 402 and the console routes that to an upgrade prompt. DISABLING is never gated. A platform super admin bypasses the plan check (operator comp/grant) and may target any org; everyone else may only change their own. Commerce unreachable is a 503, never an implicit yes.
     - Bearer Token:
       - type: http
       - name: bearer
     - parameter org: (path)  
     - parameter mutateReq: (body)  
     - parameter apiConfiguration: The configuration for the http request.
     - returns: RequestBuilder<EntitlementsView> 
     */
    open class func postOrgsByOrgEntitlementsWithRequestBuilder(org: String, mutateReq: MutateReq, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) -> RequestBuilder<EntitlementsView> {
        var localVariablePath = "/v1/orgs/{org}/entitlements"
        let orgPreEscape = "\(APIHelper.mapValueToPathItem(org))"
        let orgPostEscape = orgPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{org}", with: orgPostEscape, options: .literal, range: nil)
        let localVariableURLString = apiConfiguration.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: mutateReq, codableHelper: apiConfiguration.codableHelper)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: (any Sendable)?] = [
            "Content-Type": "application/json",
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<EntitlementsView>.Type = apiConfiguration.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true, apiConfiguration: apiConfiguration)
    }
}
