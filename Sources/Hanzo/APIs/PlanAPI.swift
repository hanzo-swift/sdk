//
// PlanAPI.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

open class PlanAPI {

    /**
     Resolved World plan limits (contract echo)
     
     - parameter plan: (query) Plan id (world-free | world-pro | world-team | world-enterprise). Defaults to world-free. (optional, default to "world-free")
     - returns: WorldWorldLimits200Response
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func worldWorldLimits(plan: String? = nil) async throws -> WorldWorldLimits200Response {
        return try await worldWorldLimitsWithRequestBuilder(plan: plan).execute().body
    }

    /**
     Resolved World plan limits (contract echo)
     - GET /v1/world/limits
     - Returns the Hanzo World enforcement limits for a plan, resolved from the @hanzo/plans catalog (the single source of truth) via the world.* entitlement vocabulary. Agents and the dashboard read this to self-config against the live catalog instead of hardcoding tier numbers. Defaults to world-free; degrades to the fail-closed Free floor on a catalog outage. 
     - Bearer Token:
       - type: http
       - name: bearerAuth
     - parameter plan: (query) Plan id (world-free | world-pro | world-team | world-enterprise). Defaults to world-free. (optional, default to "world-free")
     - returns: RequestBuilder<WorldWorldLimits200Response> 
     */
    open class func worldWorldLimitsWithRequestBuilder(plan: String? = nil) -> RequestBuilder<WorldWorldLimits200Response> {
        let localVariablePath = "/v1/world/limits"
        let localVariableURLString = HanzoAPI.basePath + localVariablePath
        let localVariableParameters: [String: Any]? = nil

        var localVariableUrlComponents = URLComponents(string: localVariableURLString)
        localVariableUrlComponents?.queryItems = APIHelper.mapValuesToQueryItems([
            "plan": (wrappedValue: plan?.encodeToJSON(), isExplode: true),
        ])

        let localVariableNillableHeaders: [String: Any?] = [
            :
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<WorldWorldLimits200Response>.Type = HanzoAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }
}
