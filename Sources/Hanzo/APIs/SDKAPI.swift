//
// SDKAPI.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

open class SDKAPI {

    /**
     Enveloped secret + threshold-key operation
     
     - parameter kmsSdkEnvelope: (body)  
     - returns: AnyCodable
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func kmsSdkSecretsOp(kmsSdkEnvelope: KmsSdkEnvelope) async throws -> AnyCodable {
        return try await kmsSdkSecretsOpWithRequestBuilder(kmsSdkEnvelope: kmsSdkEnvelope).execute().body
    }

    /**
     Enveloped secret + threshold-key operation
     - POST /v1/sdk/secrets
     - The SDK-facing native secrets plane. A single RPC endpoint: the body is a signed Envelope (ML-DSA-65 over the request), and the OPERATION is the signed `op` field — never the URL. Authorization is consensus-native: the caller's mnemonic-derived NodeID must be in the validator authority (reads: OpSecretGet 0x0040, OpSecretList 0x0042, OpVerify 0x0051) and, for writes (OpSecretPut 0x0041 — also the rotate op, OpSecretDelete 0x0043, OpSign 0x0050), additionally in the operator authority. Every envelope is verified for signature, wall-clock freshness (±5m), and replay (per-(NodeID,nonce) ledger) before dispatch. There is NO bearer token on this surface — the ML-DSA-65 envelope IS the credential. 
     - parameter kmsSdkEnvelope: (body)  
     - returns: RequestBuilder<AnyCodable> 
     */
    open class func kmsSdkSecretsOpWithRequestBuilder(kmsSdkEnvelope: KmsSdkEnvelope) -> RequestBuilder<AnyCodable> {
        let localVariablePath = "/v1/sdk/secrets"
        let localVariableURLString = HanzoAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: kmsSdkEnvelope)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<AnyCodable>.Type = HanzoAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: false)
    }
}
