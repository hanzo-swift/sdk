//
// SbomAPI.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

open class SbomAPI {

    /**
     Health is a pure liveness probe: the service is up; datastore reflects whether the datastore store is connected.
     
     - parameter apiConfiguration: The configuration for the http request.
     - returns: SbomHealth
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func getSbomHealth(apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) async throws(ErrorResponse) -> SbomHealth {
        return try await getSbomHealthWithRequestBuilder(apiConfiguration: apiConfiguration).execute().body
    }

    /**
     Health is a pure liveness probe: the service is up; datastore reflects whether the datastore store is connected.
     - GET /v1/sbom/health
     - Health is a pure liveness probe: the service is up; datastore reflects whether the datastore store is connected. Not JWT-gated, always 200 (a disconnected datastore is degraded-but-alive; the data endpoints report that as 503).
     - Bearer Token:
       - type: http
       - name: bearer
     - parameter apiConfiguration: The configuration for the http request.
     - returns: RequestBuilder<SbomHealth> 
     */
    open class func getSbomHealthWithRequestBuilder(apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) -> RequestBuilder<SbomHealth> {
        let localVariablePath = "/v1/sbom/health"
        let localVariableURLString = apiConfiguration.basePath + localVariablePath
        let localVariableParameters: [String: any Sendable]? = nil

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: (any Sendable)?] = [
            :
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<SbomHealth>.Type = apiConfiguration.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true, apiConfiguration: apiConfiguration)
    }

    /**
     Ingest persists a CycloneDX SBOM's components keyed by image digest.
     
     - parameter sbomIngest: (body)  
     - parameter apiConfiguration: The configuration for the http request.
     - returns: SbomIngested
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func postSbom(sbomIngest: SbomIngest, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) async throws(ErrorResponse) -> SbomIngested {
        return try await postSbomWithRequestBuilder(sbomIngest: sbomIngest, apiConfiguration: apiConfiguration).execute().body
    }

    /**
     Ingest persists a CycloneDX SBOM's components keyed by image digest.
     - POST /v1/sbom
     - Ingest persists a CycloneDX SBOM's components keyed by image digest. Gated to a validated SuperAdmin (owner == AdminOrg) — the canonical cloud super-admin check, which the build fleet / CI carries. Re-ingest is idempotent: rows share the (digest, name, version, purl) ORDER BY, so ReplacingMergeTree keeps the latest by ingested_at (and resolve reads FINAL).
     - Bearer Token:
       - type: http
       - name: bearer
     - parameter sbomIngest: (body)  
     - parameter apiConfiguration: The configuration for the http request.
     - returns: RequestBuilder<SbomIngested> 
     */
    open class func postSbomWithRequestBuilder(sbomIngest: SbomIngest, apiConfiguration: HanzoAPIConfiguration = HanzoAPIConfiguration.shared) -> RequestBuilder<SbomIngested> {
        let localVariablePath = "/v1/sbom"
        let localVariableURLString = apiConfiguration.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: sbomIngest, codableHelper: apiConfiguration.codableHelper)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: (any Sendable)?] = [
            "Content-Type": "application/json",
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<SbomIngested>.Type = apiConfiguration.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true, apiConfiguration: apiConfiguration)
    }
}
