//
// SendAPI.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

open class SendAPI {

    /**
     * enum for parameter sync
     */
    public enum Sync_notifyNotifySend: String, CaseIterable {
        case _true = "true"
    }

    /**
     Send a notification (generic channel)
     
     - parameter sync: (query) Must be &#x60;true&#x60;. Async dispatch is not available in the cloud fold; any other value yields &#x60;503&#x60;.  
     - parameter notifySendRequest: (body)  
     - returns: NotifyNotifySend200Response
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func notifyNotifySend(sync: Sync_notifyNotifySend, notifySendRequest: NotifySendRequest) async throws -> NotifyNotifySend200Response {
        return try await notifyNotifySendWithRequestBuilder(sync: sync, notifySendRequest: notifySendRequest).execute().body
    }

    /**
     Send a notification (generic channel)
     - POST /v1/notify/send
     - Synchronously delivers a notification. The channel is read from the request body (`channel: \"sms\"|\"email\"`). Only `sms` and `email` are supported by the cloud fold.  `sync=true` is REQUIRED. Without it the request fails closed with `503` (async dispatch is not available in the fold).  The org scope is taken from the validated principal's tenant, never from a client-supplied header. The provider is chosen from configured KMS credentials unless `provider` pins one explicitly.  RESPONSE SHAPE — for a single recipient the body is a bare `SendResponse`. For multiple recipients the body is `{\"items\": [SendResponse, ...]}`, one entry per recipient. A per-recipient terminal delivery failure is reported as a `200` with `status: \"failed\"` and an `error` message, NOT as an HTTP error. 
     - Bearer Token:
       - type: http
       - name: bearerAuth
     - parameter sync: (query) Must be &#x60;true&#x60;. Async dispatch is not available in the cloud fold; any other value yields &#x60;503&#x60;.  
     - parameter notifySendRequest: (body)  
     - returns: RequestBuilder<NotifyNotifySend200Response> 
     */
    open class func notifyNotifySendWithRequestBuilder(sync: Sync_notifyNotifySend, notifySendRequest: NotifySendRequest) -> RequestBuilder<NotifyNotifySend200Response> {
        let localVariablePath = "/v1/notify/send"
        let localVariableURLString = HanzoAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: notifySendRequest)

        var localVariableUrlComponents = URLComponents(string: localVariableURLString)
        localVariableUrlComponents?.queryItems = APIHelper.mapValuesToQueryItems([
            "sync": (wrappedValue: sync.encodeToJSON(), isExplode: true),
        ])

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<NotifyNotifySend200Response>.Type = HanzoAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     * enum for parameter sync
     */
    public enum Sync_notifyNotifySendEmail: String, CaseIterable {
        case _true = "true"
    }

    /**
     Send an email notification
     
     - parameter sync: (query) Must be &#x60;true&#x60;; otherwise &#x60;503&#x60;. 
     - parameter notifySendRequest: (body)  
     - returns: NotifyNotifySend200Response
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func notifyNotifySendEmail(sync: Sync_notifyNotifySendEmail, notifySendRequest: NotifySendRequest) async throws -> NotifyNotifySend200Response {
        return try await notifyNotifySendEmailWithRequestBuilder(sync: sync, notifySendRequest: notifySendRequest).execute().body
    }

    /**
     Send an email notification
     - POST /v1/notify/send/email
     - Convenience route that pins `channel: email`. Any `channel` in the body is overridden. Otherwise identical to `POST /v1/notify/send` — `sync=true` required, org derived from the validated principal, single vs. list response shape. 
     - Bearer Token:
       - type: http
       - name: bearerAuth
     - parameter sync: (query) Must be &#x60;true&#x60;; otherwise &#x60;503&#x60;. 
     - parameter notifySendRequest: (body)  
     - returns: RequestBuilder<NotifyNotifySend200Response> 
     */
    open class func notifyNotifySendEmailWithRequestBuilder(sync: Sync_notifyNotifySendEmail, notifySendRequest: NotifySendRequest) -> RequestBuilder<NotifyNotifySend200Response> {
        let localVariablePath = "/v1/notify/send/email"
        let localVariableURLString = HanzoAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: notifySendRequest)

        var localVariableUrlComponents = URLComponents(string: localVariableURLString)
        localVariableUrlComponents?.queryItems = APIHelper.mapValuesToQueryItems([
            "sync": (wrappedValue: sync.encodeToJSON(), isExplode: true),
        ])

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<NotifyNotifySend200Response>.Type = HanzoAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     * enum for parameter sync
     */
    public enum Sync_notifyNotifySendSms: String, CaseIterable {
        case _true = "true"
    }

    /**
     Send an SMS notification
     
     - parameter sync: (query) Must be &#x60;true&#x60;; otherwise &#x60;503&#x60;. 
     - parameter notifySendRequest: (body)  
     - returns: NotifyNotifySend200Response
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func notifyNotifySendSms(sync: Sync_notifyNotifySendSms, notifySendRequest: NotifySendRequest) async throws -> NotifyNotifySend200Response {
        return try await notifyNotifySendSmsWithRequestBuilder(sync: sync, notifySendRequest: notifySendRequest).execute().body
    }

    /**
     Send an SMS notification
     - POST /v1/notify/send/sms
     - Convenience route that pins `channel: sms`. Any `channel` in the body is overridden. Otherwise identical to `POST /v1/notify/send` — `sync=true` required, org derived from the validated principal, single vs. list response shape. 
     - Bearer Token:
       - type: http
       - name: bearerAuth
     - parameter sync: (query) Must be &#x60;true&#x60;; otherwise &#x60;503&#x60;. 
     - parameter notifySendRequest: (body)  
     - returns: RequestBuilder<NotifyNotifySend200Response> 
     */
    open class func notifyNotifySendSmsWithRequestBuilder(sync: Sync_notifyNotifySendSms, notifySendRequest: NotifySendRequest) -> RequestBuilder<NotifyNotifySend200Response> {
        let localVariablePath = "/v1/notify/send/sms"
        let localVariableURLString = HanzoAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: notifySendRequest)

        var localVariableUrlComponents = URLComponents(string: localVariableURLString)
        localVariableUrlComponents?.queryItems = APIHelper.mapValuesToQueryItems([
            "sync": (wrappedValue: sync.encodeToJSON(), isExplode: true),
        ])

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<NotifyNotifySend200Response>.Type = HanzoAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }
}
